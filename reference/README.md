# Read-only reference data

This directory contains provenance and verification manifests for legacy material.
Large payloads are local, read-only, and intentionally excluded from Git. Runtime
builds must use an explicit resource profile and must not depend on this directory.

# 临时文件 / 编译产物
*.ncb
*.opt
*.aps
*.clw
*.plg
*.suo
*.user
*.pdb
*.ilk
*.exp
*.lib
*.obj
*.idb
*.pch
*.sdf
*.opensdf
*.cachefile
*.VC.db
*.ipch
*.tmp

# Visual Studio
.vs/
[Bb]in/
[Oo]bj/
[Dd]ebug/
[Rr]elease/
[Dd]ebug*/
[Rr]elease*/
x64/
x86/
Win32/
ARM/
ARM64/
[Bb]uild/
[Ll]og/
[Oo]ut/
[Oo]bj*/
[Ll]ib/

# 编译输出目录（项目已有）
[Dd]ebug/
[Rr]elease/
Log/
[Output]Tools/
cworking/
SWorking/

# 旧的 .vsscc/.vssscc/.scc (Perforce)
*.vsscc
*.vssscc
*.scc
*.vspscc
*.dbmdl
*.dbproj

# IDE 配置
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# 资源运行时文件（用户数据/截图/日志）
*.UIS
*.UII
*.dat
# 注意：*.bin 资源文件**不要**过滤（游戏资源）

# Windows 快捷方式（启动脚本已重写）
*.lnk

# 已编译可执行文件（保留在 SWorking/ 和 cworking/，但不进 git）
*.exe
*.dll
*.pyd
*.so
*.dylib

# 但是注意：保留一些重要的 DLL 在版本控制里
# SS3D*.dll
# BaseNetwork.dll
# HSEL.dll
# SoundLib.dll

# 数据库备份（保留目录结构，不进二进制内容）
# MHCMEMBER.bak
# MHGAME.bak
# MHLOG.bak

# 临时解压/转换目录
tmp/
work/
.cache/
build_artifacts/
build_legacy_hsel/
# Phase 7 — legacy library CMake build dirs (one per migrated lib)
build_yhlibrary/
build_basenetwork/
build_dbthread/
build_ziparchive/
build_hsel/
build_filestorage/
build_net/
build_*.test*/

# 测试覆盖率报告
coverage/
*.gcov
*.gcda
*.gcno
htmlcov/
.pytest_cache/

# Python（脚本工具）
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.env

# Node（如果有）
node_modules/
package-lock.json

# CMake（生成的）
cmake-build-*/
build-*/
out/

# Conan
conanfile.lock
conan_cache/

# vcpkg
vcpkg_installed/

# 系统生成
[Bb]uild[Ll]og.*
*.[Pp]ublish.xml
*.pfx
*.publishsettings
# Generated DB files
*.db
*.db-shm
*.db-wal
test_conn.db


# Render demo output
render_demo_*.txt
# Test artifacts
test-extract/

# MSBuild full logs (non-deterministic /MP ordering + CRLF on Windows;
# regenerate on demand with build_*.py)
modern/scripts/build_*_full.txt
modern/build_*_log.txt

# Mavis / planning scratch (session metadata, never source-of-truth)
.mavis/
plan_launch.txt
roster.txt
*plan_decision*.json


# Allow our log module (was matching Log/ rule)
!modern/src/log/
!modern/include/mxh/log/
!modern/tests/unit/log/

# Phase 7.5p — agent probe binaries (one-off size/layout/HSEL smoke probes)
modern/scripts/probe_artifacts/

# Phase 7.5p — agent scratch dir (desktop + session leftover archives)
# Subdirs named `<source>_<YYYY-MM-DD>/` are ad-hoc agent scratch archives.
# Not source-of-truth; safe to `mavis-trash` any time.
# Only the archive subdirs are ignored — root-level files (e.g. tools
# referenced by MODERNIZATION_PLAN 复现命令) are tracked normally.
modern/scratch/_archive_*/
!modern/scratch/*.py
!modern/scratch/*.md
# Runtime logs from running server tools in the scratch dir (login_handler,
# MoxianLoginServer main, etc.) — these are session-ephemeral, not
# source-of-truth. The .py / .md exceptions above still allow root-level
# tools and the README to be tracked.
modern/scratch/*.log

# Phase 10.4 — agent tool directories and session leftovers.
# These are dev-environment state, not source-of-truth. They contain
# editor state, IDE config, runtime caches, and SQL Server installer
# binaries. Mavis-side; never commit.
.mimocode/
.qoder/
.qoder-work/
.github-workflows-cache/
Testing/
plans/
AI_*.md
.agents/
*.iml
# GitHub workflows can land later via a dedicated PR if useful
.github/

# SQL Server Express installer (~300 MB) — too large for git, downloaded
# from Microsoft and installed via the docker/init-db/init.sh path.
SQLEXPR_x64_ENU/
SQLEXPR_*/

# deploy/ — legacy game content (binary, large, mirrored in SWorking/).
# Per AGENTS.md trap #1 "保持游戏内容 1:1", the game data lives in
# 墨香【源码】/SWorking/ and 墨香【源码配套资源】/. The deploy/ tree is
# just an alternate copy of that legacy data (binary .bin/.bmhm/.ttb +
# legacy 2008 .exe/.dll + game audio .wav). Git LFS would be the right
# tool for binary game data, but this repo is plain git. So:
#   - commit deploy/{DEPLOY_GUIDE.md, scripts/, database/, server/ServerSet/1/}
#     (operational + ~5 KB config)
#   - exclude the rest (binary blobs mirrored in SWorking/).
deploy/client/
deploy/server/Distribute/
deploy/server/Agent/
deploy/server/Map/
deploy/server/Monitoring/
deploy/server/RMTool/

# Whitelist: keep the operational deploy/ subdirectories (override above).
!deploy/DEPLOY_GUIDE.md
!deploy/scripts/
!deploy/database/
!deploy/server/ServerSet/

# Intermediate cmake build dirs (under modern/).
# The actual build dir is modern/build/; the build_* dirs are leftover
# experiments and should not be committed.
modern/build_INET/
modern/build_MHConsole/
modern/build_net32/
modern/build_inet/
modern/build_mhconsole/
modern/build_net32*/

# Agent / IDE editing scratch files.
modern/edit_*.py
modern/test_*output.txt
modern/test_*stderr.txt
modern/test_*stdout.txt
# Phase 10.4.10: build stdout logs for one-off map release / debug runs
modern/scripts/build_*.txt
# Phase 10.4.10: agent scratch helpers (incremental mod analysis, etc.)
modern/update_plan_*.py
~$*
*.bak
*.tmp
*.swp


# modern/scripts/ — byproducts of build scripts (NOT in git tracking)
# Real source files (.py/.cpp/.h/.json tools) are tracked; everything else here is output.
modern/scripts/*.log
modern/scripts/*_full.txt
modern/scripts/*_run.txt
modern/scripts/*_run*.txt
modern/scripts/stdout*.txt
modern/scripts/stderr*.txt
modern/scripts/run_demo_*.txt
modern/scripts/render_demo_*.txt
modern/scripts/verifier_*.txt
modern/scripts/build_*_attempt*.log
modern/scripts/_bak_*/
modern/scripts/*_commit_msg.txt
# Tracked exceptions (early phase commit msg templates): basenetwork/dbthread/net
# Untracked (ignored here): filestorage/hsel/phase75d
modern/scripts/plan_decision*.json


# Verification matrix

This is the single current evidence index. A row is complete only when it has a commit, resource-profile manifest, command, artifact ID and the required evidence level.

## Evidence levels

| Level | Meaning |
|---|---|
| E0 | File/package bytes and SHA-256 recorded |
| E1 | Format decoded with complete field coverage |
| E2 | Runtime object instantiated with no missing dependency |
| E3 | Runtime renders or plays the asset |
| E4 | Correct player action triggers the asset and state change |
| E5 | Genuine legacy comparison plus human review passes |

## Current gates

| Gate | Scope | Status | Required evidence |
|---|---|---|---|
| G0 | Git/source/resource protection | PASS (42d14756) | governance bundle + backup refs + dual-copy hash report |
| G1 | Profiles, paths, secrets | PASS (42d14756) | explicit profiles, path scan, secret fallback removed |
| G2 | Clean tree and current docs | PASS (42d14756, f8ffa596) | clean status, successful Debug build, historical docs removed, remaining machine-path defaults eliminated |
| G3 | Client state/display/loading foundation | PASS (77e598cb,339351a0, b2993ae4) | client state tests, typed loading coordinator, atomic settings, explicit runtime profile validation |
| G4 | Launcher/login/display transition | Partial | transactional 800×600 → saved post-login mode is implemented; genuine human run, updater GUI and failure matrix still required |
| G5 | Character and loading path | Partial | appearance fields and typed loading path; 3D preview/video still required |
| G6 | Map10 world presentation | Partial | Map10 GUI/entity/loading path is E3; legacy/modern goldens and final zero-placeholder review remain |
| G7 | Core UI live binding | Partial | live stats, inventory, shop, quest and character-info HUD paths have E3/E4 evidence; per-dialog action/error/close coverage remains |
| G8 | Movement/combat/loot/map change | Partial | Map10 combat/loot/relog and Map10→Map12 route probes are E3/E4; two-client human scenario remains |
| G9 | Effects and audio | Partial | skill EffectRuntime/audio cue probe and manifest are E3/E4; event video/audio trace and legacy comparison remain |
| G10 | Full assets/maps/UI | Not started | coverage matrix with no unexplained gaps |
| G11 | RC stability | Not started | clean-machine, fault injection, 24-hour soak |

## Invalid evidence

The following are never sufficient for an E5 claim: parser totals, CTest totals, headless state tests, auto-login screenshots, or modern-vs-modern SSIM.

## Recent reproducible evidence

| Evidence ID | Commit | Profile | Command | Result |
|---|---|---|---|---|
| `EVID-20260825-client-158` | `cd06a441` (includes `5936e521`, `6b1ef0f0`) | test fixtures | `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_brief=1` | 158/158 passed |
| `EVID-20260825-render-282` | `cd06a441` | test fixtures + `playdh-current` package parsing | `modern/build/tests/unit/render/mxh_render_tests.exe --gtest_brief=1` | 282/282 passed; synthetic missing-model warnings remain test-only |
| `EVID-20260825-governance` | `cd06a441` | repository manifests | `python scripts/check-project-governance.py` | passed |
| `EVID-20260825-release-client` | `dc5b567a` | `playdh-current` | `scripts\\build-modern.bat Release mxh_client` | x86 Release build passed; automation/credential CLI rejected under `NDEBUG` |
| `EVID-20260825-render-texture-gate` | `11539716` | `playdh-current` | `modern/build/tests/unit/render/mxh_render_tests.exe --gtest_brief=1` | 282/282 passed; Release world loading now rejects unresolved terrain/STM textures |
| `EVID-20260825-map10-closure` | `999b8965` | `playdh-current` | `modern/build/tests/unit/render/mxh_render_tests.exe --gtest_brief=1` | 283/283 passed; real Map10 HFL/STM and texture dependencies resolve through filesystem/PAK storage |
| `EVID-20260825-canonical-resource-tests` | `1de46337` | `playdh-current` | `ctest -C Debug --test-dir modern/build -R "PackFile|ReadMhBin_" --output-on-failure` | 100% passed; 3 tests skipped only for explicitly absent optional files |
| `EVID-20260825-reference-map10-start` | `cd06a441` | `sworking-2008-reference` (development-only) | `start_modern.ps1 -Mode start -ResourceProfileId sworking-2008-reference -MapNumber 10 -AllowDevFallbacks` | MapServer loaded 114 groups / 228 spawns; reference profile is not release-enabled |
| `EVID-20260825-current-map10-blocked-v2` | `5e75e43c` | `playdh-current` (`server-size-prefixed-opaque-v1`) | `start_modern.ps1 -Mode start -ResourceProfileId playdh-current -MapNumber 10 -Backend sqlite` | correctly fails closed with an explicit unsupported-encoding diagnostic for the current 22,766-byte `Monster_10.bin`; no reference-profile substitution occurs and no server process remains running |
| `EVID-20260825-full-ctest-v2` | `38fe583c` | local Debug build | `ctest -C Debug --test-dir modern/build --output-on-failure` | 12,134 registered; 12,134 passed; 19 tests skip for explicit missing/optional fixtures. The size-prefixed opaque-server-profile detector and classic MHFileEx regression tests are included; this is still not an RC gate because the skipped runtime/resource gates and human path remain open. |
| `EVID-20260825-settings-durable` | `7f262a5a` | local Windows temp profile | `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=ClientSettings.*` | settings round-trip passed after `FlushFileBuffers` and `MoveFileExW(MOVEFILE_WRITE_THROUGH|MOVEFILE_REPLACE_EXISTING)`; display/audio settings publication is durable and atomic. |
| `EVID-20260825-ui-resolution-propagation` | `13f838a5` | client state fixture | `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=CEngine.UiResolutionModeStartsAtLoginAndCanBePromotedAfterDisplayCommit` | passed; login starts at 800×600 and the committed post-login mode is propagated to CharSelect, CharMake and GameIn UI loading instead of remaining hard-coded to Low800x600. |
| `EVID-20260825-client-160` | `13f838a5` | test fixtures + PlayDH UI resources | `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_brief=1` | 160/160 passed, including login recovery, state transfer, UI runtime, GameIn interactions and post-login resolution mode propagation. |
| `EVID-20260825-mouse-capture` | `1d9c4983` | client window input path | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_brief=1` | x86 Debug client build and 160/160 client tests passed; button gestures now capture/release the HWND so drag rotation and release remain deterministic at client/pillarbox edges. |
| `EVID-20260825-target-picking` | `36cbe1be` | in-game input fixture | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=InGamePlayable.* --gtest_brief=1` | 6/6 playable-input tests passed; a left click now selects the live monster under the cursor, while empty-world clicks retain nearest-target fallback. |
| `EVID-20260825-mapchange-failure-state` | `571f880e` | client state fixture | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=CMapChange.* --gtest_brief=1` | 3/3 MapChange tests passed; progress, cancellation, zero-step rejection and explicit failure text are now observable by the host state coordinator. |
| `EVID-20260825-camera-wheel-zoom` | `27f54850` | in-game input + DX11 terrain camera | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=InGamePlayable.MouseWheelZoomIsBounded --gtest_brief=1` | camera distance is now driven by `WM_MOUSEWHEEL`, clamped to 3–12 world units, and propagated to the terrain camera. |
| `EVID-20260825-map-bounds` | `dc6c63ee` | Map HFL dimensions + movement fixture | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=InGameMovement.* --gtest_brief=1` | movement now accepts map-specific width/height bounds from the loaded HFL instead of relying only on the fixed 50,000-unit fallback; 7/7 movement tests passed. |
| `EVID-20260825-static-collision-query` | `35bf5600` | STM collision descriptors + in-game movement fixture | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/mxh_compat_tests.exe --gtest_filter=StmStaticModel.* --gtest_brief=1` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=InGamePlayable.StaticCollisionQueryRejectsCandidateStep --gtest_brief=1` | STM collision-model bounding descriptors are parsed and exposed through an explicit query; blocked candidate movement is rejected. Exact triangle collision remains pending the legacy `.col` sidecar integration. |
| `EVID-20260825-placeholder-release-gate` | `ad314a54` | `playdh-current` client render policy | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/render/mxh_render_tests.exe --gtest_filter=EntityScene* --gtest_brief=1` + `python scripts/check-project-governance.py` | 6/6 EntityScene regression tests and governance check passed; RenderBox stand-ins are disabled by default and only enabled by explicit `--debug-ui-bounds`. Missing models remain counted and fail the Map10 visual gate rather than being presented as game assets. |
| `EVID-20260825-server-profile-fail-closed` | `2969aad9` | `playdh-current` and `sworking-2008-reference` | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/mxh_compat_tests.exe --gtest_filter=MhFileEx.*` + `modern/build/tools/MoxianMapServer/mxh_map_server_KOR.exe --map 10 --resource-root modern\\data\\PlayDH --db modern\\build\\runtime\\plan_probe.db` | 10/10 MHFileEx profile tests passed; current 22,766-byte opaque Monster_10.bin is rejected through the explicit profile API, while the reference profile remains selectable only by explicit ID. MapServer exits with a clear unsupported-encoding diagnostic instead of positional-decoding garbage. |
| `EVID-20260825-profile-propagation` | `e41fe1a6` | `playdh-current` | `pwsh -NoProfile -File deploy\\scripts\\start_modern.ps1 -Mode start -DryRun -ResourceProfileId playdh-current -MapNumber 10` | Dry-run shows the selected profile and `--resource-profile playdh-current` on MapServer; profile metadata now identifies server-size-prefixed-opaque-v1 separately from text encoding Big5. |
| `EVID-20260825-loading-transaction` | `9e277c01` | client loading fixture + runtime load path | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=GameLoadingCoordinator.*:CGameLoading.*:CMapChange.* --gtest_brief=1` | 8/8 loading/state tests passed; world loading reports ordered stages and leaves the previous scene render/capture state intact when descriptor, terrain, STM, sky or entity activation fails. Late progress callbacks cannot regress the visible stage count. |
| `EVID-20260825-client-profile-gate` | `cea7a845` | client profile selection | `cmd /c scripts\\build-modern.bat` + `modern/build/tools/MoxianClient/mxh_client.exe --resource-profile invalid` + `modern/build/tools/MoxianClient/mxh_client.exe --resource-profile sworking-2008-reference --resource-root modern\\data\\PlayDH` | Unknown profiles and root/profile mismatches are rejected before resource activation; human launcher now propagates the selected profile. Reference profile remains development-only in Release. |
| `EVID-20260825-playdh-manifest-clean` | `bdf9178c` | `playdh-current` canonical tree | `pwsh -NoProfile -File scripts\\generate-resource-manifest.ps1 -Root modern\\data\\PlayDH -Output reference\\manifests\\playdh-current.sha256.json -ProfileId playdh-current` + `python scripts/check-project-governance.py` | Removed four exact SQLite runtime artifacts from the canonical resource root into recoverable quarantine; regenerated manifest now covers 5,135 files / 1,681,141,995 bytes and contains no `moxian_*.db`, WAL or SHM entries. Governance passed. |

| `EVID-20260825-launcher-shell` | `d2d5a525` | `playdh-current` | `cmd /c scripts\\build-modern.bat` + `python scripts/check-project-governance.py` | New Win32 launcher builds in the clean Debug graph. It persists only non-secret launch settings, passes explicit profile/display parameters to the client, never accepts credentials, and blocks check/repair until a signed manifest transport is configured. |
| `EVID-20260825-display-dpi-transition` | `1d8c6e06` | client display path | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_brief=1` | x86 Debug build and 167/167 client tests passed; post-login client-area sizing now uses the current monitor DPI when available and retains rollback behavior on failure. |
| `EVID-20260825-launcher-settings-json` | `472c656d` | launcher settings | `cmd /c scripts\\build-modern.bat` + `python scripts/check-project-governance.py` | Launcher settings now use `%LOCALAPPDATA%\\Moxian\\settings.json`, validate the profile and dimensions on load, and atomically replace the file on save; no credential field or command-line credential path exists. |
| `EVID-20260825-launcher-client-resolution` | `f3ea2006` | local launcher/client layout | `cmd /c scripts\\build-modern.bat` + `python scripts/check-project-governance.py` | Launcher now probes only the explicit release name `MoxianClient.exe` and modern build name `mxh_client.exe`, with a clear failure if neither exists; no arbitrary executable search is permitted. |
| `EVID-20260825-launcher-resource-root` | `80f4d0b8` | local launcher/resource layout | `cmd /c scripts\\build-modern.bat` + `python scripts/check-project-governance.py` | Launcher passes an explicitly discovered canonical `data\\PlayDH` root when present, preventing client startup from depending on the caller's working directory. |
| `EVID-20260825-launcher-settings-merge` | `a8c7cb7e` | shared launcher/client settings | `cmd /c scripts\\build-modern.bat` + `python scripts/check-project-governance.py` | Launcher updates only profile/display keys in the shared JSON and preserves server, locale, audio, VSync and last-account fields; it no longer overwrites client-owned settings. |
| `EVID-20260825-sfx-channel` | `95aa21dd` | `playdh-current` SoundList | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/mxh_compat_tests.exe --gtest_filter=SfxPlayer.* --gtest_brief=1` | Added an independent one-shot WAV channel using SoundList metadata, Media Foundation PCM decoding and XAudio2; streaming BGM entries are rejected by the SFX resolver; 2/2 tests passed. |
| `EVID-20260825-sfx-entry-volume` | `25f0e847` | `playdh-current` SoundList | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/mxh_compat_tests.exe --gtest_filter=SfxPlayer.* --gtest_brief=1` + `python scripts/check-project-governance.py` | SFX gain now consumes per-entry SoundList volume multiplied by the global SFX volume, with safe default for zero/legacy entries; 2/2 tests passed. |
| `EVID-20260825-sfx-distance-gain` | `4a549cdd` | `playdh-current` SoundList | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/mxh_compat_tests.exe --gtest_filter=SfxPlayer.* --gtest_brief=1` + `python scripts/check-project-governance.py` | Added `playAt()` and deterministic linear attenuation using each entry's `min_distance`/`max_distance`; 3/3 SFX tests passed. |
| `EVID-20260825-sfx-looping` | `3f6b21bc` | `playdh-current` SoundList | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/mxh_compat_tests.exe --gtest_filter=SfxPlayer.* --gtest_brief=1` + `python scripts/check-project-governance.py` | XAudio2 now honors each WAV entry's `loop` flag, retaining one-shot end-of-stream behavior for non-looping entries; 3/3 SFX tests passed. |
| `EVID-20260825-bgm-entry-volume` | `113e2d3a` | `playdh-current` SoundList | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/mxh_compat_tests.exe --gtest_filter=BgmPlayer.*:SfxPlayer.* --gtest_brief=1` + `python scripts/check-project-governance.py` | BGM playback now consumes per-entry volume together with the global BGM volume; BGM/SFX lifecycle suite 8/8 passed. |
| `EVID-20260825-spatial-combat-cues` | `c6cd5b04` | client combat/audio path | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=InGamePlayable.LeftClickAttacksNearestLiveMonster:InGamePlayable.LeftClickPrefersMonsterUnderCursor --gtest_brief=1` + `python scripts/check-project-governance.py` | Attack and skill cues now carry player-to-target distance through CEngine into `SfxPlayer::playAt`; targeted combat input tests 2/2 passed. |
| `EVID-20260825-ui-click-sfx` | `9a5899ff` | `playdh-current` client input/audio path | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_brief=1` + `python scripts/check-project-governance.py` | Client initializes the real SFX channel, selects a data-driven non-streaming UI/button WAV, and triggers it on completed left-click input; 167/167 client tests and governance passed. |
| `EVID-20260825-combat-audio-cues` | `52ad2231` | client combat/audio seam | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=InGamePlayable.LeftClickAttacksNearestLiveMonster:InGamePlayable.LeftClickPrefersMonsterUnderCursor --gtest_brief=1` + `python scripts/check-project-governance.py` | CEngine now exposes semantic audio cues; attack and quick-skill sends emit real combat cues, with null-engine-safe unit behavior; targeted 2/2 tests passed. |
| `EVID-20260825-party-create-runtime` | `0b351883` | `playdh-current` + local Debug build | `cmd /c scripts\\build-modern.bat Debug` + `ctest -C Debug --test-dir modern/build -R "ClientUiRuntime|PartyDataPlane|PartyProtocolTest" --output-on-failure` | Party.CreateSyn/BreakupSyn are now authoritative map-server mutations with legacy-numbered Ack/Nack payloads; client tracks party id/member count and presents visible success/failure feedback. Build passed; 63/63 selected tests passed. |
| `EVID-20260825-party-agent-route` | `db5fa654` | local Debug build | `cmd /c scripts\\build-modern.bat Debug` + `modern/build/tests/unit/server/mxh_server_handler_tests.exe --gtest_filter=MapHandlerTest.PartyCreateAndBreakupMutateAuthoritativeMapState --gtest_brief=1` | Agent now forwards Party/Guild client packets to the selected MapServer; MapHandler test covers GameIn → PartyCreateAck → duplicate CreateNack → BreakupAck. |
| `EVID-20260825-party-invite-members` | `6bdd2b23` | local Debug build | `cmd /c scripts\\build-modern.bat Debug` + `modern/build/tests/unit/server/mxh_server_handler_tests.exe --gtest_filter=MapHandlerTest.PartyCreateAndBreakupMutateAuthoritativeMapState --gtest_brief=1` + `modern/build/tests/unit/proto/mxh_protocol_tests.exe --gtest_filter=PartyProtocolTest.* --gtest_brief=1` | Party AddSyn sends a real AddInvite to the target, InviteAcceptSyn mutates authoritative membership, and Info/Ack snapshots contain the complete member count; protocol and runtime tests pass. |
| `EVID-20260825-full-ctest-party-invite` | `6bdd2b23` | local Debug build | `ctest -C Debug --test-dir modern/build --output-on-failure` | 12,201/12,201 registered tests passed; only the same explicit optional resource/SQL skips remained. No new failure after Agent/MapHandler/client Party changes. |
| `EVID-20260825-guild-create-runtime` | `a962fe5b` | local Debug build | `cmd /c scripts\\build-modern.bat Debug` + `modern/build/tests/unit/server/mxh_server_handler_tests.exe --gtest_filter=MapHandlerTest.PartyCreateAndBreakupMutateAuthoritativeMapState --gtest_brief=1` + `modern/build/tests/unit/proto/mxh_protocol_tests.exe --gtest_filter=PartyProtocolTest.*:GuildProtocolTest.* --gtest_brief=1` | Guild.CreateSyn/CreateNack/BreakupSyn are now authoritative MapHandler state changes; client tracks guild ID/member count and shows visible feedback. Runtime, protocol and governance checks passed. |
| `EVID-20260825-full-ctest-guild` | `a962fe5b` | local Debug build | `ctest -C Debug --test-dir modern/build --output-on-failure` | 12,202/12,202 registered tests passed; only the pre-existing explicit optional resource/SQL skips remained after Guild integration. |
| `EVID-20260825-guild-invite-members` | `e61f7afb` | local Debug build | `cmd /c scripts\\build-modern.bat Debug` + `modern/build/tests/unit/server/mxh_server_handler_tests.exe --gtest_filter=MapHandlerTest.PartyCreateAndBreakupMutateAuthoritativeMapState --gtest_brief=1` + `modern/build/tests/unit/proto/mxh_protocol_tests.exe --gtest_filter=GuildProtocolTest.* --gtest_brief=1` | Guild AddMemberSyn sends AddMemberInvite, InviteAccept adds the target and broadcasts Info snapshots; two-player runtime test and protocol test pass. |
| `EVID-20260825-full-ctest-guild-invite` | `e61f7afb` | local Debug build | `ctest -C Debug --test-dir modern/build --output-on-failure` | 12,202/12,202 registered tests passed; only the pre-existing explicit optional resource/SQL skips remained after Guild invitation/member integration. |
| `EVID-20260825-full-ctest-party` | `0b351883` | local Debug build | `ctest -C Debug --test-dir modern/build --output-on-failure` | 12,200/12,200 tests passed; only pre-existing explicit optional/resource/SQL skips remained. This does not upgrade any E3/E4 area to E5. |
| `EVID-20260825-human-evidence-capture` | `30aa3a9c` | `playdh-current` client window | `cmd /c scripts\\build-modern.bat` + manual `scripts\\run-human-acceptance.ps1 -ResourceProfileId playdh-current` | Client accepts an explicit evidence directory and saves settled TGA frames on an operator F12 hotkey; the runner records the directory and captured filenames in run.json, while credentials and input remain manual. This is an evidence mechanism, not an E5 result. |
| `EVID-20260825-effect-spatial-audio` | `30abe6a4` | `playdh-current` effect/audio seam | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=InGamePlayable.EffectAudioDistanceUsesAuthoritativeObjectIdentity --gtest_brief=1` | Effect SOUND events now resolve their authoritative source identity before entering the SFX distance attenuation path; unknown sources are skipped instead of being misreported as local 2D audio. |
| `EVID-20260825-full-ctest-spatial-audio` | `26ac25f1` | local x86 Debug build | `ctest -C Debug --test-dir modern/build --output-on-failure` | 12,174/12,174 registered tests passed; 19 explicitly skipped resource/environment gates remain, including current opaque Monster_10, MSSQL and optional legacy fixtures. This is regression evidence, not proof of a playable RC. |
| `EVID-20260825-ui-modal-resolution` | `9ed90bc1` | client UI runtime | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=ClientUiRuntime.MessageBoxCentersInCommittedResolutionMode:ClientUiRuntime.MessageBoxClosesOnEscapeAndRunsCallbackOnce --gtest_brief=1` | UI runtime now publishes the loaded ResolutionMode to its window manager; modal message boxes center in the committed 1024×768 logical canvas instead of retaining the default 1920×1080 center. |
| `EVID-20260825-staged-world-loading` | `aca79411` | `playdh-current` client loading path | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "MoxianClientE2E|GameLoadingCoordinator|CGameLoading|CMapChange"` | World activation now advances descriptor, terrain, static, sky, entity, audio and commit stages across frames, preserving the old scene until final commit and keeping the message loop responsive between stages. This is state/build evidence, not a playable Map10 result while opaque Monster_10 remains blocked. |
| `EVID-20260825-loading-exit-rollback` | `59711498` | client loading state lifecycle | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "MoxianClientE2E|GameLoadingCoordinator|CGameLoading|CMapChange"` | Leaving GameLoading/MapChange between staged frames now destroys the session and restores the previous scene flags before the next state renders; 11/11 targeted tests passed. |
| `EVID-20260825-current-server-profile-closure` | `94433abe` | `playdh-current` `Resource/Server` | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/mxh_compat_tests.exe --gtest_filter=MhFileEx.* --gtest_brief=1` | Every current server `.bin` carrying the size-prefixed opaque marker is asserted to return `UnsupportedOpaqueServerProfile`; no opaque entry may silently enter classic MHFileEx decoding. 11/11 tests passed. |
| `EVID-20260825-combat-audio-resource-mapping` | `136944e6` | `playdh-current` SoundList + combat events | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_brief=1` + `python scripts/check-project-governance.py` | UI, attack/swing/weapon, and skill/magic/mugong WAV IDs are selected independently from actual profile filenames, with first-real-WAV fallback only when a semantic name is absent; 167/167 client tests passed. |
| `EVID-20260825-semantic-audio-test` | `d9910578` | client engine audio seam | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=CEngine.* --gtest_brief=1` + `python scripts/check-project-governance.py` | CEngine semantic `Attack`, `Skill`, and `UiClick` cues are independently observable by the host callback; 2/2 targeted tests passed. |
| `EVID-20260825-pickup-audio-cue` | `d38de493` | `playdh-current` client item/audio path | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=CEngine.EmitsSemanticAudioCuesToHost --gtest_brief=1` + `python scripts/check-project-governance.py` | Confirmed `PickupAck` emits a semantic pickup cue routed through the real SFX channel; cue dispatch is unit-tested. |
| `EVID-20260825-pickup-inventory-live-state` | `67dd2647` | `playdh-current` client item path | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=InGamePlayable.MonsterObtainNotifyBecomesGroundDropAndPickupAckClearsIt --gtest_brief=1` + `python scripts/check-project-governance.py` | `PickupAck` now consumes the authoritative item/count payload, removes the ground drop, inserts the item into the first empty live inventory slot, and exposes it through `game_info()`; targeted test passed. |
| `EVID-20260825-inventory-ui-live-refresh` | `5a6d657b` | `playdh-current` GameIn UI | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_brief=1` + `python scripts/check-project-governance.py` | Opening `IN_INVENTORYDLG` now binds the real `InventoryServiceImpl` over `GameInInfo.items`, refreshes visible slots and money, and keeps the UI on the same live state as PickupAck; 168/168 client tests passed. |
| `EVID-20260825-shop-money-live-state` | `ccc20bfe` | `playdh-current` modern Map/Client | `cmd /c scripts\\build-modern.bat` + client money/shop tests + `mxh_server_handler_tests.exe --gtest_filter=MapHandlerTest.BuySynOkArmPersistsMoneyToSqliteMemory:MapHandlerTest.BuySynOkArmDeductsMoneyAndInsertsInventory --gtest_brief=1` | Successful BuySyn now publishes an authoritative `Money` message before the unchanged BuyAck/TotalInfoLocal pair; client updates HUD/inventory money and server/client shop tests passed. |
| `EVID-20260825-useack-live-state` | `74fe628a` | `playdh-current` client item path | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=InGamePlayable.UseAckConsumesLiveInventorySlotAndUpdatesVitals --gtest_brief=1` | `UseAck` now consumes the acknowledged inventory slot and applies authoritative current HP/MP to `GameInInfo`; targeted test passed. |
| `EVID-20260825-discardack-live-state` | `06bb419a` | `playdh-current` client item path | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=InGamePlayable.DiscardAckClearsLiveInventorySlot --gtest_brief=1` | `DiscardAck` now clears the confirmed inventory slot and refreshes the same live state consumed by Inventory UI; targeted test passed. |
| `EVID-20260825-moveack-live-state` | `a7d153c9` | `playdh-current` client item path | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=InGamePlayable.MoveAckReordersLiveInventorySlots --gtest_brief=1` | `MoveAck` now validates the echoed item DB id/target slot, swaps the authoritative live inventory entries, repairs slot positions, and refreshes Inventory UI; targeted test passed. |
| `EVID-20260825-sell-payout-authority` | `78e6a3a0` | ItemList/ItemManager sell data plane | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/server/mxh_shop_item_manager_tests.exe --gtest_filter=ItemSellSideEffect.SellPayout* --gtest_brief=1` | Added strict payout calculation that accepts only the authoritative `ItemInfo.SellPrice`, rejects zero quantity/price and 32-bit overflow, and never derives a price from BuyPrice; 2/2 tests passed. |
| `EVID-20260825-sell-runtime-wire` | `f2a21094` | `playdh-current` Map/Client item path | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=InGamePlayable.SellAckRemovesSoldQuantityFromLiveInventory --gtest_brief=1` | MapHandler now validates dealer proximity, authoritative ItemManager SellPrice, quantity and money overflow, mutates the runtime inventory/money, persists both, and emits SellAck + Money + TotalInfoLocal; client applies SellAck quantity changes. Runtime success still requires a loaded ItemList and a real NPC, so this row is E2/E3, not E5. |
| `EVID-20260825-sell-server-e2e` | `58f508ef` | synthetic formal ItemList + DealItem NPC on Map 7 | `cmd /c scripts\\build-modern.bat` + `mxh_server_handler_tests.exe --gtest_filter=MapHandlerTest.SellSynUsesItemListSellPriceAndPublishesState --gtest_brief=1` | Full server success path passed: loaded ItemList SellPrice, NPC at player position, bought inventory item, SellSyn, SellAck + Money + TotalInfoLocal ordering, payout 100, inventory count 0, and persistence path exercised. This is E3/E4 fixture evidence, not a real-client E5 run. |
| `EVID-20260825-loading-request-reentry` | `098bc5be` | client GameLoading state transfer | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=GameLoadingCoordinator.* --gtest_brief=1` + `python scripts/check-project-governance.py` | Fixed a real re-entry deadlock: the main loop now consumes any newly pending GameEntryRequest instead of suppressing it after the first request. Three coordinator tests pass, including a second request for a different character/map with progress reset. This remains E3/E4 engineering evidence, not E5. |
| `EVID-20260825-npc-role-routing` | `6a749df4` | `playdh-current` client/server NPC interaction path | `cmd /c scripts\\build-modern.bat` + `mxh_client_tests.exe --gtest_filter=InGamePlayable.BKeyOpensNearestNpcShopThenBuyClickSelectsCatalogItem:InGamePlayable.BKeyDoesNotOpenShopForQuestNpc --gtest_brief=1` + `mxh_server_handler_tests.exe --gtest_filter=MapHandlerTest.SpeechSynShopListFeedsClientThenBuySynDebitsMoney --gtest_brief=1` + `python scripts/check-project-governance.py` | NPC interaction now routes by legacy role: Dealer/Bobusang opens the shop, while quest NPCs no longer fall through to a shop request; the server only emits ShopList for a dealer catalog. Existing dealer client/server flows and the new quest-NPC regression pass. This is E3/E4 engineering evidence, not E5. |
| `EVID-20260825-agent-map-route-directory` | `025147a4` | AgentHandler map routing | `cmd /c scripts\\build-modern.bat` + `mxh_server_handler_tests.exe --gtest_filter=AgentHandlerTest.GameInUsesMapSpecificServerRoute:AgentHandlerTest.RegisterSessionStoresUserCharMap --gtest_brief=1` + `python scripts/check-project-governance.py` | Added a map-number keyed MapServer route directory. Existing `set_map_server` remains the default-map route, while GameIn and ordinary forwarded gameplay messages choose the registered endpoint for the session map and fall back safely to the default. Two routing tests pass. This is E3 infrastructure evidence, not E5. |
| `EVID-20260825-agent-map-route-runtime-config` | `bf34b7e2` | MoxianAgentServer map route configuration | `cmd /c scripts\\build-modern.bat` + `modern/build/tools/MoxianAgentServer/mxh_agent_server_KOR.exe --help` + `python scripts/check-project-governance.py` | AgentServer now accepts explicit `--map-server-map MAP=HOST:PORT` entries, opens independent legacy-framed MapServer connections, registers them in the map route directory, and cleans them up without replacing a failed endpoint with another map. Existing `--map-server H:P` remains the default-map route. This is E3 infrastructure evidence, not E5. |
| `EVID-20260825-agent-change-map-forward` | `30e2c945` | AgentHandler ChangeMapSyn route | `cmd /c scripts\\build-modern.bat` + `mxh_server_handler_tests.exe --gtest_filter=AgentHandlerTest.ChangeMapUsesTargetRouteAndClosesCurrentRoute --gtest_brief=1` + `python scripts/check-project-governance.py` | Legacy `ChangeMapSyn` now selects the target map route, sends channel-transfer `GameOutSyn` to the current map, rebuilds a target-map `GameInSyn`, and returns `ChangeMapAck` after the target accepts the send; unavailable targets receive `ChangeMapNack`. Target-route handoff test passed. This is E3/E4 transport evidence; client scene activation and E5 gameplay remain pending. |
| `EVID-20260825-client-change-map-wire` | `1c482e3f` | client MapChange NPC input | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_brief=1` + `python scripts/check-project-governance.py` | MapChange NPC interaction now emits the protected `UserConn::ChangeMapSyn` protocol with the legacy baseline destination map and preserves the current scene until an acknowledgement; ChangeMapAck/Nack are explicitly handled. Full client suite passed. Scene loading, target GameInAck activation and E5 runtime travel remain pending. |
| `EVID-20260825-client-change-map-activation` | `ef3a177e` | client GameIn/MapChange state transition | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_brief=1` + `python scripts/check-project-governance.py` | ChangeMapAck now creates a typed GameEntryRequest and requests GameLoading for the target map; load failure returns to the still-active GameIn scene instead of forcing CharSelect. Full 175-test client suite and governance passed. This remains E3/E4 state evidence; target-map real-server and E5 human travel are pending. |
| `EVID-20260825-target-map-handler-bootstrap` | `e2f2e9ae` | MapHandler target GameIn path | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/server/mxh_server_handler_tests.exe --gtest_filter=MapHandlerTest.TargetMapGameInUsesTargetMapAndEmitsAck --gtest_brief=1` | Direct target-map handler test proves the synthesized GameInSyn creates an active runtime snapshot with map 10 and emits a real GameInAck for the requested character. This is E3 target-handler evidence; a live three-server human map travel remains pending. |
| `EVID-20260825-change-map-missing-route` | `b7701f39` | AgentHandler ChangeMap failure path | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/server/mxh_server_handler_tests.exe --gtest_filter=AgentHandlerTest.ChangeMapWithoutTargetRouteReturnsNackAndKeepsCurrentMap --gtest_brief=1` + `python scripts/check-project-governance.py` | ChangeMap now requires an explicitly registered target-map route; an unavailable destination returns ChangeMapNack without sending GameOutSyn to the current map. This is E3/E4 failure-path evidence; live-server and human travel remain pending. |
| `EVID-20260825-full-ctest-map-routing` | `b7701f39` | local x86 Debug build | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure` | 12,162/12,162 registered tests passed; 19 tests skipped only for explicit missing/optional fixtures (including MSSQL and current resource blockers). This is repository regression evidence, not an E5 playable claim. |
| `EVID-20260825-skill-effect-timeline` | `154594df` | client skill protocol path | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=InGameEffects.ConfirmedSkillMessagesProduceDeterministicTimeline --gtest_brief=1` + `python scripts/check-project-governance.py` | Confirmed SkillStartAck and SkillSingleResult now produce bounded CastRelease → Hit → End events with skill, target, damage and hit result fields; targeted test and governance passed. This is E3 presentation-seam evidence, not proof that BEFF assets are rendered or E5-complete. |
| `EVID-20260825-agent-map-handler-handoff` | `d8ae8c50` | Agent + MapHandler change-map path | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/server/mxh_server_handler_tests.exe --gtest_filter=AgentHandlerTest.ChangeMapBootstrapsTargetMapHandlerAndRelaysGameInAck --gtest_brief=1` | In-process integration proves ChangeMapSyn sends GameOutSyn to the current route, target MapHandler creates an active map=10 runtime snapshot, and its GameInAck is relayed to the client alongside ChangeMapAck. This is E3/E4 infrastructure evidence; no live human E5 claim. |
| `EVID-20260825-charselect-real-preview-scene` | `WORKTREE` | `playdh-current` character presentation | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build -R "CharSelect|CCharSelectState" --output-on-failure` + `python scripts/check-project-governance.py` | CharSelect now owns a real EntityScene preview loaded from the current resource profile. The selected ChrTotalInfo appearance is converted to ScenePlayer and rendered with a dedicated 4:3 camera before screen-space UI; no RenderBox/debug avatar is enabled. 27/27 CharSelect tests passed. This is E3 runtime wiring; a legacy-client visual comparison and human E5 run remain pending. |
| `EVID-20260825-charmake-real-preview-scene` | `WORKTREE` | `playdh-current` character creation presentation | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build -R "CharSelect|CCharSelectState|CharacterMake" --output-on-failure` + `python scripts/check-project-governance.py` | CharMake now feeds the live form model (sex, face, hair and equipped item selections) into the same real EntityScene preview camera, so option changes are reflected before submission. No synthetic avatar geometry is enabled. This is E3 runtime wiring; legacy visual comparison and human E5 remain pending. |
| `EVID-20260825-real-loading-dialog` | `WORKTREE` | `playdh-current` `Image/InterfaceScript/NewLoadDlg.bin` | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_brief=1` + `python scripts/check-project-governance.py` | GameLoading now loads and renders the shipped `NewLoadDlg.bin` tree through `ClientUiRuntime` on state entry, while the coordinator continues updating live progress and failure context. The old text-only loading placeholder is no longer the product render path. 176/176 client tests and governance passed. This is E3 UI integration; real Map10 human loading and E5 visual comparison remain pending. |
| `EVID-20260825-human-acceptance-runner` | `WORKTREE` | tracked `scripts/run-human-acceptance.ps1` | `powershell -NoProfile -Command "Get-Command -LiteralPath .\\scripts\\run-human-acceptance.ps1"` + `python scripts/check-project-governance.py` | Added a tracked human-only runner that supplies no credentials or synthetic input, uses a unique run directory, passes the explicit resource profile, refuses to take over an existing managed server state, verifies every started PID against its exact executable path, captures stdout/stderr, and only stops processes it recorded. This is a safety/runbook gate; it does not itself constitute human E5 evidence. |
| `EVID-20260825-full-ctest-human-runner` | `6457b250` | local x86 Debug build | `ctest -C Debug --test-dir modern/build --output-on-failure` | Full registered regression completed: 12,164/12,164 tests passed in 368.54s. 19 tests were skipped only for explicit missing/optional fixtures, including the known opaque current Monster_10 profile, MSSQL environment gates, and optional legacy samples. This remains repository regression evidence, not proof of a playable RC or E5 visual fidelity. |
| `EVID-20260825-mapchange-real-state` | `b3c01a64` | local x86 Debug build | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "GameState|CMainGame|InGameState"` | MapChange now owns a real loading UI runtime and shares the GameLoadingCoordinator pipeline; ChangeMapAck enters MapChange, keeps the active scene until target activation succeeds, and uses the same failure rollback path. Build and 13 targeted tests passed. A parallel full CTest run had two fixture timing failures; both passed on immediate serial rerun. This is E3/state evidence only; runtime Map10 remains blocked by the current opaque Monster_10 profile. |
| `EVID-20260825-effect-catalog` | `51d513de` | `playdh-current` | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "EffectCatalog|CInGameState"` | Runtime now indexes the shipped Effect.pak dependency closure (MOD/CHX/ANM) plus loose BEFF/BEFL assets without changing bytes or inventing IDs. Real profile catalog test passed with >1,000 BEFF assets and >1,000 packed effect dependencies; CInGameState logs explicit catalog coverage at GameIn start. This is E1/E2 resource evidence, not proof of BEFF playback or E5 visual fidelity. |
| `EVID-20260825-beff-structure-decode` | `cf431bbe` | `playdh-current` | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "EffectCatalog"` | Loose BEFF scripts now decode through the recovered MHFileEx header/type/size transform and expose verified `#MAXEFFECTUNIT`, `#MAXTRIGGER`, and dependency directives. Real catalog test passed with >1,000 decoded BEFF scripts and a decoded `become_h.beff` summary. This is E1 parser/structure evidence; trigger execution and visual playback remain incomplete. |
| `EVID-20260825-beff-trigger-summary` | `b5486222` | `playdh-current` | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R EffectCatalog` | BEFF summaries now retain exact unit indices/types and trigger records (`time_token`, unit, operation) from the original script, including `f0` frame tokens without guessing TickPerFrame conversion. Real `become_h.beff` unit/trigger counts and first trigger assertions passed. This remains parser/timeline-input evidence, not runtime effect execution. |
| `EVID-20260825-effect-timeline` | `7229b18d` | synthetic decoded BEFF summaries | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "EffectTimeline|EffectCatalog"` | Added deterministic trigger scheduler: explicit frame duration, millisecond/frame token conversion, stable ordering, monotonic clock protection, one-shot emission, and invalid-token rejection. Five effect catalog/timeline tests passed. This is E2 timeline infrastructure; renderer/audio trigger execution remains open. |
| `EVID-20260825-effect-runtime` | `3942536a` | `playdh-current`, `become_h.beff` | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R EffectRuntime` | Client EffectRuntime now creates active instances only from decoded catalog scripts, advances deterministic timelines, emits trigger events with source/target object IDs, and removes completed instances. Real `become_h.beff` runtime test passed; missing effects fail without fallback. This is E2/E3 execution infrastructure, not yet particle/mesh/audio rendering evidence. |
| `EVID-20260825-gamein-effect-runtime` | `37f58c17` | GameIn + `playdh-current` | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "CInGameState|EffectRuntime"` | CInGameState now owns the EffectRuntime and exposes its catalog/runtime boundary; GameIn startup loads one authoritative effect catalog instead of a separate orphan index. Four targeted tests passed. This is integration infrastructure; no claim of completed in-game visual effect playback. |
| `EVID-20260825-effect-id-lists` | `18205269` | `playdh-current` `List_M.befl`/`List_W.befl` | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "EffectCatalog|EffectRuntime"` | EffectCatalog now decodes the original gender-specific BEFL lists and resolves numeric effect IDs to real BEFF names (including ID 0 → `shadow.beff`). Five targeted catalog/runtime tests passed. This removes the name-resolution gap; server/client skill event mapping and visual execution still remain. |
| `EVID-20260825-effect-runtime-id` | `2bb5ed8d` | `playdh-current` effect ID lists | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R EffectRuntime` | EffectRuntime now starts timelines by authoritative gender-specific effect ID and rejects unknown IDs without selecting a nearby or fabricated asset. Four runtime tests passed, including real BEFF execution and missing-ID fail-closed behavior. |
| `EVID-20260825-skill-effect-refs` | `ca12bba5` | SkillList parser | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R SkillListParser` | SkillList parsing now preserves the original five effect reference tokens per skill (including the exact BEFF filename instead of collapsing every non-zero value to `1`). Build and 9 parser tests passed; the two real-file tests were skipped because their legacy fixture path is unavailable in this environment. This is E1 parser evidence, not runtime skill playback evidence. |
| `EVID-20260825-skill-manager-effect-names` | `5b12e781` | SkillManager | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "SkillManager|SkillListParser"` | SkillManager now retains and exposes the five original BEFF names by SkillIdx after `init_from_bin`; clearing/reinitializing also clears the reference map. 32 targeted tests passed, with only the two unavailable legacy-path tests skipped. This is E1/E2 data plumbing, not proof of live skill playback. |
| `EVID-20260825-gamein-skill-effect-runtime` | `a97cdf59` | GameIn + current `SkillList.bin`/EffectScript | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "CInGameState|EffectRuntime|SkillManager|SkillListParser"` | GameIn now loads the current SkillList, resolves `effect_use` by SkillIdx on successful quick-slot casts, starts EffectRuntime with the recovered 30 FPS legacy timing (or a host override), advances it each Process tick, and exposes a bounded runtime-event queue. 37 targeted tests passed; unavailable legacy-path tests remain explicitly skipped. This is E2/E3 integration evidence; actual particle/mesh/audio consumers remain open. |
| `EVID-20260825-legacy-tick-timing` | `4055b7c4` | recovered `GAMEDESC_INI` + GameIn | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "EffectTimeline|EffectRuntime|CInGameState"` | Effect timing now accepts fractional milliseconds and uses the recovered legacy default `FramePerSec=30`, `TickPerFrame=1000/30.0f`; frame delays are truncated only at final due-time conversion as in the original code. Eight targeted tests passed. |
| `EVID-20260825-beff-sound-routing` | `75fe3723` | `playdh-current` BEFF `#SOUNDNUM` + SoundList/SfxPlayer | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "EffectCatalog|EffectTimeline|EffectRuntime"` + `python scripts/check-project-governance.py` | BEFF SOUND units retain their exact `#SOUNDNUM` (verified against `become_h.beff`), runtime events carry unit kind and sound ID, and the Windows client routes valid SOUND events to the real SoundList-backed SfxPlayer. Generic guessed skill SFX is suppressed to avoid duplicate/fabricated playback. 9/9 targeted tests and governance passed. This is E2/E3 audio-event integration, not proof of complete audible skill coverage or E5 fidelity. |
| `EVID-20260825-authoritative-skill-effect-start` | `023ae4b7` | GameIn SkillStartAck + EffectRuntime | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "CInGameState|EffectRuntime"` | BEFF presentation now starts only after the server's `SkillStartAck`; client send success alone cannot create a phantom effect. Five targeted tests passed. This is E3 authoritative timing infrastructure; full remote-player visual synchronization and E5 gameplay evidence remain open. |
| `EVID-20260825-beff-visual-dependencies` | `d212caaf` | `playdh-current` decoded BEFF scripts | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "EffectCatalog|EffectTimeline|EffectRuntime"` + `python scripts/check-project-governance.py` | Decoded effect units now retain original `OBJECTNAME`, `SOUNDNAME`, and `TEXTURE` references alongside unit kind and SoundList ID; timeline/runtime events carry these references without inventing replacement assets. Nine targeted tests and governance passed. This is E1/E2 visual dependency plumbing; actual billboard/particle/mesh renderer consumption and E5 visual comparison remain pending. |
| `EVID-20260825-beff-billboard-consumer` | `eec772e7` | GameIn DX11 render path + decoded BEFF texture events | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "EffectCatalog|EffectTimeline|EffectRuntime|CInGameState"` + `python scripts/check-project-governance.py` | GameIn now consumes ON/OFF BEFF texture events and creates only asset-backed world billboards at the authoritative source/target projection; missing textures fail closed and are counted in logs, with no RenderBox/debug substitute. Ten targeted tests and governance passed. This is E3 billboard integration; exact particle/mesh/骨骼 rendering and E5 legacy comparison remain open. |
| `EVID-20260825-beff-object-mesh-consumer` | `ae1b56e8` | decoded BEFF OBJECT units + EntityScene MOD/ANM pipeline | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "EntityScene|EffectCatalog|EffectTimeline|EffectRuntime"` + `python scripts/check-project-governance.py` | BEFF OBJECT units such as `become_h.chx` now enter an explicit EntityScene effect-object cache and render through the original CHX→MOD→ANM mesh path at the source/target world position; they are not converted to debug quads. Fifteen targeted tests and governance passed. This is E3 mesh integration; bone attachment, particle systems, exact effect transforms and E5 legacy comparison remain open. |
| `EVID-20260825-beff-type-audit` | `cbc64df4` | full decoded `playdh-current` BEFF catalog | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "EffectCatalog|EffectTimeline|EffectRuntime"` + `python scripts/check-project-governance.py` | Full catalog audit confirms decoded unit types `ANIMATION`, `DAMAGE`, `LIGHT`, `MOVE`, `OBJECT`, and `SOUND`; counts and dependencies are profile-derived, not guessed. `POSITION`, `RADIUS`, `COLORINDEX`, and `COORDINATE` are preserved for each unit and carried into runtime events. Nine targeted tests and governance passed. LIGHT/MOVE/DAMAGE/ANIMATION renderer consumers and E5 comparison remain pending. |
| `EVID-20260825-beff-light-consumer` | `551d2913` | GameIn BEFF LIGHT events + DX11 `SetRTLight` | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "EffectCatalog|EffectTimeline|EffectRuntime|EntityScene"` + `python scripts/check-project-governance.py` | LIGHT ON/OFF events now drive up to eight position/radius-aware DX11 RT lights at the authoritative source/target object; all unused slots are cleared when leaving GameIn. Unknown nonzero color indices fail closed instead of using a guessed palette. Fifteen targeted tests and governance passed. This is E3 light integration; exact palette, animation, particle and E5 legacy comparison remain open. |
| `EVID-20260825-beff-motion-damage-metadata` | `208a28b5` | decoded BEFF MOVE/ANIMATION/DAMAGE units | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "EffectCatalog|EffectTimeline|EffectRuntime"` + `python scripts/check-project-governance.py` | MOVE `POSITION/DURATION/COORDINATE`, ANIMATION `MOTION`, and DAMAGE `PERCENT/DURATION/DAMAGEKIND` are parsed from the original scripts and carried into timeline/runtime events. Profile assertions cover all six unit kinds and representative non-empty fields. Nine targeted tests and governance passed. Entity action/trajectory/damage-number consumers and E5 comparison remain pending. |
| `EVID-20260825-beff-animation-consumer` | `cba4ee29` | BEFF ANIMATION events + EntityScene/ANM | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "EntityScene|EffectCatalog|EffectTimeline|EffectRuntime"` + `python scripts/check-project-governance.py` | ANIMATION ON events now select the referenced ANM motion slot on the matching OBJECT CHX instance; EntityScene validates the slot against the model's motion table and falls back only to slot 0 when the referenced slot is unavailable. Fifteen targeted tests and governance passed. MOVE trajectory, DAMAGE feedback and E5 animation comparison remain open. |
| `EVID-20260825-beff-move-consumer` | `f68ef330` | BEFF MOVE events + EntityScene effect objects | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "EntityScene|EffectCatalog|EffectTimeline|EffectRuntime"` + `python scripts/check-project-governance.py` | MOVE ON/OFF events now apply their original POSITION vector over DURATION to the matching CHX effect object, interpolated against the same monotonic runtime clock; world coordinates are resolved against the loaded terrain height. Fifteen targeted tests and governance passed. Complex projectile paths, server-authoritative trajectory reconciliation, DAMAGE feedback and E5 comparison remain open. |
| `EVID-20260825-damage-feedback-consumer` | `926117d7` | GameIn confirmed Hit events + UI text renderer | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "CInGameState|EffectCatalog|EffectTimeline|EffectRuntime|EntityScene"` + `python scripts/check-project-governance.py` | Server-confirmed `Hit` events now create bounded damage/heal feedback entries, projected at the target's live world position with deterministic 900 ms rise/fade lifetime; no client-side damage calculation is introduced. Sixteen targeted tests and governance passed. Exact legacy font/color/critical-hit variants and E5 visual comparison remain open. |
| `EVID-20260825-remote-skill-source` | `f89b4392` | SkillStartAck protocol header + EffectRuntime/GameIn | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "InGameEffects|CInGameState|EffectRuntime"` + `python scripts/check-project-governance.py` | Confirmed skill effects now use `SkillStartAck.header.object_id` as the source object, with local-player fallback only when the header is zero. The regression fixture uses a remote source ID and verifies the emitted CastRelease event. Six targeted tests and governance passed. Live dual-client E5 synchronization remains pending. |
| `EVID-20260825-remote-damage-source` | `fadad4a3` | SkillSingleResult protocol header + damage feedback | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "InGameEffects|CInGameState|EffectRuntime"` + `python scripts/check-project-governance.py` | Hit/End events from `SkillSingleResult` now preserve the protocol header source object, matching SkillStartAck; a remote-source fixture verifies the Hit event identity. Six targeted tests and governance passed. Live dual-client E5 damage presentation remains pending. |

| `EVID-20260825-staged-loader-single-entry` | `3c38aa43` | `modern/tools/MoxianClient/main.cpp` | `rg -n "loadGameWorld\\(" modern/tools/MoxianClient/main.cpp` (zero matches) + `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "MoxianClientE2E|MoxianClientE2EDumpCli|GameLoadingCoordinator|CGameLoading|CMapChange"` + `python scripts/check-project-governance.py` | Removed the obsolete synchronous world-loader implementation after staged `GameWorldLoadSession` became the sole runtime entry. Build, 11/11 loading/E2E tests, 24/24 focused client tests, and governance passed. This proves single-entry staged-loading structure, not a claim that the current opaque `playdh-current` Map10 server profile is playable or E5-complete. |
| `EVID-20260825-release-automation-gate-v2` | `b87ef021` | `playdh-current` x86 Release client | `cmd /c scripts\\build-modern.bat Release mxh_client` + `modern\\build\\tools\\MoxianClient\\mxh_client.exe --auto-login --username leak --password secret` + `cmd /c scripts\\build-modern.bat Debug mxh_client` + targeted Loading/E2E CTest + `python scripts/check-project-governance.py` | Release build passed; release client rejected automation/credential arguments before resource/window startup and did not echo the supplied secret. Debug build retained the automation macro and 11/11 Loading/E2E tests plus governance passed. This is a release-safety gate, not human login or E5 evidence. |
| `EVID-20260825-full-ctest-release-gate-v2` | `b87ef021` | local x86 Debug build after Release/Debug configuration switch | `ctest -C Debug --test-dir modern/build --output-on-failure` | 12,176/12,176 registered tests passed in 379.79s. 19 explicit skips remain, including current opaque Map10 AIGroup, MSSQL environment gates, and optional legacy fixtures. This is full regression evidence; it does not upgrade the skipped runtime gate or any E5 visual/human requirement. |
| `EVID-20260825-charselect-failure-recovery` | `18423624` | `playdh-current` character-select UI/runtime | `cmd /c scripts\\build-modern.bat Debug mxh_client mxh_client_tests` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "CCharSelectState|CharSelect"` + `python scripts/check-project-governance.py` | Character-list Nack, Agent disconnect and HSEL failures now expose a typed failure reason; the host returns to the real login dialog, clears the password buffer and shows a visible recoverable message instead of leaving the player stranded in CharSelect. 28/28 CharSelect tests and governance passed. This is E3/E4 recovery evidence, not a claim of successful server startup or E5 human completion. |
| `EVID-20260825-charmake-failure-recovery` | `771377b9` | `playdh-current` character-creation UI/runtime | `cmd /c scripts\\build-modern.bat Debug mxh_client mxh_client_tests` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "CharacterMake|CCharMake"` + `python scripts/check-project-governance.py` | CharacterMakeNack, invalid form data, Agent failure and disconnect now return the player to CharSelect with the preserved LoginResult and visible error context instead of leaving a dead creation state. 37/37 targeted tests and governance passed. This is E3/E4 recovery evidence, not successful server startup or E5 human completion. |
| `EVID-20260825-server-profile-preflight` | `129a56fa` | `playdh-current` `Resource/Server/Monster_10.bin` | `pwsh -NoProfile -File deploy\\scripts\\start_modern.ps1 -Mode start -DryRun -ResourceProfileId playdh-current -MapNumber 10` + same command without `-DryRun` + `python scripts/check-project-governance.py` | Start dry-run now reports `decoder-unavailable`, `bytes=22766`, and `first_u32_le=22766` for the structurally detected size-prefixed server resource. A real start fails before database migration or process launch with an actionable decoder-unavailable message and explicitly refuses reference-profile fallback. Governance passed. This is an E1/E2 fail-closed deployment gate, not playable Map10 or E5 evidence. |
| `EVID-20260825-profile-manifest-validation` | `a56f415f` | `deploy/resource-profiles.json` | `pwsh -NoProfile -File deploy\\scripts\\start_modern.ps1 -Mode start -DryRun -ResourceProfileId playdh-current -MapNumber 10` + `python scripts/check-project-governance.py` | Startup now rejects a profile with missing required entries, unsafe absolute/parent paths, malformed SHA-256 values, or duplicate paths before resolving runtime roots; the canonical current profile passes this validation and retains the explicit opaque-resource preflight. Governance passed. This is an E1 deployment-safety gate only. |
| `EVID-20260825-opaque-container-read` | `3c3767a5` | canonical `playdh-current` `Monster_10.bin` | `cmd /c scripts\\build-modern.bat Debug mxh_compat_tests` + `modern\\build\\tests\\unit\\mxh_compat_tests.exe --gtest_filter=MhFileEx.* --gtest_brief=1` + `python scripts/check-project-governance.py` | Added a read-only raw-container API that validates the size prefix and exposes the 22,762-byte payload without applying an unproven transform. The canonical fixture asserts total size 22,766 and the first payload bytes `DD 3A F2 F2`; all 12 MHFileEx tests and governance passed. The payload is intentionally not sent to AIGroup parsing, so this remains E1 forensic evidence and the Map10 decoder blocker remains explicit. |
| `EVID-20260825-terrain-height-interpolation` | `9fd6bb96` | `playdh-current` HFL terrain runtime | `cmd /c scripts\\build-modern.bat Debug mxh_client mxh_client_tests` + `modern\\build\\tests\\unit\\client\\mxh_client_tests.exe --gtest_brief=1` + `python scripts/check-project-governance.py` | TerrainScene now samples the four surrounding HFL height samples with bilinear interpolation, preserving the legacy world origin and clamping at map edges; player/entity projected heights no longer snap at face boundaries. Client suite 183/183 passed and governance passed. This is E2/E3 rendering evidence, not Map10 E5 evidence while the server resource decoder remains blocked. |
| `EVID-20260825-profile-full-inventory` | `6f4bdb14` | `deploy/resource-profiles.json` + full hash manifests | `pwsh -NoProfile -File deploy\\scripts\\start_modern.ps1 -Mode start -DryRun -ResourceProfileId playdh-current -MapNumber 10` + `python scripts/check-project-governance.py` | Both resource profiles now reference and validate their full machine-readable inventory: `playdh-current` 5,135 files / 1,681,141,995 bytes and `sworking-2008-reference` 469 files / 146,900,343 bytes. The current profile dry-run passes inventory validation and still reports the explicit opaque decoder gate; reference remains release-disabled. This is E0/E1 resource-contract evidence only. |
| `EVID-20260825-entity-model-atomicity` | `fb688ba2` | `playdh-current` EntityScene CHX/MOD/material runtime | `cmd /c scripts\\build-modern.bat Debug mxh_client mxh_client_tests` + `modern\\build\\tests\\unit\\client\\mxh_client_tests.exe --gtest_brief=1` + `python scripts/check-project-governance.py` | Entity model construction now rejects a declared MOD or non-empty material texture when it cannot be read/decoded, instead of publishing a partial model with silently missing parts. The client suite 183/183 passed and governance passed. This is E2/E3 integrity evidence; it does not claim that all current Map10 entities resolve or that placeholder count is zero. |

These are E1–E3 engineering gates only. They do not upgrade G4–G11 to E5 and do not replace a genuine legacy-client human run.

| `EVID-20260825-full-ctest-entity-integrity` | `7526a7d7` | local x86 Debug build after entity atomicity gate | `ctest -C Debug --test-dir modern/build --output-on-failure` | 12,178/12,178 registered tests passed after the entity-model atomicity change. 17 tests remain explicitly skipped for unavailable optional/legacy/MSSQL fixtures and the current opaque Map10 AIGroup resource; this is regression evidence only and does not claim a playable Map10 or E5 completion. |
| `EVID-20260825-npc-map-route-seam` | `f7dc6470` | client NPC interaction / MapChange path | `cmd /c scripts\\build-modern.bat Debug mxh_client mxh_client_tests` + `modern\\build\\tests\\unit\\client\\mxh_client_tests.exe --gtest_brief=1` + `python scripts/check-project-governance.py` | Removed the hard-coded destination map from MapChange-role NPC interaction. The client now requires an injected authoritative route resolver and refuses to send a fabricated destination when route data is absent; client suite 183/183 and governance passed. This is E3 interaction-safety evidence; authoritative MapChange table integration and human map-change E5 remain open. |
| `EVID-20260825-mapchange-resource-parser` | `f4763dcc` | canonical `playdh-current` `Resource/MapChange.bin` | `cmd /c scripts\\build-modern.bat Debug mxh_compat_tests` + `modern\\build\\tests\\unit\\mxh_compat_tests.exe --gtest_filter=MapChangeCatalog.* --gtest_brief=1` + `python scripts/check-project-governance.py` | Added a classic MHFileEx + tabular text parser matching the recovered `MAPCHANGE_INFO` field order. The canonical resource loads 180 entries, including a 10→2 route; 2/2 focused tests and governance passed. This is E1/E2 resource evidence; client/server route injection and human MapChange E5 remain open. |
| `EVID-20260825-mapchange-runtime-binding` | `272e0a47` | `playdh-current` client startup + NPC MapChange interaction | `cmd /c scripts\\build-modern.bat Debug mxh_client mxh_client_tests` + `modern\\build\\tests\\unit\\client\\mxh_client_tests.exe --gtest_brief=1` + `python scripts/check-project-governance.py` | Client startup now loads `Resource/MapChange.bin` and injects a resolver that matches the live NPC name/current map to a catalog route; no destination is invented when there is no match. Client suite 183/183 and governance passed. This remains E3 integration evidence, not human MapChange E5. |
| `EVID-20260825-mapchange-route-lookup` | `39a0fd70` | `playdh-current` MapChange catalog lookup | `cmd /c scripts\\build-modern.bat Debug mxh_compat_tests` + `modern\\build\\tests\\unit\\mxh_compat_tests.exe --gtest_filter=MapChangeCatalog.* --gtest_brief=1` + `python scripts/check-project-governance.py` | Centralized current-map + live-NPC-name route lookup in the catalog and covered positive, wrong-map, and empty-route cases. Focused catalog tests 2/2 and governance passed. This remains E2 route-resolution evidence, not human MapChange E5. |
| `EVID-20260825-machine-path-governance` | `46276029` | client, E2E runner and UI resource scanner | `cmd /c scripts\\build-modern.bat Debug mxh_client mxh_client_e2e mxh_dialog_children_type_scan` + `rg -n "C:/moxiang|C:\\\\\\\\moxiang|modern/scratch/e2e_client" modern/src modern/tools scripts deploy` + `python scripts/check-project-governance.py` | Removed hard-coded `C:\moxiang` and scratch paths. Client UI discovery now uses explicit engine/env/current-tree roots, E2E SQLite uses the OS temporary directory, and the scanner walks from the current tree or accepts an explicit root. Build and governance passed; this is G1 governance evidence, not runtime E5. |
| `EVID-20260825-map10-opaque-decoder` | `39b24778` | `playdh-current` `Resource/Server/Monster_10.bin` | `cmd /c scripts\\build-modern.bat Debug mxh_compat_tests mxh_ai_group_loader_tests` + `modern\\build\\tests\\unit\\mxh_compat_tests.exe --gtest_filter=MhFileEx.* --gtest_brief=1` + `modern\\build\\tests\\unit\\server\\mxh_ai_group_loader_tests.exe --gtest_filter=AiGroupLoader.LoadsPlayDhMonster10Bin --gtest_brief=1` + `pwsh -NoProfile -File deploy\\scripts\\start_modern.ps1 -Mode start -DryRun -ResourceProfileId playdh-current -MapNumber 10` + real start/stop on ports 19601/19701/19801 + `python scripts/check-project-governance.py` | Recovered the current 20-byte opaque-container header and eight-byte repeating XOR body transform. Monster_10 now decodes to valid AIGroup text; loader reports 114 groups and 228 spawns. Focused compatibility tests 13/13, AIGroup test 1/1, three-server start/stop and governance passed. This is E2/E3 runtime evidence; human Map10 combat, all-map coverage and E5 remain open. |
| `EVID-20260825-launcher-display-controls` | `01b0a223` | launcher settings + client Win32 display transition | `cmd /c scripts\\build-modern.bat Debug mxh_client MoxianLauncher` + `modern\\build\\tests\\unit\\client\\mxh_client_tests.exe --gtest_brief=1` + `python scripts/check-project-governance.py` | Launcher now exposes editable post-login width/height and borderless mode, validates 800×600–7680×4320, persists the selected values, and passes them to the client. Client now parses login/post-login dimensions and applies borderless style only during the post-LoginAck transaction, including frame-change and style rollback on failure; logical viewport remains 4:3. Build, 183/183 client tests and governance passed. This is E2/E3 display integration evidence; physical GPU/monitor/DPI and human E5 remain open. |
| `EVID-20260825-settings-corrupt-backup` | `d2bd6b05` | `%LOCALAPPDATA%\\Moxian\\settings.json` recovery | `cmd /c scripts\\build-modern.bat Debug mxh_client_tests` + `modern\\build\\tests\\unit\\client\\mxh_client_tests.exe --gtest_filter=ClientSettings.* --gtest_brief=1` + `python scripts/check-project-governance.py` | Invalid settings objects are moved to a non-destructive `.corrupt[.N]` sibling before safe defaults are returned; the recovery path is covered by the new test, while valid atomic round-trip remains covered. Client suite 184/184 and governance passed. This is E2 settings-integrity evidence. |
| `EVID-20260825-e2e-build-working-directory` | `73b4349c` | `modern/build` CTest working directory | `cmd /c scripts\\build-modern.bat Debug mxh_client_e2e` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "^MoxianClientE2E$"` + `python scripts/check-project-governance.py` | Fixed E2E resource-root discovery to walk from the executable location as well as the current directory. The CTest invocation now passes 1/1 from the build working directory, starts all three servers, uses canonical PlayDH resources, and preserves the Map10 228-monster assertion. This is E3 headless integration evidence, not GUI/GPU or E5 human evidence. |
| `EVID-20260825-map10-client-e2e` | `d689ac80` | `playdh-current`, Map10, modern Login/Agent/Map | `cmd /c scripts\\build-modern.bat Debug mxh_client_e2e` + `modern\\build\\tools\\MoxianClientE2E\\mxh_client_e2e.exe --timeout 15 --map-number 10` + `python scripts/check-project-governance.py` | The headless client runner propagates an explicit map number and canonical PlayDH/server-resource roots to AgentServer and MapServer, then starts `CInGameState` with the selected map. A fresh SQLite run completed LoginAck, CharacterListAck, CharacterMakeSyn, CharacterSelectAck(map=10), GameInAck(map=10), and received the complete 228-monster population plus NPC adds; effect catalog loaded 4,564 assets/3,078 BEFF and SkillList loaded 1,817 entries. This is E3 protocol/resource integration evidence only: no HWND, GPU frame, human input, or E5 visual/combat claim. |
| `EVID-20260825-map10-gui-smoke-gamein` | `625144b6` | Win32 GUI client, `playdh-current`, Map10 | `cmd /c scripts\\build-modern.bat Debug mxh_client` + `pwsh -NoProfile -File scripts\\gui-client-smoke.ps1 -MapNumber 10 -TimeoutSeconds 90` + `python scripts/check-project-governance.py` | Isolated GUI run registered a temporary account without command-line credentials, completed LoginAck, post-login 1024×768 transition, CharacterMake/CharacterSelectAck(map=10), real NewLoadDlg/GameLoading, HFL/STM/sky/entity catalog loading, GameInSyn/GameInAck(map=10), and emitted `GUI_SMOKE_PASS` only after receiving exactly 228 Map10 MonsterAdd messages and NPCAdd messages. State-frame validation passed for connect/login/charselect/charmake/gameloading/gamein; the 1024×768 Map10 terrain frame passed coverage/color validation. Effect catalog loading is asynchronous in normal runtime and explicitly deferred only for this smoke exit path. This is E3 GUI/GPU/network entity integration evidence; no human input, combat, or E5 legacy comparison claim. |
| `EVID-20260825-map10-gui-follow-camera` | `5d8c84fd` | Win32 GUI client entity renderer, `playdh-current`, Map10 | `cmd /c scripts\\build-modern.bat Debug mxh_client` + `pwsh -NoProfile -File scripts\\gui-client-smoke.ps1 -MapNumber 10 -FollowCamera -TimeoutSeconds 90` + `python scripts/check-project-governance.py` | Follow-camera run received 228 MonsterAdd messages, rendered the local `man.chx` player and at least one original `L*.chx` monster model with active ANM motion, and reported `failed=0 placeholders=0 monsters=228 npcs=2 players=1`; player silhouette/skin plus monster body/markings passed in the 1024×768 frame. State frames and terrain/entity checks passed. The validator now scales logical 800×600 crops to the committed post-login display size. This is E3 GUI/GPU/entity presentation evidence; no human input, combat, or E5 legacy comparison claim. |
| `EVID-20260825-release-hud-no-solid-fallback` | `e7698593` | GameIn HUD release render path | `cmd /c scripts\\build-modern.bat Debug mxh_client` + `pwsh -NoProfile -File scripts\\gui-client-smoke.ps1 -MapNumber 10 -TimeoutSeconds 90` + `python scripts/check-project-governance.py` | Solid 1×1 diagnostic bars, quick-slot geometry, inventory/shop/quest panels and NPC marker overlays are now confined to explicit `--debug-ui-bounds`; normal GameIn rendering uses the loaded InterfaceScript tree. Map10 GUI smoke, state frames, terrain validation and governance still passed. This is a release-path integrity gate; it does not yet prove E5 pixel equivalence of every live HUD binding. |
| `EVID-20260825-live-stats-ui-binding` | `7a467d4e` | GameIn live stats → original Character/MP gauge dialogs | `cmd /c scripts\\build-modern.bat Debug mxh_client mxh_client_tests` + `modern\\build\\tests\\unit\\client\\mxh_client_tests.exe --gtest_filter=InGameUiRuntime.*:CInGameState.* --gtest_brief=1` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "^MoxianClientE2E$"` + `pwsh -NoProfile -File scripts\\gui-client-smoke.ps1 -MapNumber 10 -TimeoutSeconds 90` + `python scripts/check-project-governance.py` | Injected a persistent read-only player-stats service backed by the authoritative GameInInfo into loaded `cCharacterDialog` and `cMPGuageDialog` instances; level, HP/MP and experience values refresh after network updates while InterfaceScript rendering remains the release path. Focused UI/state tests, headless E2E, GUI Map10 smoke and governance passed. This is E3/E4 live-state binding evidence, not E5 legacy pixel equivalence. |
| `EVID-20260825-character-info-hud-button` | `2ba23973` | `playdh-current` GameIn HUD → `CI_CHARDLG` | `cmd /c scripts\\build-modern.bat Debug mxh_client_tests` + `modern\\build\\tests\\unit\\client\\mxh_client_tests.exe --gtest_filter=InGameUiRuntime.CharacterInfoHudButtonTogglesLiveDialog --gtest_color=no` | The shipped `CI_BESTTIP` HUD button now routes through the legacy-ID activation path to toggle the real `CI_CHARDLG` character-information dialog, with live GameIn stats rebinding on open and the original close button/Escape path on close. Targeted test passed. This is E3/E4 UI interaction evidence; complete player-visible HUD coverage and E5 legacy comparison remain open. |
| `EVID-20260825-chat-dialog-input` | `86e9f4c7` | `playdh-current` GameIn chat → `CTI_DLG` | `cmd /c scripts\\build-modern.bat Debug mxh_client_tests` + `modern\\build\\tests\\unit\\client\\mxh_client_tests.exe --gtest_filter=InGameUiRuntime.EnterAndEscapeOwnRealChatDialog --gtest_color=no` | Enter/Escape now activate and close the shipped `CTI_DLG` instead of only flipping an internal boolean; the existing chat buffer and send path remain attached to the same state. Targeted test passed. This is E3/E4 input/UI evidence; text rendering, channel controls, server chat acknowledgement and E5 comparison remain open. |
| `EVID-20260825-quick-slot-mouse-input` | `122355e1` | `playdh-current` GameIn quick bar | `cmd /c scripts\\build-modern.bat Debug mxh_client_tests mxh_client_e2e` | Logical 800×600 mouse coordinates over the shipped quick-slot bar are now routed to the same authoritative `use_quick_slot` path as F1–F8, with gap/outside clicks rejected before world attack handling. Build passed; live skill probe and legacy visual comparison remain required. |
| `EVID-20260825-quest-page-buttons` | `b899bfe6` | `playdh-current` `QUE_TOTALDLG` quest pages | `cmd /c scripts\\build-modern.bat Debug mxh_client_tests` + `modern\\build\\tests\\unit\\client\\mxh_client_tests.exe --gtest_filter=InGameUiRuntime.QuestPageButtonsSelectLiveQuestEntry --gtest_color=no` | `QUE_PAGE1BTN`–`QUE_PAGE5BTN` now select the corresponding live main-quest entry and update `m_questId`, sharing the same state used by Up/Down, J accept and K claim. Invalid pages are rejected without changing selection. Targeted test passed. This is E3/E4 quest UI evidence; full server-backed quest dialog action/error/close and E5 comparison remain open. |
| `EVID-20260825-chat-editbox-sync` | `815bc9be` | `playdh-current` `CTI_DLG/MI_CHATEDITBOX` | `cmd /c scripts\\build-modern.bat Debug mxh_client_tests` + `modern\\build\\tests\\unit\\client\\mxh_client_tests.exe --gtest_filter=InGameUiRuntime.FocusedChatEditboxSynchronizesAndEnterSubmits --gtest_color=no` | Fixed the shipped chat EditBox buffer setup and synchronized focused text with the authoritative chat buffer; Enter submits through the state-level path even when the EditBox has focus, and Escape/backspace remain state-safe. Targeted test passed. This is E3/E4 input evidence; IME, channel semantics, server acknowledgement and E5 comparison remain open. |
| `EVID-20260825-inventory-movesyn-drag` | `c01d9d5f` | `playdh-current` `IN_INVENTORYDLG/IN_TABDLG1` | `cmd /c scripts\\build-modern.bat Debug mxh_client_tests` | Inventory grid drag coordinates now resolve against the shipped `IN_TABDLG1` 5×4 grid, retain a source slot, and send the authoritative 24-byte `ITEMBASE + target position` `MoveSyn`; local state waits for existing MoveAck/MoveNack handling rather than applying an unconfirmed swap. Build passed; live connected drag and all inventory tabs remain open. |
| `EVID-20260825-inventory-tabs` | `9dbb4a71` | `playdh-current` `IN_TABBTN1..4` / `IN_TABDLG1..4` | `cmd /c scripts\\build-modern.bat Debug mxh_client_tests` + `modern\\build\\tests\\unit\\client\\mxh_client_tests.exe --gtest_filter=InGameUiRuntime.InventoryTabButtonsSwitchRealGridDialog --gtest_color=no` | Inventory tab activations now select one of four shipped tab grids, reset an in-progress drag, and apply the 0/20/40/60 slot offset used by subsequent MoveSyn requests. Targeted test passed. This is E3/E4 inventory UI evidence; visual child-dialog activation, equipment tabs, live connected drag and E5 comparison remain open. |
| `EVID-20260825-equipment-movesyn` | `d8862822` | `playdh-current` server inventory/equipment containers | `cmd /c scripts\\build-modern.bat Debug mxh_server_handler_tests` + `modern\\build\\tests\\unit\\server\\mxh_server_handler_tests.exe --gtest_filter=MapHandlerTest.MoveSynMovesInventoryItemIntoEquipmentAndPersistsContainer --gtest_color=no` | Server `MoveSyn` now accepts legacy equipment positions 80–89 in addition to inventory positions, swaps across authoritative inventory/equipment arrays, mirrors `WearedItem`, and persists container=1. Targeted test passed. Client equipment-slot hit testing, equip stat/appearance refresh, live E4 drag and E5 comparison remain open. |
| `EVID-20260825-equipment-slot-hit-testing` | `4deeae97` | `playdh-current` `IN_WEAREDDLG` icon cells | `cmd /c scripts\\build-modern.bat Debug mxh_client_tests` | Client drag routing now resolves all ten shipped `IN_WEAREDDLG` `#ICONCELL` rectangles and permits inventory↔equipment and equipment↔equipment MoveSyn targets 80–89; unconfirmed local equipment/appearance state is not mutated. Build passed; connected GUI drag, equip validation rules, stat recompute and E5 visual comparison remain open. |
| `EVID-20260825-equipment-stat-recompute` | `cf72a8bb` | `playdh-current` authoritative MapServer player state | `cmd /c scripts\\build-modern.bat Debug mxh_server_handler_tests` + `modern\\build\\tests\\unit\\server\\mxh_server_handler_tests.exe --gtest_filter=MapHandlerTest.MoveSynMovesInventoryItemIntoEquipmentAndPersistsContainer --gtest_color=no` | Successful cross-container MoveSyn now invokes the authoritative player stat recomputation before mirroring inventory/equipment and persistence. The equipment MoveSyn integration test still passes. Client live stat notification and visual model refresh remain open. |
| `EVID-20260825-equipment-totalinfo-sync` | `8fdd3e17` | `playdh-current` server → client `TotalInfoLocal` | `cmd /c scripts\\build-modern.bat Debug mxh_server_handler_tests` + `modern\\build\\tests\\unit\\server\\mxh_server_handler_tests.exe --gtest_filter=MapHandlerTest.MoveSynMovesInventoryItemIntoEquipmentAndPersistsContainer --gtest_color=no` | After a successful equipment MoveSyn, MapServer now sends the authoritative full `ItemTotalInfo` as `TotalInfoLocal` after MoveAck; the client’s existing handler refreshes inventory and `WearedItem` without speculative local mutation. Targeted test verifies ordering payload size and persisted equipment container. Live client model/stat notification remains open. |
| `EVID-20260825-client-equipment-appearance-sync` | `02441381` | `playdh-current` client `TotalInfoLocal` → EntityScene | `cmd /c scripts\\build-modern.bat Debug mxh_client_tests` + `modern\\build\\tests\\unit\\client\\mxh_client_tests.exe --gtest_filter=InGameUiRuntime.TotalInfoLocalRefreshesEquipmentAppearance --gtest_color=no` | Client `TotalInfoLocal` handling now copies all ten authoritative `WearedItem` icon IDs into `GameInInfo.weared_item_idx`, refreshes live UI bindings, and feeds the existing per-frame EntityScene snapshot so equipped appearance can be rebuilt from server-confirmed state. Targeted test passed. Equipment legality, stat packet notification and human visual comparison remain open. |
| `EVID-20260825-full-ctest-live-ui` | `a170428a` | complete local x86 Debug regression after live UI binding | `ctest -C Debug --test-dir modern/build --output-on-failure` | 12,182/12,182 registered tests passed in 352.41s. 19 explicit skips remain limited to optional legacy fixtures, MSSQL environment gates, and unavailable historical resource samples; Map10 E2E and RenderDemo headless acceptance passed. This is full regression evidence and does not upgrade the remaining human/E5/clean-machine gates. |
| `EVID-20260825-map10-audio-manifest` | `9a103da1` | Win32 BGM/SFX startup path, `playdh-current`, Map10 | `cmd /c scripts\\build-modern.bat Debug mxh_client` + `pwsh -NoProfile -File scripts\\gui-client-smoke.ps1 -MapNumber 10 -TimeoutSeconds 90` + `python scripts/check-project-governance.py` | GUI smoke now requires the SFX manifest readiness marker and observed `entries=1674 wav=740` with data-driven UI/attack/skill/pickup IDs, alongside original BGM 1667/1670 playback and the full Map10 entity path. This is E3 audio-resource/runtime initialization evidence; individual skill-triggered sound timing and E5 legacy audio comparison remain open. |
| `EVID-20260825-release-client-gate-after-ui-audio` | `bfd0ef28` | x86 Release client automation/credential gate | `cmd /c scripts\\build-modern.bat Release mxh_client` + `modern\\build\\tools\\MoxianClient\\mxh_client.exe --auto-login --username leak --password secret` + `cmd /c scripts\\build-modern.bat Debug mxh_client` + `python scripts/check-project-governance.py` | Release build completed successfully and rejected automation/credential command-line options before startup without echoing the supplied secret; Debug build was restored afterward. This is release safety evidence, not human launcher/login evidence. |
| `EVID-20260825-map10-live-combat-probe` | `126680f8` | Debug-only three-server Map10 combat/loot probe | `cmd /c scripts\\build-modern.bat Debug mxh_map_server_CHINA mxh_client_e2e` + `modern\\build\\tools\\MoxianClientE2E\\mxh_client_e2e.exe --exercise-combat --timeout 4` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "DropItemLoaderTest|^MoxianClientE2E$"` + `python scripts/check-project-governance.py` | The opt-in probe now drives click-to-move prediction, Skill.StartSyn, server-authoritative SkillSingleResult/LifeNotify, real MonsterObtainNotify, ground-drop rendering state, PickupSyn/PickupAck, and live inventory insertion. A fresh three-server Map10 run observed item `25050`, drop object `90000`, and `inventory=updated`; the strict gate passed with `hit/effect/life/drop/pickup`. This is E3/E4 runtime evidence, not human or E5 legacy comparison evidence. |
| `EVID-20260825-gameout-persistence` | `3fb967ed` | GameOut session-boundary persistence | `cmd /c scripts\\build-modern.bat Debug mxh_agent_server_CHINA mxh_client_e2e` + `modern\\build\\tools\\MoxianClientE2E\\mxh_client_e2e.exe --exercise-combat --timeout 2` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "GameOutSynPersistsLiveMoneyBeforeRuntimeRemoval"` | Client `CInGameState::Release()` sends a real `GameOutSyn`; Agent forwards explicit GameOut while the character route is live; MapHandler flushes inventory, money, and quest state before removing the live runtime. The SQLite integration test passed, and a fresh three-server re-login observed the picked item `25050` after GameIn. This is E3/E4 runtime evidence, not human or E5 legacy comparison evidence. |
| `EVID-20260825-map10-skills` | `523ee569` | Map10 quick-slot skill/effect probe | `cmd /c scripts\\build-modern.bat Debug mxh_client_e2e` + `modern\\build\\tools\\MoxianClientE2E\\mxh_client_e2e.exe --exercise-skills --timeout 2` | The opt-in three-server probe moves into authoritative range, invokes four live quick-slot entries, observes `SkillSingleResult` damage, starts the matching EffectRuntime timeline from the authoritative result, and records the spatial skill audio cue. Latest run passed with `casts=4`, `life_changes=3`, `runtime_effect_events=15`, and `audio_skill_cues=4`. This is E3/E4 runtime evidence; full BEFF visual/audio legacy comparison remains pending. |
| `EVID-20260825-mapchange-live` | `1a96c5ce` | Three-server Map10→Map12 change-map probe | `cmd /c scripts\\build-modern.bat Debug mxh_client_e2e` + `modern\\build\\tools\\MoxianClientE2E\\mxh_client_e2e.exe --exercise-mapchange --timeout 3` | The opt-in probe starts a second MapServer on port 18002, registers an explicit Agent route for map 12, sends real `ChangeMapSyn`, observes current-map `GameOutSyn`, target-map `GameInSyn`, and receives a fresh `GameInAck map=12` on the same client session. Agent updates the active map route before acknowledging, final GameOut cleanup targets Map12, and the GUI client now rebinds the committed terrain bounds/static collision to the new GameIn state after staged loading. The probe then performs GameOut and a fresh GameIn, again reaching `map=12`, proving route persistence. This is E3/E4 transport/runtime evidence; full renderer scene swap, loading UI capture, and human E5 travel remain pending. |
| `EVID-20260825-governance-cleanup` | `132fcaea` | Repository cleanup audit | `python scripts/check-project-governance.py` + `git status --short` + `git ls-files -ci --exclude-standard` | Historical documents are no longer tracked under the current docs entry chain; generated root `Log` and `dist` artifacts were removed, tracked-but-ignored count is `0`, governance check passes, and the working tree is clean. Reference payloads and dated scratch evidence remain intentionally ignored/read-only rather than being deleted. |
| `EVID-20260825-drop-table-profile-mismatch` | `126680f8` | `playdh-current` drop profile routing | `cmd /c scripts\\build-modern.bat Debug mxh_drop_item_tests` + `modern\\build\\tests\\unit\\server\\mxh_drop_item_tests.exe --gtest_filter=DropItemLoaderTest.* --gtest_brief=1` + `python scripts/check-project-governance.py` | The legacy-compatible 20-entry loader now parses the verified top-level `Resource/MonsterDropItemList.bin` (1,230 tables); MapServer routes current profile to that file and keeps the three `$Group` server-directory copies fail-closed. Map10 monster kinds 102/104/105/219 have real tables, and no fake item is injected. |
| `EVID-20260825-equipment-legality` | `WORKTREE` | `playdh-current` ItemList metadata + authoritative MoveSyn | `cmd /c scripts\\build-modern.bat Debug mxh_server_handler_tests` + `modern\\build\\tests\\unit\\server\\mxh_server_handler_tests.exe --gtest_filter=MapHandlerTest.MoveSyn* --gtest_color=no` | MapServer now checks loaded ItemList metadata before accepting a MoveSyn into equipment positions 80–89: known non-equipment ItemKind values are rejected with MoveNack, while missing metadata remains fail-open for synthetic/incomplete profiles. Three focused MoveSyn tests passed, including persistence for legal equipment and rejection for a known potion. This is E3/E4 server-integrity evidence; per-slot equip legality, live stat presentation and human visual E5 remain open. |
| `EVID-20260825-equipment-live-stat-refresh` | `WORKTREE` | existing `CharacterAdd` wire path + client HUD/model state | `cmd /c scripts\\build-modern.bat Debug mxh_server_handler_tests mxh_client_tests` + `modern\\build\\tests\\unit\\server\\mxh_server_handler_tests.exe --gtest_filter=MapHandlerTest.MoveSyn* --gtest_color=no` + `modern\\build\\tests\\unit\\client\\mxh_client_tests.exe --gtest_filter=InGameCharacterAdd.SelfRefreshUpdatesLiveVitalsAndEquipment --gtest_color=no` | After an accepted equipment MoveSyn, MapServer copies recomputed authoritative vitals into PlayerInfo and broadcasts the existing CharacterAdd payload before TotalInfoLocal. The client now applies a self CharacterAdd to live HP/max-HP, gender/face/hair and equipment appearance instead of discarding it as an own echo. Focused server/client builds and tests passed. This is E3/E4 synchronization evidence; full stat categories and human E5 visual comparison remain open. |
| `EVID-20260825-equipment-slot-legality` | `WORKTREE` | legacy `ItemKind`/`EquipKind` and ten wear slots | `cmd /c scripts\\build-modern.bat Debug mxh_server_handler_tests` + `modern\\build\\tests\\unit\\server\\mxh_server_handler_tests.exe --gtest_filter=MapHandlerTest.MoveSyn* --gtest_color=no` | MoveSyn now mirrors the legacy acceptance rules: normal equipment bit, quest/shop equipment exceptions, exact EquipKind-to-slot mapping, and Ring1 allowed in either ring cell. Focused tests cover legal metadata-backed equipment, known non-equipment rejection, and valid equipment in the wrong-slot rejection. Unknown metadata remains fail-open only for incomplete profiles. This is E3/E4 integrity evidence; full level/class CanEquip rules and human E5 remain open. |
| `EVID-20260825-equipment-player-limits` | `WORKTREE` | legacy `CanEquip` level/attribute limits | `cmd /c scripts\\build-modern.bat Debug mxh_server_handler_tests` + `modern\\build\\tests\\unit\\server\\mxh_server_handler_tests.exe --gtest_filter=MapHandlerTest.MoveSyn* --gtest_color=no` | Known equipment now also checks `LimitLevel`, `LimitGenGol`, `LimitMinChub`, `LimitSimMek` and `LimitCheRyuk` against the authoritative PlayerState before accepting the MoveSyn. A level-5 item is rejected for the default level-1 player; the focused MoveSyn suite passes. Shop-level-free and stage/job gates remain separate follow-up work. |
| `EVID-20260825-equipment-move-feedback` | `WORKTREE` | client Item MoveNack → shipped modal message box | `cmd /c scripts\\build-modern.bat Debug mxh_client_tests` + `modern\\build\\tests\\unit\\client\\mxh_client_tests.exe --gtest_filter=InGamePlayable.MoveNackShowsVisibleItemMoveMessage --gtest_color=no` | A server rejection is now surfaced through the existing modal UI path instead of being silently ignored. The client test confirms `MoveNack` creates a visible message modal; no new protocol or placeholder HUD is introduced. |
| `EVID-20260825-equipment-swap-legality` | `WORKTREE` | authoritative two-way equipment swap validation | `cmd /c scripts\\build-modern.bat Debug mxh_server_handler_tests` + `modern\\build\\tests\\unit\\server\\mxh_server_handler_tests.exe --gtest_filter=MapHandlerTest.MoveSyn* --gtest_color=no` | Cross-equipment swaps now validate both directions: the dragged item must fit the target slot and the displaced item must fit the source slot. A hat and weapon can each enter their proper slots, but a subsequent hat→weapon-slot swap is rejected. Focused MoveSyn suite passes. |
| `EVID-20260825-full-ctest-stat-refresh` | `WORKTREE` | x86 Debug full regression after equipment stat refresh | `ctest -C Debug --test-dir modern/build --output-on-failure` | Full CTest reached 12,195 registered tests; the only failure was a stale assertion for the superseded ten-entry drop cap. That historical test is now corrected to the current legacy-compatible 20-entry loader contract; the rerun is the required clean regression gate. Explicit historical/MSSQL/resource skips remain unchanged and documented. |
| `EVID-20260825-party-guild-persistence-schema` | `667600ae` | SQLite/MSSQL membership persistence schema and MapHandler mutation hooks | `cmd /c scripts\build-modern.bat Debug` + `modern\build\tests\unit\server\mxh_server_handler_tests.exe --gtest_filter=MapHandlerTest.PartyCreateAndBreakupMutateAuthoritativeMapState --gtest_brief=1` + `ctest -C Debug --test-dir modern/build --output-on-failure` + `python scripts/check-project-governance.py` | Added durable party/guild and member tables to both schema backends, mirrored them in MoxianDbTool, and made MapHandler persist create/add/accept/breakup mutations and restore complete member snapshots on GameIn. The authoritative regression passed; full CTest passed 12,202/12,202 and governance passed. This is E3 persistence plumbing evidence; multi-process live routing and human relog evidence remain open. |
| `EVID-20260825-gamein-social-snapshot` | `d3938524` | GameIn restored Party/Guild state → client Info snapshot | `cmd /c scripts\build-modern.bat Debug` + `modern\build\tests\unit\server\mxh_server_handler_tests.exe --gtest_filter=MapHandlerTest.PartyCreateAndBreakupMutateAuthoritativeMapState --gtest_brief=1` + `ctest -C Debug --test-dir modern/build --output-on-failure` | GameIn now emits explicit Party/Guild Info snapshots after the authoritative GameInAck whenever persisted membership is restored, so client state is populated immediately after relog. Targeted regression passed and the post-change full CTest completed 12,202/12,202 with only documented environment/resource skips. This is E3/E4 protocol/runtime evidence; human relog and E5 visual proof remain open. |
| `EVID-20260825-player-state-schema-upgrade` | `bc66db2b` | Existing SQLite player-state database upgrade | `cmd /c scripts\\build-modern.bat Debug mxh_db_tests` + `modern\\build\\tests\\unit\\db\\mxh_db_tests.exe --gtest_filter=SchemaMigration.* --gtest_brief=1` + `ctest -C Debug --test-dir modern/build --output-on-failure` + `python scripts/check-project-governance.py` | SQLite migration now inspects `modern_player_state` and adds missing `party_id`/`guild_id` columns for legacy databases; a regression test creates the old four-field table and verifies the upgraded seven-field schema. Schema tests (4/4), governance and full CTest (12,203/12,203) passed; documented environment/resource skips remain. |
| `EVID-20260825-cross-map-social-relay` | `8ebbb14f` | MapHandler → Agent routing for off-map Party/Guild members | `cmd /c scripts\\build-modern.bat Debug` + `modern\\build\\tests\\unit\\server\\mxh_server_handler_tests.exe --gtest_filter=MapHandlerTest.PartyCreateAndBreakupMutateAuthoritativeMapState --gtest_brief=1` + `ctest -C Debug --test-dir modern/build --output-on-failure` + `python scripts/check-project-governance.py` | Party/Guild snapshots now fall back to the current MapServer→Agent link when a member is not connected to the local MapHandler, preserving the target member ID so Agent can route to a different map session. Targeted regression, governance and full CTest (12,203/12,203) passed. This is E3/E4 cross-route evidence; dual-client different-map human verification remains open. |
| `EVID-20260825-agent-offmap-social-route-test` | `7b842323` | Agent target routing for Party/Guild Info from another map | `cmd /c scripts\\build-modern.bat Debug mxh_server_handler_tests` + `modern\\build\\tests\\unit\\server\\mxh_server_handler_tests.exe --gtest_filter=AgentHandlerTest.ForwardFromMapRoutesPartyAndGuildToOffMapSession --gtest_brief=1` + `ctest -C Debug --test-dir modern/build --output-on-failure` | Added an AgentHandler regression with two sessions on maps 10 and 12. MapServer-originated Party and Guild Info messages addressed to char_id 456 are both routed to connection 101, preserving category, object ID and payload. Targeted test and full CTest (12,204/12,204) passed; human dual-client evidence remains open. |
| `EVID-20260825-social-route-disconnect-cleanup` | `d979611b` | Agent char route removal after disconnect | `cmd /c scripts\\build-modern.bat Debug mxh_server_handler_tests` + `modern\\build\\tests\\unit\\server\\mxh_server_handler_tests.exe --gtest_filter=AgentHandlerTest.ForwardFromMapRoutesPartyAndGuildToOffMapSession:AgentHandlerTest.DisconnectRemovesOffMapSocialRoute --gtest_brief=1` + `ctest -C Debug --test-dir modern/build --output-on-failure` + `python scripts/check-project-governance.py` | Added a regression proving disconnect forwards GameOut once, removes the target char route, and prevents later Party Info delivery to the stale connection. Targeted tests and full CTest (12,205/12,205) passed; documented environment/resource skips remain. |
| `EVID-20260825-social-member-map-preservation` | `dda179a4` | Party/Guild member map_num preservation across MapServer mutations | `cmd /c scripts\\build-modern.bat Debug mxh_server_handler_tests` + `modern\\build\\tests\\unit\\server\\mxh_server_handler_tests.exe --gtest_filter=MapHandlerTest.PartyCreateAndBreakupMutateAuthoritativeMapState --gtest_brief=1` + `ctest -C Debug --test-dir modern/build --output-on-failure` + `python scripts/check-project-governance.py` | Persistence now reads prior member locations before rewriting a party/guild, updates only members connected to the current map, and preserves off-map values; guild member GameIn restores `connected_map_num`. Targeted regression, governance and full CTest (12,205/12,205) passed. |
| `EVID-20260825-human-runner-safety-governance` | `4965add4` | Governance enforcement for tracked human acceptance runner | `python -m py_compile scripts/check-project-governance.py` + `python scripts/check-project-governance.py` + `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/run-human-acceptance.ps1 -?` | Governance now fails if the human runner contains auto-login/auto-create, command-line credential flags, synthetic input APIs, taskkill, or process-name-based termination; it also requires exact owned-process identity checks, unique run IDs and evidence output. Syntax, runner help invocation and governance passed. This is a safety gate, not human E5 gameplay evidence. |
| `EVID-20260825-human-evidence-minimum-gate` | `49eb75b7` | Human runner settled-frame completeness | `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/run-human-acceptance.ps1 -?` + `python scripts/check-project-governance.py` | The tracked human runner now requires at least eight operator-captured F12 TGA frames by default before a successful completion, while still supplying no credentials or synthetic input. The run metadata continues to record the actual frame list; operators may explicitly choose a different reviewed threshold. This is evidence completeness/safety gating, not proof of gameplay or E5 equivalence. |
| `EVID-20260825-startup-resource-integrity` | `ab05edbd` | `deploy/scripts/start_modern.ps1` complete resource profile preflight | `powershell -NoProfile -ExecutionPolicy Bypass -File deploy/scripts/start_modern.ps1 -Mode start -DryRun -SkipResourceIntegrity -AllowDevFallbacks` + `python scripts/check-project-governance.py` | Startup now validates the complete profile inventory (registered path set, file count, byte count, per-file size and SHA-256) before launching servers. The only bypass is an explicit development-only combination of `-SkipResourceIntegrity -AllowDevFallbacks`; normal startup cannot silently accept a partial or mutated resource tree. Dry-run and governance passed; this is E1/E2 resource-integrity evidence, not visual/runtime E5 evidence. |
| `EVID-20260825-startup-resource-integrity-baseline` | `daee70be` | `playdh-current` cleaned canonical baseline | `powershell -NoProfile -ExecutionPolicy Bypass -File deploy/scripts/start_modern.ps1 -Mode start -DryRun` + `python scripts/check-project-governance.py` | Removed three previously cleaned `Thumbs.db` derivatives from the profile manifest and synchronized the inventory to 5,132 files / 1,681,047,275 bytes. The production dry-run then completed a full per-file SHA-256 verification and passed, with Map10 server preflight reporting `decoder-ready`. This is E1/E2 integrity evidence; no human gameplay or E5 visual claim. |
| `EVID-20260826-clean-deploy-profile-staging` | `9ec8ecbf` | `deploy/scripts/deploy_client.ps1` clean-machine package layout | `powershell -NoProfile -Command "[scriptblock]::Create((Get-Content -Raw -LiteralPath 'deploy/scripts/deploy_client.ps1')) | Out-Null; Write-Output 'parse-ok'"` + `python scripts/check-project-governance.py` | Deployment now verifies the complete profile manifest before copying and stages the entire verified tree under `data/PlayDH`, which is the launcher/client discovery location. Legacy top-level copies remain for compatibility. Script parsing and governance passed; actual full package installation and human clean-machine run remain required. |
| `EVID-20260826-clean-deploy-regression` | `WORKTREE` | post-deploy-script Debug regression | `cmd /c scripts\\build-modern.bat Debug` + `ctest -C Debug --test-dir modern/build --output-on-failure` + `python scripts/check-project-governance.py` | Debug build completed 272/272 targets; full CTest completed 12,205/12,205 in 352.09s. The documented 19 environment/resource/MSSQL skips remain; no test failure was introduced by deployment profile staging. This is regression evidence only; package installation and clean-machine E5 remain open. |
| `EVID-20260826-client-experience-curve` | `8c5ef3f9` | GameIn HUD experience threshold from `CharacterExpPoint.bin` | `cmd /c scripts\\build-modern.bat Debug` + `modern\\build\\tests\\unit\\client\\mxh_client_tests.exe --gtest_filter='*GameIn*:*Experience*' --gtest_color=no` + `ctest -C Debug --test-dir modern/build --output-on-failure` + `python scripts/check-project-governance.py` | GameIn now loads the 121-level canonical experience curve before binding the live stats service; missing or malformed curve fails GameIn visibly instead of falling back to `100×level`. Targeted client tests (8/8), Debug build, governance, and full CTest (12,205/12,205 in 363.67s) passed. This is E3 live-data binding evidence; visual HUD/E5 legacy comparison remains open. |
| `EVID-20260826-client-hero-attributes` | `9e52dca3` | GameInAck legacy HeroTotal attributes → character panel | `cmd /c scripts\\build-modern.bat Debug` + `ctest -C Debug --test-dir modern/build --output-on-failure -R \"CCharacterDialog\\.(SetDefenseRateCriticalAttackRangeResetToZero|RefreshFromPlayerStatsWritesMappedLiveFields)|InGameGameInAck\\.DecodesCurrentLegacyLayout\"` + `python scripts/check-project-governance.py` | Parsed the four legacy `HeroTotalInfo` core attributes (`wGenGol`, `wMinChub`, `wCheRyuk`, `wSimMek`) from the real `GameInAck` layout and bound them to the character panel through the live stats service. Targeted regression 3/3 passed and governance passed; the prior full CTest baseline remains valid, while visual/HUD placement and human E5 comparison remain open. |
| `EVID-20260826-login-display-settings` | `33f3c4f2` | LoginAck → persisted display/profile settings → CharSelect | `cmd /c scripts\\build-modern.bat Debug mxh_client mxh_client_tests` + `modern\\build\\tests\\unit\\client\\mxh_client_tests.exe --gtest_filter=ClientSettings.* --gtest_color=no` + `python scripts/check-project-governance.py` | Normal client startup now carries the persisted resource profile and borderless choice into the LoginAck display transition, while retaining the saved post-login dimensions; the transition already performs DPI-aware client sizing, viewport update and rollback on failure. Settings round-trip/corrupt-file tests passed (2/2), build and governance passed. Actual GUI LoginAck transition, swap-chain failure injection and human 800×600→1024×768 evidence remain open. |
| `EVID-20260826-login-display-override-precedence` | `4d1749bd` | Explicit launcher/client display arguments vs persisted settings | `cmd /c scripts\\build-modern.bat Debug mxh_client` + `python scripts/check-project-governance.py` + `git diff --check` | Added explicit override tracking for resource profile, post-login width/height and borderless mode. Persisted settings are now used only when the corresponding option was not supplied, so values such as an explicit 1024px width are no longer mistaken for defaults. Debug client build, governance and whitespace checks passed; GUI argument precedence and human display transition evidence remain open. |
| `EVID-20260826-map-load-bgm-gate` | `f5644000` | GameWorldLoadSession Map10 BGM stage | `cmd /c scripts\\build-modern.bat Debug mxh_client` + `python scripts/check-project-governance.py` + `git diff --check` | Release map loading now fails and rolls back when the descriptor’s BGM cannot be played; only explicit debug-bounds runs may continue for diagnosis, with a warning. This removes a silent audio fallback from the formal path. Build, governance and whitespace checks passed; real Map10 audio playback and human E5 audio comparison remain open. |
| `EVID-20260826-sfx-semantic-map-gate` | `3a76c05a` | Startup semantic SFX mapping for UI/attack/skill/pickup | `cmd /c scripts\\build-modern.bat Debug mxh_client` + `python scripts/check-project-governance.py` + `git diff --check` | Removed the arbitrary “first available WAV” fallback. Release startup now fails if any required semantic cue is not mapped from the SoundList-derived manifest; debug mode preserves the missing IDs for diagnosis. Build, governance and whitespace checks passed; live cue timing and legacy audio E5 comparison remain open. |
| `EVID-20260826-mapchange-resource-gate` | `a250bfd9` | Startup MapChange.bin prerequisite | `cmd /c scripts\\build-modern.bat Debug mxh_client` + `Test-Path -LiteralPath modern/data/PlayDH/Resource/MapChange.bin` + `python scripts/check-project-governance.py` + `git diff --check` | Formal startup now fails when `MapChange.bin` is missing or invalid instead of entering a world where NPC warp interactions are silently disabled. Explicit debug mode remains available for diagnosis. The canonical profile file exists; build, governance and whitespace checks passed. Human portal/map-change E5 evidence remains open. |
| `EVID-20260826-mapchange-failure-rollback` | `3db78973` | GameIn → MapChange failed-load rollback | `cmd /c scripts\\build-modern.bat Debug mxh_client` + `python scripts/check-project-governance.py` + `git diff --check` | Remembered whether a MapChange request originated in GameIn before clearing the input target. If staged world loading fails and the old terrain/static/entity scene still exists, the client now restores the previous GameIn state instead of incorrectly falling back to CharSelect. Build, governance and whitespace checks passed; injected runtime failure and human 50× map-change soak remain open. |
| `EVID-20260826-full-regression-mapchange-gates` | `WORKTREE` | Full local Debug regression after startup/audio/MapChange gates | `cmd /c scripts\\build-modern.bat Debug` + `ctest -C Debug --test-dir modern/build --output-on-failure` + `python scripts/check-project-governance.py` + targeted `CCharacterDialog` regression | Full Debug build reported no work needed; the 12,205-test CTest run completed without a test failure after the display/audio/MapChange changes, with the documented environment/resource/MSSQL skips only. The two previously stale character-dialog failures were rerun and passed 2/2. Governance passed. This is regression evidence only; injected MapChange failure, 50× soak and human E5 remain open. |
| `EVID-20260826-mapchange-rollback-eligibility-test` | `0a80a3dd` | Pure rollback eligibility predicate for failed MapChange | `cmd /c scripts\\build-modern.bat Debug mxh_client mxh_client_tests` + `modern\\build\\tests\\unit\\client\\mxh_client_tests.exe --gtest_filter=GameLoadingCoordinator.* --gtest_color=no` + `python scripts/check-project-governance.py` | Extracted the old-scene restoration predicate from the monolithic client loop and covered all missing-component combinations. Coordinator regression passed 4/4, client build and governance passed. This proves the decision logic, not a substitute for injected GPU/resource failure or 50× human MapChange soak. |
| `EVID-20260826-server-changemap-route-regression` | `WORKTREE` | Agent ChangeMapAck/Nack and target MapServer bootstrap | `cmd /c scripts\\build-modern.bat Debug mxh_server_handler_tests` + `modern\\build\\tests\\unit\\server\\mxh_server_handler_tests.exe --gtest_filter=\"AgentHandlerTest.ChangeMap*\" --gtest_color=no` + `python scripts/check-project-governance.py` | Server-side ChangeMap route closure, missing-target Nack/current-map preservation, and target MapServer GameInAck relay all passed 3/3. This confirms protocol/routing support for the client rollback path; it is not human map-change or 50× soak evidence. |
| `EVID-20260826-mapchange-coordinator-50-request-soak` | `0ea064b0` | GameLoadingCoordinator repeated MapChange request state | `cmd /c scripts\\build-modern.bat Debug mxh_client_tests` + `modern\\build\\tests\\unit\\client\\mxh_client_tests.exe --gtest_filter=GameLoadingCoordinator.* --gtest_color=no` + `python scripts/check-project-governance.py` | Added a 50-iteration request loop alternating Map10/Map12, asserting every new request resets progress and clears failed/cancelled state. Coordinator suite passed 5/5, build and governance passed. This is coordinator-state soak evidence; it does not replace GPU/resource or human 50× map-change evidence. |
| `EVID-20260826-map10-gui-smoke` | `97d02ab2` | Isolated Map10 client/server GUI smoke with real resources | `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\\gui-client-smoke.ps1 -MapNumber 10 -TimeoutSeconds 90 -FollowCamera` + `python scripts/check-project-governance.py` | Fixed byte-stable ASCII stdin for isolated account bootstrap, then completed Login → CharacterSelect/Make → Loading → GameIn with original BGM, 228 Map10 monsters, 2 NPCs, state frames and entity silhouette validation. `GUI_CLIENT_SMOKE PASS`; evidence run `1fff87cc746e4adf96b027762ea3beb8`. This is automated E3 runtime evidence, not human input or legacy E5 comparison. |
| `EVID-20260826-map12-empty-monster-profile` | `f4723aac` | Canonical Map12 14-byte empty Monster table | `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\\gui-client-smoke.ps1 -MapNumber 12 -TimeoutSeconds 90` + startup script parse/governance checks | Startup now explicitly accepts the documented Map12 14-byte empty monster table (`first_u32_le=20040309`) without attempting positional decode. The subsequent GUI run correctly reached Loading but failed closed on 9 unresolved HFL textures, proving the map is not falsely reported as playable. This is honest blocker evidence; Map12 remains below E3/E5 until those canonical texture dependencies are resolved. |
| `EVID-20260826-map12-gui-smoke-untextured-materials` | `e8268555` | Map12 HFL/STM valid untextured material handling | `cmd /c scripts\\build-modern.bat Debug mxh_client` + `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\\gui-client-smoke.ps1 -MapNumber 12 -TimeoutSeconds 90` + `python scripts/check-project-governance.py` | Corrected render accounting for HFL palette name `1` and empty STM texture names as intentional untextured materials. Map12 then completed Login → CharacterMake/Select → Loading → GameIn with 0 monsters, 1 NPC, original BGM, terrain frame and all state-frame checks: `GUI_CLIENT_SMOKE PASS`, run `c6ded02976c14e338aec6ba398cc14f2`. This is automated E3 map runtime evidence; visual legacy E5 and interaction soak remain open. |

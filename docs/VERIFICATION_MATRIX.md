# Verification matrix

This is the single current evidence index. A row is complete only when it has a commit, resource-profile manifest, command, artifact ID and the required evidence level.

## Evidence levels

| Level | Meaning |
|---|---|
| E0 | File/package bytes and SHA-256 recorded |
| E1 | Format decoded with complete field coverage |
| E2 | Runtime object instantiated with no missing dependency |
| E3 | Runtime renders or plays the asset |
| E4 | Correct player action triggers the asset and state change |
| E5 | Genuine legacy comparison plus human review passes |

## Current gates

| Gate | Scope | Status | Required evidence |
|---|---|---|---|
| G0 | Git/source/resource protection | PASS (42d14756) | governance bundle + backup refs + dual-copy hash report |
| G1 | Profiles, paths, secrets | PASS (42d14756) | explicit profiles, path scan, secret fallback removed |
| G2 | Clean tree and current docs | PASS (42d14756, f8ffa596) | clean status, successful Debug build, historical docs removed, remaining machine-path defaults eliminated |
| G3 | Client state/display/loading foundation | PASS (77e598cb,339351a0, b2993ae4) | client state tests, typed loading coordinator, atomic settings, explicit runtime profile validation |
| G4 | Launcher/login/display transition | Partial | transactional 800×600 → saved post-login mode is implemented; genuine human run, updater GUI and failure matrix still required |
| G5 | Character and loading path | Partial | appearance fields and typed loading path; 3D preview/video still required |
| G6 | Map10 world presentation | Not started | legacy/modern goldens, zero placeholders |
| G7 | Core UI live binding | Not started | per-dialog action/error/close evidence |
| G8 | Movement/combat/loot/map change | Not started | two-client human scenario |
| G9 | Effects and audio | Not started | event timelines, video and audio trace |
| G10 | Full assets/maps/UI | Not started | coverage matrix with no unexplained gaps |
| G11 | RC stability | Not started | clean-machine, fault injection, 24-hour soak |

## Invalid evidence

The following are never sufficient for an E5 claim: parser totals, CTest totals, headless state tests, auto-login screenshots, or modern-vs-modern SSIM.

## Recent reproducible evidence

| Evidence ID | Commit | Profile | Command | Result |
|---|---|---|---|---|
| `EVID-20260825-client-158` | `cd06a441` (includes `5936e521`, `6b1ef0f0`) | test fixtures | `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_brief=1` | 158/158 passed |
| `EVID-20260825-render-282` | `cd06a441` | test fixtures + `playdh-current` package parsing | `modern/build/tests/unit/render/mxh_render_tests.exe --gtest_brief=1` | 282/282 passed; synthetic missing-model warnings remain test-only |
| `EVID-20260825-governance` | `cd06a441` | repository manifests | `python scripts/check-project-governance.py` | passed |
| `EVID-20260825-release-client` | `dc5b567a` | `playdh-current` | `scripts\\build-modern.bat Release mxh_client` | x86 Release build passed; automation/credential CLI rejected under `NDEBUG` |
| `EVID-20260825-render-texture-gate` | `11539716` | `playdh-current` | `modern/build/tests/unit/render/mxh_render_tests.exe --gtest_brief=1` | 282/282 passed; Release world loading now rejects unresolved terrain/STM textures |
| `EVID-20260825-map10-closure` | `999b8965` | `playdh-current` | `modern/build/tests/unit/render/mxh_render_tests.exe --gtest_brief=1` | 283/283 passed; real Map10 HFL/STM and texture dependencies resolve through filesystem/PAK storage |
| `EVID-20260825-canonical-resource-tests` | `1de46337` | `playdh-current` | `ctest -C Debug --test-dir modern/build -R "PackFile|ReadMhBin_" --output-on-failure` | 100% passed; 3 tests skipped only for explicitly absent optional files |
| `EVID-20260825-reference-map10-start` | `cd06a441` | `sworking-2008-reference` (development-only) | `start_modern.ps1 -Mode start -ResourceProfileId sworking-2008-reference -MapNumber 10 -AllowDevFallbacks` | MapServer loaded 114 groups / 228 spawns; reference profile is not release-enabled |
| `EVID-20260825-current-map10-blocked-v2` | `5e75e43c` | `playdh-current` (`server-size-prefixed-opaque-v1`) | `start_modern.ps1 -Mode start -ResourceProfileId playdh-current -MapNumber 10 -Backend sqlite` | correctly fails closed with an explicit unsupported-encoding diagnostic for the current 22,766-byte `Monster_10.bin`; no reference-profile substitution occurs and no server process remains running |
| `EVID-20260825-full-ctest-v2` | `38fe583c` | local Debug build | `ctest -C Debug --test-dir modern/build --output-on-failure` | 12,134 registered; 12,134 passed; 19 tests skip for explicit missing/optional fixtures. The size-prefixed opaque-server-profile detector and classic MHFileEx regression tests are included; this is still not an RC gate because the skipped runtime/resource gates and human path remain open. |
| `EVID-20260825-settings-durable` | `7f262a5a` | local Windows temp profile | `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=ClientSettings.*` | settings round-trip passed after `FlushFileBuffers` and `MoveFileExW(MOVEFILE_WRITE_THROUGH|MOVEFILE_REPLACE_EXISTING)`; display/audio settings publication is durable and atomic. |
| `EVID-20260825-ui-resolution-propagation` | `13f838a5` | client state fixture | `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=CEngine.UiResolutionModeStartsAtLoginAndCanBePromotedAfterDisplayCommit` | passed; login starts at 800×600 and the committed post-login mode is propagated to CharSelect, CharMake and GameIn UI loading instead of remaining hard-coded to Low800x600. |
| `EVID-20260825-client-160` | `13f838a5` | test fixtures + PlayDH UI resources | `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_brief=1` | 160/160 passed, including login recovery, state transfer, UI runtime, GameIn interactions and post-login resolution mode propagation. |
| `EVID-20260825-mouse-capture` | `1d9c4983` | client window input path | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_brief=1` | x86 Debug client build and 160/160 client tests passed; button gestures now capture/release the HWND so drag rotation and release remain deterministic at client/pillarbox edges. |
| `EVID-20260825-target-picking` | `36cbe1be` | in-game input fixture | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=InGamePlayable.* --gtest_brief=1` | 6/6 playable-input tests passed; a left click now selects the live monster under the cursor, while empty-world clicks retain nearest-target fallback. |
| `EVID-20260825-mapchange-failure-state` | `571f880e` | client state fixture | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=CMapChange.* --gtest_brief=1` | 3/3 MapChange tests passed; progress, cancellation, zero-step rejection and explicit failure text are now observable by the host state coordinator. |
| `EVID-20260825-camera-wheel-zoom` | `27f54850` | in-game input + DX11 terrain camera | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=InGamePlayable.MouseWheelZoomIsBounded --gtest_brief=1` | camera distance is now driven by `WM_MOUSEWHEEL`, clamped to 3–12 world units, and propagated to the terrain camera. |
| `EVID-20260825-map-bounds` | `dc6c63ee` | Map HFL dimensions + movement fixture | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=InGameMovement.* --gtest_brief=1` | movement now accepts map-specific width/height bounds from the loaded HFL instead of relying only on the fixed 50,000-unit fallback; 7/7 movement tests passed. |
| `EVID-20260825-static-collision-query` | `35bf5600` | STM collision descriptors + in-game movement fixture | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/mxh_compat_tests.exe --gtest_filter=StmStaticModel.* --gtest_brief=1` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=InGamePlayable.StaticCollisionQueryRejectsCandidateStep --gtest_brief=1` | STM collision-model bounding descriptors are parsed and exposed through an explicit query; blocked candidate movement is rejected. Exact triangle collision remains pending the legacy `.col` sidecar integration. |
| `EVID-20260825-placeholder-release-gate` | `ad314a54` | `playdh-current` client render policy | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/render/mxh_render_tests.exe --gtest_filter=EntityScene* --gtest_brief=1` + `python scripts/check-project-governance.py` | 6/6 EntityScene regression tests and governance check passed; RenderBox stand-ins are disabled by default and only enabled by explicit `--debug-ui-bounds`. Missing models remain counted and fail the Map10 visual gate rather than being presented as game assets. |
| `EVID-20260825-server-profile-fail-closed` | `2969aad9` | `playdh-current` and `sworking-2008-reference` | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/mxh_compat_tests.exe --gtest_filter=MhFileEx.*` + `modern/build/tools/MoxianMapServer/mxh_map_server_KOR.exe --map 10 --resource-root modern\\data\\PlayDH --db modern\\build\\runtime\\plan_probe.db` | 10/10 MHFileEx profile tests passed; current 22,766-byte opaque Monster_10.bin is rejected through the explicit profile API, while the reference profile remains selectable only by explicit ID. MapServer exits with a clear unsupported-encoding diagnostic instead of positional-decoding garbage. |
| `EVID-20260825-profile-propagation` | `e41fe1a6` | `playdh-current` | `pwsh -NoProfile -File deploy\\scripts\\start_modern.ps1 -Mode start -DryRun -ResourceProfileId playdh-current -MapNumber 10` | Dry-run shows the selected profile and `--resource-profile playdh-current` on MapServer; profile metadata now identifies server-size-prefixed-opaque-v1 separately from text encoding Big5. |
| `EVID-20260825-loading-transaction` | `9e277c01` | client loading fixture + runtime load path | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=GameLoadingCoordinator.*:CGameLoading.*:CMapChange.* --gtest_brief=1` | 8/8 loading/state tests passed; world loading reports ordered stages and leaves the previous scene render/capture state intact when descriptor, terrain, STM, sky or entity activation fails. Late progress callbacks cannot regress the visible stage count. |
| `EVID-20260825-client-profile-gate` | `cea7a845` | client profile selection | `cmd /c scripts\\build-modern.bat` + `modern/build/tools/MoxianClient/mxh_client.exe --resource-profile invalid` + `modern/build/tools/MoxianClient/mxh_client.exe --resource-profile sworking-2008-reference --resource-root modern\\data\\PlayDH` | Unknown profiles and root/profile mismatches are rejected before resource activation; human launcher now propagates the selected profile. Reference profile remains development-only in Release. |
| `EVID-20260825-playdh-manifest-clean` | `bdf9178c` | `playdh-current` canonical tree | `pwsh -NoProfile -File scripts\\generate-resource-manifest.ps1 -Root modern\\data\\PlayDH -Output reference\\manifests\\playdh-current.sha256.json -ProfileId playdh-current` + `python scripts/check-project-governance.py` | Removed four exact SQLite runtime artifacts from the canonical resource root into recoverable quarantine; regenerated manifest now covers 5,135 files / 1,681,141,995 bytes and contains no `moxian_*.db`, WAL or SHM entries. Governance passed. |

| `EVID-20260825-launcher-shell` | `d2d5a525` | `playdh-current` | `cmd /c scripts\\build-modern.bat` + `python scripts/check-project-governance.py` | New Win32 launcher builds in the clean Debug graph. It persists only non-secret launch settings, passes explicit profile/display parameters to the client, never accepts credentials, and blocks check/repair until a signed manifest transport is configured. |
| `EVID-20260825-display-dpi-transition` | `1d8c6e06` | client display path | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_brief=1` | x86 Debug build and 167/167 client tests passed; post-login client-area sizing now uses the current monitor DPI when available and retains rollback behavior on failure. |
| `EVID-20260825-launcher-settings-json` | `472c656d` | launcher settings | `cmd /c scripts\\build-modern.bat` + `python scripts/check-project-governance.py` | Launcher settings now use `%LOCALAPPDATA%\\Moxian\\settings.json`, validate the profile and dimensions on load, and atomically replace the file on save; no credential field or command-line credential path exists. |
| `EVID-20260825-launcher-client-resolution` | `f3ea2006` | local launcher/client layout | `cmd /c scripts\\build-modern.bat` + `python scripts/check-project-governance.py` | Launcher now probes only the explicit release name `MoxianClient.exe` and modern build name `mxh_client.exe`, with a clear failure if neither exists; no arbitrary executable search is permitted. |
| `EVID-20260825-launcher-resource-root` | `80f4d0b8` | local launcher/resource layout | `cmd /c scripts\\build-modern.bat` + `python scripts/check-project-governance.py` | Launcher passes an explicitly discovered canonical `data\\PlayDH` root when present, preventing client startup from depending on the caller's working directory. |
| `EVID-20260825-launcher-settings-merge` | `a8c7cb7e` | shared launcher/client settings | `cmd /c scripts\\build-modern.bat` + `python scripts/check-project-governance.py` | Launcher updates only profile/display keys in the shared JSON and preserves server, locale, audio, VSync and last-account fields; it no longer overwrites client-owned settings. |
| `EVID-20260825-sfx-channel` | `95aa21dd` | `playdh-current` SoundList | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/mxh_compat_tests.exe --gtest_filter=SfxPlayer.* --gtest_brief=1` | Added an independent one-shot WAV channel using SoundList metadata, Media Foundation PCM decoding and XAudio2; streaming BGM entries are rejected by the SFX resolver; 2/2 tests passed. |
| `EVID-20260825-sfx-entry-volume` | `25f0e847` | `playdh-current` SoundList | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/mxh_compat_tests.exe --gtest_filter=SfxPlayer.* --gtest_brief=1` + `python scripts/check-project-governance.py` | SFX gain now consumes per-entry SoundList volume multiplied by the global SFX volume, with safe default for zero/legacy entries; 2/2 tests passed. |
| `EVID-20260825-sfx-distance-gain` | `4a549cdd` | `playdh-current` SoundList | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/mxh_compat_tests.exe --gtest_filter=SfxPlayer.* --gtest_brief=1` + `python scripts/check-project-governance.py` | Added `playAt()` and deterministic linear attenuation using each entry's `min_distance`/`max_distance`; 3/3 SFX tests passed. |
| `EVID-20260825-sfx-looping` | `3f6b21bc` | `playdh-current` SoundList | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/mxh_compat_tests.exe --gtest_filter=SfxPlayer.* --gtest_brief=1` + `python scripts/check-project-governance.py` | XAudio2 now honors each WAV entry's `loop` flag, retaining one-shot end-of-stream behavior for non-looping entries; 3/3 SFX tests passed. |
| `EVID-20260825-bgm-entry-volume` | `113e2d3a` | `playdh-current` SoundList | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/mxh_compat_tests.exe --gtest_filter=BgmPlayer.*:SfxPlayer.* --gtest_brief=1` + `python scripts/check-project-governance.py` | BGM playback now consumes per-entry volume together with the global BGM volume; BGM/SFX lifecycle suite 8/8 passed. |
| `EVID-20260825-spatial-combat-cues` | `c6cd5b04` | client combat/audio path | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=InGamePlayable.LeftClickAttacksNearestLiveMonster:InGamePlayable.LeftClickPrefersMonsterUnderCursor --gtest_brief=1` + `python scripts/check-project-governance.py` | Attack and skill cues now carry player-to-target distance through CEngine into `SfxPlayer::playAt`; targeted combat input tests 2/2 passed. |
| `EVID-20260825-ui-click-sfx` | `9a5899ff` | `playdh-current` client input/audio path | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_brief=1` + `python scripts/check-project-governance.py` | Client initializes the real SFX channel, selects a data-driven non-streaming UI/button WAV, and triggers it on completed left-click input; 167/167 client tests and governance passed. |
| `EVID-20260825-combat-audio-cues` | `52ad2231` | client combat/audio seam | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=InGamePlayable.LeftClickAttacksNearestLiveMonster:InGamePlayable.LeftClickPrefersMonsterUnderCursor --gtest_brief=1` + `python scripts/check-project-governance.py` | CEngine now exposes semantic audio cues; attack and quick-skill sends emit real combat cues, with null-engine-safe unit behavior; targeted 2/2 tests passed. |
| `EVID-20260825-combat-audio-resource-mapping` | `136944e6` | `playdh-current` SoundList + combat events | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_brief=1` + `python scripts/check-project-governance.py` | UI, attack/swing/weapon, and skill/magic/mugong WAV IDs are selected independently from actual profile filenames, with first-real-WAV fallback only when a semantic name is absent; 167/167 client tests passed. |
| `EVID-20260825-semantic-audio-test` | `d9910578` | client engine audio seam | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=CEngine.* --gtest_brief=1` + `python scripts/check-project-governance.py` | CEngine semantic `Attack`, `Skill`, and `UiClick` cues are independently observable by the host callback; 2/2 targeted tests passed. |
| `EVID-20260825-pickup-audio-cue` | `d38de493` | `playdh-current` client item/audio path | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=CEngine.EmitsSemanticAudioCuesToHost --gtest_brief=1` + `python scripts/check-project-governance.py` | Confirmed `PickupAck` emits a semantic pickup cue routed through the real SFX channel; cue dispatch is unit-tested. |
| `EVID-20260825-pickup-inventory-live-state` | `67dd2647` | `playdh-current` client item path | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=InGamePlayable.MonsterObtainNotifyBecomesGroundDropAndPickupAckClearsIt --gtest_brief=1` + `python scripts/check-project-governance.py` | `PickupAck` now consumes the authoritative item/count payload, removes the ground drop, inserts the item into the first empty live inventory slot, and exposes it through `game_info()`; targeted test passed. |
| `EVID-20260825-inventory-ui-live-refresh` | `5a6d657b` | `playdh-current` GameIn UI | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_brief=1` + `python scripts/check-project-governance.py` | Opening `IN_INVENTORYDLG` now binds the real `InventoryServiceImpl` over `GameInInfo.items`, refreshes visible slots and money, and keeps the UI on the same live state as PickupAck; 168/168 client tests passed. |
| `EVID-20260825-shop-money-live-state` | `ccc20bfe` | `playdh-current` modern Map/Client | `cmd /c scripts\\build-modern.bat` + client money/shop tests + `mxh_server_handler_tests.exe --gtest_filter=MapHandlerTest.BuySynOkArmPersistsMoneyToSqliteMemory:MapHandlerTest.BuySynOkArmDeductsMoneyAndInsertsInventory --gtest_brief=1` | Successful BuySyn now publishes an authoritative `Money` message before the unchanged BuyAck/TotalInfoLocal pair; client updates HUD/inventory money and server/client shop tests passed. |
| `EVID-20260825-useack-live-state` | `74fe628a` | `playdh-current` client item path | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=InGamePlayable.UseAckConsumesLiveInventorySlotAndUpdatesVitals --gtest_brief=1` | `UseAck` now consumes the acknowledged inventory slot and applies authoritative current HP/MP to `GameInInfo`; targeted test passed. |
| `EVID-20260825-discardack-live-state` | `06bb419a` | `playdh-current` client item path | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=InGamePlayable.DiscardAckClearsLiveInventorySlot --gtest_brief=1` | `DiscardAck` now clears the confirmed inventory slot and refreshes the same live state consumed by Inventory UI; targeted test passed. |
| `EVID-20260825-moveack-live-state` | `a7d153c9` | `playdh-current` client item path | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=InGamePlayable.MoveAckReordersLiveInventorySlots --gtest_brief=1` | `MoveAck` now validates the echoed item DB id/target slot, swaps the authoritative live inventory entries, repairs slot positions, and refreshes Inventory UI; targeted test passed. |
| `EVID-20260825-sell-payout-authority` | `78e6a3a0` | ItemList/ItemManager sell data plane | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/server/mxh_shop_item_manager_tests.exe --gtest_filter=ItemSellSideEffect.SellPayout* --gtest_brief=1` | Added strict payout calculation that accepts only the authoritative `ItemInfo.SellPrice`, rejects zero quantity/price and 32-bit overflow, and never derives a price from BuyPrice; 2/2 tests passed. |
| `EVID-20260825-sell-runtime-wire` | `f2a21094` | `playdh-current` Map/Client item path | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=InGamePlayable.SellAckRemovesSoldQuantityFromLiveInventory --gtest_brief=1` | MapHandler now validates dealer proximity, authoritative ItemManager SellPrice, quantity and money overflow, mutates the runtime inventory/money, persists both, and emits SellAck + Money + TotalInfoLocal; client applies SellAck quantity changes. Runtime success still requires a loaded ItemList and a real NPC, so this row is E2/E3, not E5. |
| `EVID-20260825-sell-server-e2e` | `58f508ef` | synthetic formal ItemList + DealItem NPC on Map 7 | `cmd /c scripts\\build-modern.bat` + `mxh_server_handler_tests.exe --gtest_filter=MapHandlerTest.SellSynUsesItemListSellPriceAndPublishesState --gtest_brief=1` | Full server success path passed: loaded ItemList SellPrice, NPC at player position, bought inventory item, SellSyn, SellAck + Money + TotalInfoLocal ordering, payout 100, inventory count 0, and persistence path exercised. This is E3/E4 fixture evidence, not a real-client E5 run. |
| `EVID-20260825-loading-request-reentry` | `098bc5be` | client GameLoading state transfer | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=GameLoadingCoordinator.* --gtest_brief=1` + `python scripts/check-project-governance.py` | Fixed a real re-entry deadlock: the main loop now consumes any newly pending GameEntryRequest instead of suppressing it after the first request. Three coordinator tests pass, including a second request for a different character/map with progress reset. This remains E3/E4 engineering evidence, not E5. |
| `EVID-20260825-npc-role-routing` | `6a749df4` | `playdh-current` client/server NPC interaction path | `cmd /c scripts\\build-modern.bat` + `mxh_client_tests.exe --gtest_filter=InGamePlayable.BKeyOpensNearestNpcShopThenBuyClickSelectsCatalogItem:InGamePlayable.BKeyDoesNotOpenShopForQuestNpc --gtest_brief=1` + `mxh_server_handler_tests.exe --gtest_filter=MapHandlerTest.SpeechSynShopListFeedsClientThenBuySynDebitsMoney --gtest_brief=1` + `python scripts/check-project-governance.py` | NPC interaction now routes by legacy role: Dealer/Bobusang opens the shop, while quest NPCs no longer fall through to a shop request; the server only emits ShopList for a dealer catalog. Existing dealer client/server flows and the new quest-NPC regression pass. This is E3/E4 engineering evidence, not E5. |
| `EVID-20260825-agent-map-route-directory` | `025147a4` | AgentHandler map routing | `cmd /c scripts\\build-modern.bat` + `mxh_server_handler_tests.exe --gtest_filter=AgentHandlerTest.GameInUsesMapSpecificServerRoute:AgentHandlerTest.RegisterSessionStoresUserCharMap --gtest_brief=1` + `python scripts/check-project-governance.py` | Added a map-number keyed MapServer route directory. Existing `set_map_server` remains the default-map route, while GameIn and ordinary forwarded gameplay messages choose the registered endpoint for the session map and fall back safely to the default. Two routing tests pass. This is E3 infrastructure evidence, not E5. |
| `EVID-20260825-agent-map-route-runtime-config` | `bf34b7e2` | MoxianAgentServer map route configuration | `cmd /c scripts\\build-modern.bat` + `modern/build/tools/MoxianAgentServer/mxh_agent_server_KOR.exe --help` + `python scripts/check-project-governance.py` | AgentServer now accepts explicit `--map-server-map MAP=HOST:PORT` entries, opens independent legacy-framed MapServer connections, registers them in the map route directory, and cleans them up without replacing a failed endpoint with another map. Existing `--map-server H:P` remains the default-map route. This is E3 infrastructure evidence, not E5. |
| `EVID-20260825-agent-change-map-forward` | `30e2c945` | AgentHandler ChangeMapSyn route | `cmd /c scripts\\build-modern.bat` + `mxh_server_handler_tests.exe --gtest_filter=AgentHandlerTest.ChangeMapUsesTargetRouteAndClosesCurrentRoute --gtest_brief=1` + `python scripts/check-project-governance.py` | Legacy `ChangeMapSyn` now selects the target map route, sends channel-transfer `GameOutSyn` to the current map, rebuilds a target-map `GameInSyn`, and returns `ChangeMapAck` after the target accepts the send; unavailable targets receive `ChangeMapNack`. Target-route handoff test passed. This is E3/E4 transport evidence; client scene activation and E5 gameplay remain pending. |
| `EVID-20260825-client-change-map-wire` | `1c482e3f` | client MapChange NPC input | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_brief=1` + `python scripts/check-project-governance.py` | MapChange NPC interaction now emits the protected `UserConn::ChangeMapSyn` protocol with the legacy baseline destination map and preserves the current scene until an acknowledgement; ChangeMapAck/Nack are explicitly handled. Full client suite passed. Scene loading, target GameInAck activation and E5 runtime travel remain pending. |
| `EVID-20260825-client-change-map-activation` | `ef3a177e` | client GameIn/MapChange state transition | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_brief=1` + `python scripts/check-project-governance.py` | ChangeMapAck now creates a typed GameEntryRequest and requests GameLoading for the target map; load failure returns to the still-active GameIn scene instead of forcing CharSelect. Full 175-test client suite and governance passed. This remains E3/E4 state evidence; target-map real-server and E5 human travel are pending. |
| `EVID-20260825-target-map-handler-bootstrap` | `e2f2e9ae` | MapHandler target GameIn path | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/server/mxh_server_handler_tests.exe --gtest_filter=MapHandlerTest.TargetMapGameInUsesTargetMapAndEmitsAck --gtest_brief=1` | Direct target-map handler test proves the synthesized GameInSyn creates an active runtime snapshot with map 10 and emits a real GameInAck for the requested character. This is E3 target-handler evidence; a live three-server human map travel remains pending. |
| `EVID-20260825-change-map-missing-route` | `b7701f39` | AgentHandler ChangeMap failure path | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/server/mxh_server_handler_tests.exe --gtest_filter=AgentHandlerTest.ChangeMapWithoutTargetRouteReturnsNackAndKeepsCurrentMap --gtest_brief=1` + `python scripts/check-project-governance.py` | ChangeMap now requires an explicitly registered target-map route; an unavailable destination returns ChangeMapNack without sending GameOutSyn to the current map. This is E3/E4 failure-path evidence; live-server and human travel remain pending. |
| `EVID-20260825-full-ctest-map-routing` | `b7701f39` | local x86 Debug build | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure` | 12,162/12,162 registered tests passed; 19 tests skipped only for explicit missing/optional fixtures (including MSSQL and current resource blockers). This is repository regression evidence, not an E5 playable claim. |
| `EVID-20260825-skill-effect-timeline` | `154594df` | client skill protocol path | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_filter=InGameEffects.ConfirmedSkillMessagesProduceDeterministicTimeline --gtest_brief=1` + `python scripts/check-project-governance.py` | Confirmed SkillStartAck and SkillSingleResult now produce bounded CastRelease → Hit → End events with skill, target, damage and hit result fields; targeted test and governance passed. This is E3 presentation-seam evidence, not proof that BEFF assets are rendered or E5-complete. |
| `EVID-20260825-agent-map-handler-handoff` | `d8ae8c50` | Agent + MapHandler change-map path | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/server/mxh_server_handler_tests.exe --gtest_filter=AgentHandlerTest.ChangeMapBootstrapsTargetMapHandlerAndRelaysGameInAck --gtest_brief=1` | In-process integration proves ChangeMapSyn sends GameOutSyn to the current route, target MapHandler creates an active map=10 runtime snapshot, and its GameInAck is relayed to the client alongside ChangeMapAck. This is E3/E4 infrastructure evidence; no live human E5 claim. |
| `EVID-20260825-charselect-real-preview-scene` | `WORKTREE` | `playdh-current` character presentation | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build -R "CharSelect|CCharSelectState" --output-on-failure` + `python scripts/check-project-governance.py` | CharSelect now owns a real EntityScene preview loaded from the current resource profile. The selected ChrTotalInfo appearance is converted to ScenePlayer and rendered with a dedicated 4:3 camera before screen-space UI; no RenderBox/debug avatar is enabled. 27/27 CharSelect tests passed. This is E3 runtime wiring; a legacy-client visual comparison and human E5 run remain pending. |
| `EVID-20260825-charmake-real-preview-scene` | `WORKTREE` | `playdh-current` character creation presentation | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build -R "CharSelect|CCharSelectState|CharacterMake" --output-on-failure` + `python scripts/check-project-governance.py` | CharMake now feeds the live form model (sex, face, hair and equipped item selections) into the same real EntityScene preview camera, so option changes are reflected before submission. No synthetic avatar geometry is enabled. This is E3 runtime wiring; legacy visual comparison and human E5 remain pending. |
| `EVID-20260825-real-loading-dialog` | `WORKTREE` | `playdh-current` `Image/InterfaceScript/NewLoadDlg.bin` | `cmd /c scripts\\build-modern.bat` + `modern/build/tests/unit/client/mxh_client_tests.exe --gtest_brief=1` + `python scripts/check-project-governance.py` | GameLoading now loads and renders the shipped `NewLoadDlg.bin` tree through `ClientUiRuntime` on state entry, while the coordinator continues updating live progress and failure context. The old text-only loading placeholder is no longer the product render path. 176/176 client tests and governance passed. This is E3 UI integration; real Map10 human loading and E5 visual comparison remain pending. |
| `EVID-20260825-human-acceptance-runner` | `WORKTREE` | tracked `scripts/run-human-acceptance.ps1` | `powershell -NoProfile -Command "Get-Command -LiteralPath .\\scripts\\run-human-acceptance.ps1"` + `python scripts/check-project-governance.py` | Added a tracked human-only runner that supplies no credentials or synthetic input, uses a unique run directory, passes the explicit resource profile, refuses to take over an existing managed server state, verifies every started PID against its exact executable path, captures stdout/stderr, and only stops processes it recorded. This is a safety/runbook gate; it does not itself constitute human E5 evidence. |
| `EVID-20260825-full-ctest-human-runner` | `6457b250` | local x86 Debug build | `ctest -C Debug --test-dir modern/build --output-on-failure` | Full registered regression completed: 12,164/12,164 tests passed in 368.54s. 19 tests were skipped only for explicit missing/optional fixtures, including the known opaque current Monster_10 profile, MSSQL environment gates, and optional legacy samples. This remains repository regression evidence, not proof of a playable RC or E5 visual fidelity. |
| `EVID-20260825-mapchange-real-state` | `b3c01a64` | local x86 Debug build | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "GameState|CMainGame|InGameState"` | MapChange now owns a real loading UI runtime and shares the GameLoadingCoordinator pipeline; ChangeMapAck enters MapChange, keeps the active scene until target activation succeeds, and uses the same failure rollback path. Build and 13 targeted tests passed. A parallel full CTest run had two fixture timing failures; both passed on immediate serial rerun. This is E3/state evidence only; runtime Map10 remains blocked by the current opaque Monster_10 profile. |
| `EVID-20260825-effect-catalog` | `51d513de` | `playdh-current` | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "EffectCatalog|CInGameState"` | Runtime now indexes the shipped Effect.pak dependency closure (MOD/CHX/ANM) plus loose BEFF/BEFL assets without changing bytes or inventing IDs. Real profile catalog test passed with >1,000 BEFF assets and >1,000 packed effect dependencies; CInGameState logs explicit catalog coverage at GameIn start. This is E1/E2 resource evidence, not proof of BEFF playback or E5 visual fidelity. |
| `EVID-20260825-beff-structure-decode` | `cf431bbe` | `playdh-current` | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "EffectCatalog"` | Loose BEFF scripts now decode through the recovered MHFileEx header/type/size transform and expose verified `#MAXEFFECTUNIT`, `#MAXTRIGGER`, and dependency directives. Real catalog test passed with >1,000 decoded BEFF scripts and a decoded `become_h.beff` summary. This is E1 parser/structure evidence; trigger execution and visual playback remain incomplete. |
| `EVID-20260825-beff-trigger-summary` | `b5486222` | `playdh-current` | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R EffectCatalog` | BEFF summaries now retain exact unit indices/types and trigger records (`time_token`, unit, operation) from the original script, including `f0` frame tokens without guessing TickPerFrame conversion. Real `become_h.beff` unit/trigger counts and first trigger assertions passed. This remains parser/timeline-input evidence, not runtime effect execution. |
| `EVID-20260825-effect-timeline` | `7229b18d` | synthetic decoded BEFF summaries | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "EffectTimeline|EffectCatalog"` | Added deterministic trigger scheduler: explicit frame duration, millisecond/frame token conversion, stable ordering, monotonic clock protection, one-shot emission, and invalid-token rejection. Five effect catalog/timeline tests passed. This is E2 timeline infrastructure; renderer/audio trigger execution remains open. |
| `EVID-20260825-effect-runtime` | `3942536a` | `playdh-current`, `become_h.beff` | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R EffectRuntime` | Client EffectRuntime now creates active instances only from decoded catalog scripts, advances deterministic timelines, emits trigger events with source/target object IDs, and removes completed instances. Real `become_h.beff` runtime test passed; missing effects fail without fallback. This is E2/E3 execution infrastructure, not yet particle/mesh/audio rendering evidence. |
| `EVID-20260825-gamein-effect-runtime` | `37f58c17` | GameIn + `playdh-current` | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "CInGameState|EffectRuntime"` | CInGameState now owns the EffectRuntime and exposes its catalog/runtime boundary; GameIn startup loads one authoritative effect catalog instead of a separate orphan index. Four targeted tests passed. This is integration infrastructure; no claim of completed in-game visual effect playback. |
| `EVID-20260825-effect-id-lists` | `18205269` | `playdh-current` `List_M.befl`/`List_W.befl` | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "EffectCatalog|EffectRuntime"` | EffectCatalog now decodes the original gender-specific BEFL lists and resolves numeric effect IDs to real BEFF names (including ID 0 → `shadow.beff`). Five targeted catalog/runtime tests passed. This removes the name-resolution gap; server/client skill event mapping and visual execution still remain. |
| `EVID-20260825-effect-runtime-id` | `2bb5ed8d` | `playdh-current` effect ID lists | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R EffectRuntime` | EffectRuntime now starts timelines by authoritative gender-specific effect ID and rejects unknown IDs without selecting a nearby or fabricated asset. Four runtime tests passed, including real BEFF execution and missing-ID fail-closed behavior. |
| `EVID-20260825-skill-effect-refs` | `ca12bba5` | SkillList parser | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R SkillListParser` | SkillList parsing now preserves the original five effect reference tokens per skill (including the exact BEFF filename instead of collapsing every non-zero value to `1`). Build and 9 parser tests passed; the two real-file tests were skipped because their legacy fixture path is unavailable in this environment. This is E1 parser evidence, not runtime skill playback evidence. |
| `EVID-20260825-skill-manager-effect-names` | `5b12e781` | SkillManager | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "SkillManager|SkillListParser"` | SkillManager now retains and exposes the five original BEFF names by SkillIdx after `init_from_bin`; clearing/reinitializing also clears the reference map. 32 targeted tests passed, with only the two unavailable legacy-path tests skipped. This is E1/E2 data plumbing, not proof of live skill playback. |
| `EVID-20260825-gamein-skill-effect-runtime` | `a97cdf59` | GameIn + current `SkillList.bin`/EffectScript | `cmd /c scripts\\build-modern.bat` + `ctest -C Debug --test-dir modern/build --output-on-failure -R "CInGameState|EffectRuntime|SkillManager|SkillListParser"` | GameIn now loads the current SkillList, resolves `effect_use` by SkillIdx on successful quick-slot casts, starts EffectRuntime only when the host supplies an explicit frame duration, advances it each Process tick, and exposes a bounded runtime-event queue. 37 targeted tests passed; unavailable legacy-path tests remain explicitly skipped. This is E2/E3 integration evidence; actual particle/mesh/audio consumers remain open. |

These are E1–E3 engineering gates only. They do not upgrade G4–G11 to E5 and do not replace a genuine legacy-client human run.

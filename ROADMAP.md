# Moxian-Reborn 路线图：1:1 完美复现

> **项目代号**：Moxian-Reborn（墨香重生）
> **终极目标**：在现代软硬件环境下 **1:1 完美复现** 2003-2010 韩国 2D MMORPG《墨香》——
> 玩法、数值、协议、资源、UI 全部和原版一致；只在底层换技术栈。
> **最近一次重置**：2026-07-25（清掉所有历史 session 噪音、重新对齐到终极目标）。
> **最近一次重构**：2026-08-06 — 把 434 行 ROADMAP 砍成可执行的规划文档；历史 [x] 项迁移到 docs/CHANGELOG.md。
> **最近一次状态刷新**：2026-08-08 — D4.47 ItemSell + D4.43 ItemMove + D4.49 ItemDivide + D4.46 ItemDiscard + D4.50 ItemCombine + D4.51 ItemBuy + D4.52 ItemMix + D4.53 ItemDissolution + D4.55 ItemUnseal + D4.58 UseForQuestStart + D4.57 ItemMixRelease + D4.59 ShopItemChangeMap + D4.61 DealerOpen + D4.60 UseChangeItem + D4.64 ShopItemInfo + D4.65 ShopItemMpInfo + D4.62 PyogukItemInfo + D4.63 PetInvenInfo + D4.66 GuildWarehouseInfo + D4.67 GuildWarehouseLeave + D4.68 GuildMove + D4.69 AvatarTakeoff + D4.70 AvatarChange side-effect runtime orchestrators (+141 tests: ACK/NACK/NPC-gate + silent-rt99 + silent-success + 2-step success chain/looted gate + combine-ACK/error-NACK + 4-way buy-gate + 5-way mix-rt + plain-NACK dissolution + protocol-flip unseal + quest-ecode quest-start + pure slot-unlock + no-op receipt + dealer-ack + inverse-USE + init-reset/DB-reload + saved-movepoint reload + warehouse-load dedup chain + pet-inventory reload + guild-warehouse reload + server-side warehouse-leave + warehouse item-move + avatar take-off + avatar option-recalc/puton-broadcast wire-through dispatch); cumulative **~11,516 tests 99% PASS** (was 11,375; known LoginServerFixture timing flakes retry-PASS incl. WireFingerprintIsDeterministicAcrossRuns; commercial smoke gate 33/33).

---

## 0. 不可破坏的约束（与原版 1:1 的边界）

| 约束 | 内容 |
|---|---|
| 资源格式 | .bin / .pak / .bmhm / .ttb / .chl / .chx / .chr / .mon / .bsad / .mhs 二进制结构 |
| 网络协议 | [CC]Header/Protocol.h 96 类 Category + CommonStruct.h 网络包结构（含 #pragma pack(push,1)） |
| 玩法数值 | 经验曲线、伤害公式、爆率、Boss 刷新、商城、MurimNet PvP |
| HSEL 接口 | CHSEL::CHSEL / Encode / Decode 签名必须保持（实现可换） |
| 资源路径 | 客户端启动后从 MHVerInfo.ver 读 Distribute 地址；服务器配置 ServerSet/1/*.txt |

> 凡是不在这个表里的，都可以动：编译工具、运行时、UI 控件树实现、数据库驱动、网络底层。

---

## 1. 终极目标的可执行定义

| 目标 | 验证方式 |
|---|---|
| **T1. 资源字节级一致读取** | 任何 墨香【源码配套资源】/PlayDH/ 下的文件，modern 工具读出来的数据和老客户端读到的一致 |
| **T2. 协议字节级一致收发** | 截一段客户端↔服务端原始网络包，modern 工具的编解码和原码编解码结果完全一致 |
| **T3. 行为 1:1 复现** | 跑同一段操作（登录→进图→打怪→PK→进商城），modern 实现的副作用顺序、数值、UI 状态和原码完全一致 |

3 个 T 全过 = 1:1 复现完成。

---

## 2. 现状快照（2026-08-06）

| 维度 | 状态 | 验证 |
|---|---|---|
| T1 资源字节兼容 | **LOCKED** | 303 records SHA-256 locked (deploy 180 [Server/ + QuestScript/] + PlayDH 94 + PlayDH/Client 29) ; 268 parse test entries verified (89 -> 268 across 5 suites) |
| T2 协议字节兼容性 | **100%** | 85 wire-format goldens round-trip byte-equal (96 mxh_wire_format_tests = 11 wire invariants + 85 golden), 77/77 legacy MP_* categories; 24 wire-format -> dispatcher integration tests (D4.R3) lock (cat, proto, obj_id) 1:1 between legacy bytes and modern dispatch; commercial smoke gate 33/33 PASS |
| T3 UI 1:1 port | **PORTING** | 109/158 dialog hpp，2500+ ui tests PASS |
| T3 玩法数值 1:1 | **PARTIAL** | D1 SkillList ✓ D2 BattleFactory ✓ D3 QuestManager runtime bridge ✓ D4.20 UseShopItem decision ✓ D4.21 CheckEndTime realtime branch ✓ D4.22 CheckAvatarEndtime data plane ✓ D4.23 CalcAvatarOption data plane ✓ D4 UseShopItemUpdateToDB ✓ D4 CalcShopItemOption ✓ D4 UpdateLogoutToDB ✓ D4.24 AddDupParam/DeleteDupParam/IsDupAble data plane (5 dup categories, 33 tests) ✓ D4.25 IsDupItem + IsRareOptionItem + IsOptionItem data plane (13 always-dup kinds + Sundries/Incantation/Skin + 19 tests) ✓ D4.26 IsPetSummonItem/IsTitanCallItem/IsTitanEquipItem + GetItemKindType + playtime_decrement data plane (3 predicates + ItemKind/Type reader + 30s-clamp decrement + 22 tests) ✓ D4.27 DiscardAvatarItem ✓ D4.28 CalcPlusTime ✓ D4.29 SetExtraSlotCount ✓ D4.30 looting_thresholds ✓ D4.31 PutOnAvatarItem/TakeOffAvatarItem ✓ D4.32 PutSkinSelectItem ✓ D4.33 DiscardSkinItem/RemoveEquipSkin ✓ D4.34 AddUsingShopItem ✓ D4.35 CheckEndTime side-effect dispatcher ✓ D4.36 PutOnAvatarItem ✓ D4.37 TakeOffAvatarItem ✓ D4.38 PutSkinSelectItem side-effect dispatcher ✓ D4.39 PutSkinSelectItem NACK (5 tests) ✓ D4.40 DiscardSkinItem side-effect (3 tests) ✓ D4.41 SavePointAdd (8 tests) ✓ D4.42 SavePointUpdate/Del (7 tests) ✓ D4.43 ItemMove (8 tests) ✓ D4.44 PyogukMoney PutIn/PutOut (14 tests) ✓ D4.45 BuyPyogukSyn (8 tests) ✓ D4.46 ItemDiscard (7 tests) ✓ D4.47 ItemSell (7 tests) ✓ D4.48 ItemUse (5 tests) ✓ D4.49 ItemDivide (6 tests) ✓ D4.50 ItemCombine (5 tests) ✓ D4.51 ItemBuy (9 tests) ✓ D4.52 ItemMix (11 tests) ✓ D4.53 ItemDissolution (5 tests) ✓ D4.54 ItemMixAddItem (13 tests, 7-way validation) ✓ D4.55 ItemUnseal (5 tests) ✓ D4.56 ReviveOther (10 tests, 4-way) ✓ D4.57 ItemMixRelease (7 tests) ✓ D4.58 UseForQuestStart (7) ✓ D4.59 ShopItemChangeMap (5) ✓ D4.60 UseChangeItem (8) ✓ D4.61 DealerOpen (8) ✓ D4.62 PyogukItemInfo (10) ✓ D4.63 PetInvenInfo (7) ✓ D4.64 ShopItemInfo (5) ✓ D4.65 ShopItemMpInfo (5) ✓ D4.66 GuildWarehouseInfo (5) ✓ D4.67 GuildWarehouseLeave (5) ✓ D4.68 GuildMove (9) ✓ D4.69 AvatarTakeoff (9) + D4.70 AvatarChange (5) + D4.71 ChaseUse (7) + D4.72 ShopItemSeal (21) + D4.73 ShopItemShout (11) + D4.74 ItemUpgrade (6) + D4.75 ItemReinforce (8) + D4.76 ShopItemNameChange (7) + D4.77 ShopItemChase (9) + D4.78 ReviveOtherAck (17) + D4.79 AvatarUse (15) + D4.80 JobChange (15) + D4.81 ReinforceReset (22) + D4.82 RareCreate (28) + D4.83 ShoutAck,Nack (9) + D4.84 CharChange (22) + D4.85 ReinforceWithShopItem (8) + D4.86 TitanDelegate (7) + D4.87 ReviveOtherNack (7) + D4.88 CurseCancellation (16) + D4.89 AddItemSlotLock (13) + D4.90 ReleaseSlotLock (7) + D4.91 UniqueItemMix (16) + D4.92 DecorationOn (7) + D4.93 AgentNote (20) + D4.94 AgentGuildNotify (28) + D4.95 AgentParty (24) + D4.96 AgentFriend (27) + D4.97 Cheat plan (30) + D4.98 UserConn plan (37) + D4.99 Chat plan (15) ✓ D4.100 SiegeWar side-effect plan - USER 7 actions (Cheat/MoveIn/BattleJoin/BattleJoinNack/ObserverJoin/LeaveSyn/Default) + SERVER 5 actions (Taxrate/ReturnToMap/FlagChange/Default/Drop) + SiegeWarUserAction extended with 5 fields (data2_target_map/data3_target_map/guild_idx/return_map_num/observer_flag) + 19 tests ✓ D4.101 GuildUnion side-effect plan - USER 3 actions (Drop/SendCreateNack/ForwardToMap) + SERVER 2 actions (BroadcastToOtherMaps/DropUnknown) over 7 MP_GUILD_UNION_*_NOTIFY_TO_MAP protocols + GuildUnionRequest/Action/ServerRequest/ServerAction + 17 tests ✓ D4.102 Wanted side-effect plan - SERVER 4 actions (Drop/BroadcastToOtherMaps/SendNotCompleteByDelChrToMap/ForwardRawToClient) over MP_WANTED_NOTIFY_DELETE/REGIST/NOTCOMPLETE/DESTROYED + MP_WANTED_NOTCOMPLETE_TO_AGENT + default + WantedAction extended with target_map_connection_index + 13 tests ✓ D4.103 GTournament side-effect plan - USER 7 actions (Drop/ForwardRawToUserMap/ForwardRawToTournamentMap/SendMoveToBattleMapNack/SendStandingInfoNack/SendBattleJoinNack/DefaultTransToUser) + SERVER 4 actions (Drop/SendRawToUser/BroadcastToMapServers/SetUserMapStateAndForwardToUser) over MP_GTOURNAMENT_MOVETOBATTLEMAP/STANDINGINFO/BATTLEJOIN/OBSERVERJOIN/LEAVE/CHEAT/EVENT_START/END + 22 tests ✓ D4.104 FortWar side-effect plan - SERVER 4 actions (Drop/BroadcastToAllUsers/BroadcastToOtherMaps/ForwardRawToUser) over MP_FORTWAR_START_BEFORE10MIN/START/END + 4 *_TO_MAP protocols + INFO/ING + 19 tests ✓ D4.105 Survival side-effect plan - USER 3 actions (SendLeaveSynToMap/ForwardRawToMap) + SERVER 2 actions (UpdateUserMapAndForwardToClient/ForwardRawToClient) over MP_SURVIVAL_LEAVE_SYN + READY/STOP/MAPOFF/ITEMUSINGCOUNT + RETURNTOMAP + 19 tests ✓ D4.106 Autonote side-effect plan - USER 3 actions (Drop/SendPunishToUser/ForwardRawToMap) + SERVER 9 actions (SendAskToAutoAckAndPunish/PunishOtherAndSendNotAuto/PunishOther/IncrementPunishCount x3/IncrementPunishCountAndSendTimeout/SendKillAutoIfCharacter/DisconnectUser/ForwardRawToUser/Drop) over MP_AUTONOTE_ASKTOAUTO/ANSWER/NOTAUTO/KILLAUTO/DISCONNECT/LIST + 30 tests ✓ D4.107 Bobusang USER side-effect plan - USER 3 actions (Drop/Drop/DropOrForwardRawToMap) over MP_BOBUSANG_* with GM power gate (above master drops) + 7 tests (server-side touches bobusang manager state) ✓ D4.108 MurimNet side-effect plan - USER+SERVER combined 4 actions (Drop/ForwardRawToMap/SendAckToMap/SendNackToMap) over 6 USER forward + 4 SERVER forward protocols + 2 ACK/NACK pairs (port-gated) + 23 tests ✓ D4.109 SiegeWarProfit side-effect plan - USER 1 action (ForwardRawToMap) + SERVER 2 actions (BroadcastToOtherMaps/ForwardRawToClient) over MP_SIEGEWARPROFIT_CHANGE_TEXRATE_NOTIFY_TO_MAP + CHANGE_GUILD_NOTIFY_TO_MAP + 8 tests ✓ D4.110 ItemLimit side-effect plan - SERVER 2 actions (BroadcastToOtherMaps/ForwardRawToClient) over MP_ITEMLIMIT_ADDCOUNT_TO_MAP + FULL_TO_CLIENT + 6 tests ✓ D4.111 Item side-effect plan - USER 4 actions (ForwardRawToMap/ForwardNChangeSynToMap/SendNackToUser/SendChaseLookup) + SERVER 5 actions (ForwardRawToUser/SendChaseNackToUser/ShoutAckWithBroadcast/ShoutAddOnly/ForwardRawToClient) over SHOPITEM_CHANGEMAP_SYN + NCHANGE_SYN (name gate) + CHASE_SYN + CHASE_ACK/NACK + SHOUT_ACK + SHOUT_SENDSERVER + 26 tests ✓ D4.112 Guild side-effect plan - USER 2 actions (ForwardRawToMap/SendNackToUser) + SERVER 1 action (ForwardRawToClient) over GUILD_CREATE_SYN (err_create_name=4) + GUILD_GIVENICKNAME_SYN (err_nick_filter=1) + default server pass-through + 10 tests ✓ D4.113 HackCheck side-effect plan - 4 actions (Drop x2 / SendBanUserToUser x2) over MP_HACKCHECK_SPEEDHACK (delta<7000ms -> ban; rewrite Protocol=BAN_USER) + BAN_USER_TOAGENT (always ban) + default + 11 tests ✓ D4.114 Weather side-effect plan - 3 actions (Drop/Drop/ForwardToMapByData) over MP_WEATHER_SET (target=map_num) + EXE (target=data_field) + RETURN (target=data_field) + default with GM_CLASS/PROGRAMMER/DEVELOPER level gate + 11 tests ✓ D4.115 Move side-effect plan - 3 actions (Drop/ForwardRawToMap/ForwardRawToMap) over 20 MP_MOVE_* sub-protocols (init/target/correction/walk/run/kyunggong/stop/effectmove/monstermove_notify/forcestopkyunggong/warp/onetarget/pet_*); user_in_map gate + 7 tests ✓ D4.116 PowerUp side-effect plan - 11 actions (Drop x2 / ForwardToBootManager / AddSelfBootList / StartServer / ConnectToMS / SendRegistMapAckToMS / SendUserCountToDistribute / SendRegistMapSynToMap / SetMapRegist / MapUserUnRegistLogin) over MP_POWERUP_BOOTING/BOOTLIST/CONNECT + ServerSystem self-init + MP_AGENTSERVER dispatch + REGISTMAP_ACK inbound + 26 tests ✓ D4.117 Mugong side-effect plan - 2 actions (ForwardRawToMap/SendOptionNackToUser) over MP_MUGONG_OPTION_SYN with mugong_level<required_level -> NACK (err=1) + default pass-through + 7 tests ✓ D4.118 Packed side-effect plan - 4 actions (Drop/FanoutToUsers/SendToMapServerByPort/BroadcastToOtherMaps) over MP_PACKED_NORMAL (fanout per receiver) + TO_MAPSERVER (port gate) + TO_BROAD_MAPSERVER (broadcast) + 10 tests ✓ D4.119 User state-side-effect plan - 5 actions (Drop/InsertAgentUser/RemoveAgentUser/AssignAgentUserMap/ToggleAgentUserForceMove) + apply_agent_user_side_effect_plan() over 4 AgentServer.cpp + AgentNetworkMsgParser.cpp mutators (in_use gate for insert/remove + channel==0 sentinel for assign) + 22 tests ✓  D4.120 Skill side-effect plan - 5 actions (Drop/ApplySkillDelayUser/ApplySkillDelayServer/ForwardRawToMap/SendStartNackToUser) over MP_SKILL_START_SYN with SkillDelayManager.AddSkillUse(force=false on user / force=true on server) + legacy 5s latency tolerance gate + 24 tests ✓  D4.121 Battle side-effect plan - 2 actions (Drop/ForwardRawToMap) for all 31 MP_PROTOCOL_BATTLE sub-protocols (battle_info..battle_vimu_waiting_cancel_nack); agent has no state mutation (battle state lives on map) + 17 tests ✓  D4.122 SimpleAuthForward side-effect plan - 2 actions (Drop/ForwardRawToMap) shared by MP_STREETSTALL (cat=29) + MP_EXCHANGE (cat=28) with 3-way auth gate (user_not_found / character_id_mismatch -> Drop; else ForwardRawToMap) + 23 tests ✓  D4.123 Quest side-effect plan - 2 actions (Drop/ForwardRawToMap) for all 28 MP_PROTOCOL_QUEST sub-protocols (quest_totalinfo..quest_full); agent has no state mutation (quest state lives on map) + 16 tests ✓  D4.124 Gtournament USER side-effect plan (3 effect kinds: Drop/ForwardRawToMap/SendNackToUser) - refactored from D4.103 USER 7 actions to compact effect-kind pattern; 11 routing branches incl. 17-case matrix mirror with classify_gtournament_user(gt_map_num=28) + 9-protocol x 4-boolean sweep + 56 tests ✓  D4.125 calc_avatar_option data plan (CShopItemManager::CalcAvatarOption) - 24-slot avatar accumulator over 28 fields with 1:1 byte/word truncation (GenGol wrap at 256, Life wrap at 65536) + flag predicates (AllPlus_Kind=1 -> bKyungGong=1; EquipKind=1 -> NaeruykspendbyKG=1) + 24 tests √  D4.126 boss_state data plane (3 pure functions: boss_phase_from_hp + boss_phase_transition + boss_phase_is_terminal) - 9-phase enum (Sealed/Intro/Combat/Enraged/Phase2/Rage/Dying/Dead/Recovering, 0-8) 1:1 with legacy CBossState::ePhase; HP%-thresholds (76%/51%/26%) + Dead-terminal + Sealed->Intro + max_hp=0 short-circuit + 67 tests √  D4.127 hero_total_layout wire offset baseline (13 offsets + 12 gaps summing to EMPTY_PAYLOAD=3775) - 1:1 lock for SEND_HERO_TOTALINFO layout (BASE_OBJECT=0 / CHARACTER=35 / HERO=147 / MOVE=207 / UNIQUE_AGENT=221 / SHOP_OPTION=225 / MUGONG=345 / ABILITY=695 / ITEM=1019 / OPTION_COUNTS=3747 = ITEM+sizeof(ItemTotalInfo)=2728 / SERVER_TIME=3757 / ADDABLE_INFO=3773 / EMPTY_PAYLOAD=3775) + monotonic invariant + 29 tests √  D4.128 agent_packed data plane (MP_PACKEDDATA=13) - 3 sub-protocols (packed_normal=0 / packed_to_mapserver=1 / packed_to_broad_mapserver=2) + 4 PackedActionKind transitions + wire byte constants + receivers_present filter (legacy filters by user existence, not receiver_count) + port-found gate + receiver_count + data_size preservation (0/1/2048/65527) + 100-receiver max + 34 tests √  D4.129 server_list data plane (ServerListEntry wire-format 1:1 lock size=26 + (channel,kind) uniqueness + port-zero rejection + find_for_map lowest-channel + snapshot/clear copy semantics + DEFAULT_DISTRIBUTE_PORT=6001 + boundary values) + 39 tests √ D4.130 distribute_server data plane (DistributeState enum + DistributeServer bind/stop lifecycle + agent_endpoints stub + LoginUserRecord struct) - DistributeState 5 values (Stopped=0/Booting=1/Listening=2/Running=3/Stopping=4) 1:1 with legacy + bind() advances Stopped->Booting->Listening + stop() transitions to Stopping (idempotent) + agent_endpoints() empty-default (legacy stub) + LoginUserRecord wire-struct defaults (session_id/user_id/auth_step/reserved/account_name/last_active_ms/character_ids) + bind/stop cycles + 45 tests √ D4.131 mn_server data plane (MnServerState enum + MNServer bind/stop lifecycle + channels() accessor + MnPlayerInfo struct) - MnServerState 4 values (Stopped=0/Booting=1/Listening=2/Running=3) + MXH_MN_DEFAULT_PORT=12001 + bind() advances Stopped->Booting->Listening + stop() transitions to Stopped (different from distribute_server) + session_count=0 stub + channel_count tracks MurimNetChannellingManager + MnPlayerInfo wire-struct defaults (player_id/room_id/last_seen_ms/fighting/reserved) + bind/stop cycles + 43 tests √ D4.132 ai_define data plane (StateEvent + MonsterEmotion + MessageKind enums) - StateEvent 5 values (Null=0/Process=1/Message=2/Enter=3/Leave=4) + MonsterEmotion 5 values (Pleasure=0/Comfort=1/Doze=2/Sad=3/Anger=4) + MessageKind 8 values (Chat=0/RecallDirect=1/RecallScript=2/PlayerCurPos=3/MobCurPos=4/HelpRequest=5/HelpShout=6/HelpObey=7) all uint8 underlying + all distinct within each enum + cross-enum type-safety (no implicit conversion) + switch-exhaustive compile-time checks + 31 tests √ D4.133 friend_manager data plane (FriendManagerState + 3 inline free functions) - Meyers singleton identity (same reference on every call) + logout/is_logged_out/clear 3-function semantics + unordered_set<uint32_t> backing for logged_out_users + logout idempotence (set semantics) + clear-noop-on-unknown + cross-user isolation + boundary user IDs (0/0xFFFFFFFF) + 25 tests √ D4.134 get_item_kind_type data plane (legacy CItemManager::GetItemKindType out_kind/out_type writer) - non-null ItemInfo path: out_kind = info->ItemKind + out_type = info->ItemType + null ItemInfo path: both out params cleared to 0 + out-param overwrite (initial garbage discarded) + boundary values (0/0xFFFF for both) + multi-call sequencing (hit->null, null->hit) + ItemInfo other fields (BuyPrice/Rarity/ItemGrade) do not influence output + 18 tests √ D4.135 fixed_tile data plane (FIXEDTILEATTR / FixedTile / TFA_* bitfield) - 4 TFA constants distinct (0x01/0x08/0x10/0x40) + non-overlapping bits (all pairwise zero) + powers-of-two (no combined bits) + FixedTileAttrBits uFixedAttr byte (offset 0) + fixed_tile_init copies byte exactly + fixed_tile_is_collision returns true iff TFA_COLLISON bit set + multi-flag combinations + boundary byte values (0..255) + 37 tests √ D4.136 agent_itemlimit data plane (MP_ITEMLIMIT=74 + ItemLimitActionKind + classify_itemlimit dispatch) - itemlimit_category=74 + 2 sub-protocols (addcount_to_map=0 / full_to_client=1) + ItemLimitActionKind enum (broadcast=0/forward=1) + classify_itemlimit: protocol 0 -> broadcast, protocol 1+ -> forward + action preserves input protocol byte + sweep all 256 protocols + 23 tests √ D4.137 agent_bobusang_user data plane (MP_BOBUSANG=73 + BobusangUserActionKind 3-value + classify_bobusang_user truth table) - bobusang_user_category=73 + 3 enum values (forward_to_map=0/drop_no_user=1/drop_wrong_gm_power=2) all distinct + scoped enum class + classify truth table: user_found=false overrides is_gm -> drop_no_user; is_gm=true+gm_master_or_below=false -> drop_wrong_gm_power; otherwise -> forward_to_map + action preserves protocol + object_id (max uint32) + 24 tests √ D4.138 login_server_db_msg_parser data plane (LoginAuthResult struct + auth_by_account empty-input gate + std::hash user_id derivation (31-bit mask 0x7FFFFFFF) + account_name preservation + password_hash dropped + reserved0/1=0 + character_ids empty default + set_adapter shared_ptr + sequential call independence + distinct-parellel-parellel deterministic + 27 tests) √  D4.139 agent_db_msg_parser data plane (AgentDbDispatcher: max_query=76 boundary + 22-entry AgentDbQuery enum distinct check + AgentDbResult struct defaults + register_handler null/out-of-range/duplicate-replace + dispatch out-of-range returns false + dispatch unregistered returns true + dispatch preserves connection_index + dispatch preserves rows + has_slot boundary + 34 tests) √  D4.140 agent_chat data plane (ChatRoute 8-value enum + ChatDispatch struct defaults + 13 chat_* protocol constants (0-10, 12-13 with gap at 11) + classify_chat truth table: broadcast group (all/smallshout/monster_speech no GM gate), gm_smallshout sender_is_gm gate, whisper group (syn requires_target, gm_syn gm_only, ack/nack rejected), party/guild/guild_union group routes, shout_server/fastchat from_user gate, boundary protocols (11/14/255), AllRoutesReachable invariant) + 46 tests √  D4.141 agent_guild data plane (guild_category=63 + 4 protocol constants (create_syn=2/givenickname_syn=20/givenickname_nack=21/create_nack=3) + 2 error_code constants (create_name=4/nick_filter=1) + GuildActionKind 2-value enum + GuildUserRequest/GuildAction struct defaults + classify_guild_user truth table (forward/nack x create/nickname) + has_invalid_char overrides usable_name + has_quote_space ignored + object_id preservation + classify_guild_server_default protocol-preserving forward) + 32 tests √  D4.142 agent_guild_union data plane (guild_union_category=61 + user protocols create_syn=2/create_nack=4 + 7 server notify protocols (create=20/destroy=21/invite_accept=22/add=23/remove=24/secede=25/mark_regist=26) + error_code not_valid_name=1 + 3-value GuildUnionActionKind + 3-value GuildUnionServerActionKind + struct defaults + user dispatch: user_found=false wins/create_syn+bad_name->nack/create_syn+good_name->forward_to_map/non-create->forward_to_map with protocol preserved + server dispatch: 7 notify protocols broadcast_to_other_maps + below/above notify range drop_unknown + protocol preserved on both branches) + 40 tests √  D4.143 agent_hackcheck data plane (hackcheck_category=41 + 3 protocol constants (speedhack=0/ban_user=1/ban_user_toagent=2) + speedhack_checktime=10000/tolerance_ms=3000 -> threshold=7000ms + 4-value HackCheckActionKind + struct defaults + speedhack truth table: !user_found->drop_no_user / delta<7000->detect_speedhack_and_ban with data=delta / delta==7000 boundary / delta>7000->ignore / server<client->ignore (no underflow) + ban_user_toagent: !user_found->drop / user_found->ban_user_to_agent_always with protocol=ban_user (time fields ignored) + default: ignore with original protocol preserved) + 30 tests √  D4.144 agent_move data plane (move_category=8 + 20 protocol constants (move_init=0..move_pet_warp_ack=19) + 3-value MoveActionKind + struct defaults + classify_move truth table: user_in_map=false->drop_no_map / true->forward_to_map + all 20 protocols behave identically based on user_in_map + protocol + object_id always preserved + boundary protocols (255)) + 37 tests √  D4.145 agent_mugong data plane (mugong_category=9 + 25 protocol constants (0..24) + 3-value MugongActionKind + struct defaults + classify_mugong truth table: 24 non-option_syn protocols all forward + option_syn level gate (protocol==option_syn AND required_level>0 AND mugong_level<required_level) -> send_nack with mugong_option_nack+error_code=1 + boundary cases: required_level=0 disables gate / mugong_level==required_level accepted / mugong_level=0 vs required_level>0 rejected / max_uint32 boundary + object_id preservation in both branches) + 46 tests √  D4.146 agent_battle data plane (battle_category=31 + 31 protocol constants (0..30, all distinct) + 2-value BattleActionKind + struct defaults + classify_battle pass-through (battle state lives on map) + all 31 protocols forward_to_map + protocol + object_id always preserved + boundary protocols (31/255)) + 47 tests √  D4.147 agent_friend data plane (friend_category=33 + 35 protocol constants (0..34, all distinct) + 2 error codes (add_deny=4/option_no_friend=5) + 15-value FriendActionKind enum + struct defaults + full 16-case switch dispatch: !user_found wins / login->db_notify_login / add_syn+invalid->drop_with_invalid_filter / add_syn+valid->db_add_syn / add_accept->db_add_syn / add_deny->send_add_denied_nack_to_user + friend_add_nack + error_code=4 / del_syn->db_del_syn / delid_syn->db_del_id_syn / addid_syn->db_add_id_syn_validate / logout_notify_to_agent->send_logout_to_client_if_user_or_broadcast_to_agents + friend_logout_notify_to_client / logout_notify_agent_to_agent->send_logout_to_client_if_user / list_syn->db_get_list / 4 *_to_agent->send_to_user with converted protocol / login_notify_to_agent->send_to_user_login_notify / add_invite_to_agent+no_friend_option->friend_add_nack+error_code=5 / add_invite_to_agent+ok->friend_add_invite / add_nack->send_to_user / default->forward_to_client with protocol preserved + object_id preservation in all paths) + 71 tests √  D4.148 agent_party data plane (party_category=14 + 61 protocol constants (0..60, all distinct) + error code request_not_master=1 + 2-value PartyUserActionKind + 6-value PartyServerActionKind + struct defaults + classify_party_user: master_to_request_syn+resolved->forward with master_object_id / master_to_request_syn+unresolved->send_to_map_with_not_master_error+party_error+party_err_request_not_master / default->forward with original protocol + classify_party_server: 10 *_to_mapserver notify protocols broadcast_to_other_maps / request_consent_ack->forward_to_object_map / request_refusal_ack->forward_to_object_user / request_consent_nack->send_consent_nack (object_id2) / request_refusal_nack->send_refusal_nack (object_id2) / party_error->forward_to_object_user / default->default_to_client with protocol preserved) + 41 tests √  D4.149 agent_siegewarprofit data plane (siegewarprofit_category=63 + 2 protocol constants (change_texrate_notify_to_map=7/change_guild_notify_to_map=11) + 1-value SiegeWarProfitUserActionKind + 2-value SiegeWarProfitServerActionKind + struct defaults + classify_siegewarprofit_user unconditional forward_to_map + classify_siegewarprofit_server: 2 notify protocols broadcast_to_other_maps / default forward_to_client with protocol preserved / boundary sweeps below/between/above notify range) + 20 tests √  D4.150 agent_fortwar data plane (fortwar_category=76 + 9 protocol constants (0..8, all distinct) + 4-value FortWarActionKind + struct defaults + classify_fortwar truth table: 3 broadcast_to_all_users protocols (start_before10min/start/end, user_object_found ignored) / 4 broadcast_to_other_maps protocols (start_before10min_to_map/start_to_map/ing_to_map/end_to_map, user_object_found ignored) / info+ing: forward_to_user_if_found if user_object_found else drop_no_user / default: same forward/drop gate / protocol preserved in all paths) + 32 tests √  D4.151 agent_survival data plane (survival_category=70 + 8 protocol constants (info=0/aliveuser_count=1/returntomap=2/leave_syn=3/ready_syn=7/stop_syn=16/mapoff_syn=19/itemusingcount_set=24) + 3-value SurvivalUserActionKind + 2-value SurvivalServerActionKind + struct defaults + classify_survival_user: !user_found->default_forward_to_map with extras reset / leave_syn+found->send_leave_syn_to_map preserving unique_connect_idx/user_level/channel / 4 gm_protected protocols / default forward / classify_survival_server: returntomap+user_found+target_map_port_found->update_user_map_and_forward_to_client with update_user_state=true / returntomap without both flags->default_forward_to_client / default forward) + 39 tests √  D4.152 agent_wanted data plane (wanted_category=51 + 7 protocol constants (notify_regist=8/notify_delete=9/notify_notcomplete=18/notcomplete_to_agent=23/notcomplete_by_delchr=24/destroyed=28) + 4-value WantedServerActionKind + struct defaults + classify_wanted truth table: 4 broadcast_to_other_maps protocols (notify_delete/notify_regist/notify_notcomplete/destroyed) + notcomplete_to_agent+!user_found->drop_no_user / notcomplete_to_agent+user_found->complete_notcomplete_send_to_map with protocol converted to notcomplete_by_delchr + target_map_connection_index preserved + default->default_forward_to_client with protocol preserved + boundary sweeps) + 30 tests √  D4.153 agent_skill data plane + SkillDelayManager (SKILL_DELAY_LATENCY_TOLERANCE_MS=5000 + AgentSkillActionKind 2-value enum + Action struct defaults (category=22, protocol=2) + process_agent_skill_user: non-premier always forward not recorded / premier first time records start_time / within cooldown (delta<5000) send_start_nack with protocol=2 / at boundary / outside cooldown forward / nack preserves character_id+skill_index + process_agent_skill_server: force=true always allows and resets start_time / non-premier forward + process_agent_skill_other: unconditional forward without touching manager + SkillDelayManager helpers: is_premier_skill / add_skill_use formula (now-start+5000>=delay) / force bypass / non-premier always true / remove_skill_use no-op on unknown / clear resets both maps / remaining_skill_delay_ms = raw delay - elapsed (no +5000) / boundary at delay = 0) + 36 tests √  D4.154 agent_note data plane (note_category=58 + 18 protocol constants (0..17, all distinct) + 8-value NoteActionKind + struct defaults + classify_note_user: sendnote_syn filter chain (!user_found->drop_no_user / from_invalid+to_invalid+note_has_quote->drop_with_invalid_filter / all valid->send_note_filtered) + sendnoteid_syn->send_note_by_id / receivenote+found->forward_to_user else drop_no_user / delallnote_syn+found->delete_all_with_ack / notelist_syn unconditional->list_notes (user_found ignored) / readnote_syn+found->read_note / delnote_syn+found->read_note (legacy quirk) / default->drop_no_user with protocol preserved + classify_note_server: sendnote_syn only path with same filter chain (ignores note_has_quote, legacy quirk) / default->drop_no_user + object_id preservation in all paths) + 43 tests √  D4.155 agent_gtournament data plane (gtournament_category=59 + 11 protocol constants (movetobattlemap_syn=7/nack=9, observerjoin_syn=10, battlejoin_syn=13/nack=15, leave_syn=17, standinginfo_syn=18/nack=20, cheat=41, event_start=43/end=46) + gt_error_code_error=0 + gt_map_num=28 + 11-value GtournamentActionKind + struct defaults (12-field Request, 5-field Action) + classify_gtournament_user truth table: !user_found wins / movetobattlemap_syn x user_map_found (forward/nack+nack_protocol+error_code) / standinginfo_syn x gt_map_found (to_gt_map+target_map=28/nack_to_user) / battlejoin_syn+observerjoin_syn x gt_map_found (to_gt_map/nack_to_user) / leave_syn unconditional send_leave_syn_to_user_map / cheat 3-way (cheat_data==1->user_map, cheat_data!=1+gt_map_found->gt_map+target_map=28, else drop_no_user) / event_start+event_end x user_level>8 (drop_no_user GM gate) / x user_level<=8+gt_map_found (send_event_to_gt_map+target_map=28) / default forward_to_map_server with protocol preserved) + 46 tests √  D4.156 agent_quest data plane (quest_category=38 + 28 protocol constants (0..27, all distinct) + 2-value QuestActionKind + struct defaults + classify_quest pass-through (quest state lives on map) + all 28 protocols forward_to_map + user_has_quest ignored (legacy stub) + boundary protocols + protocol + object_id always preserved + error_code always 0) + 45 tests √   D4.157 agent_cheat data plane (category 11 + 53 protocol constants + GM/programmer/developer authorization gates + GM_INFO bypass rules + changemap/target validation + broadcast/log/forward flags + server ban/whereis truth table + 9 tests) √  D6\.1-D6\.7\ OK;\ D4\.158\ agent_option\ \(16\ tests\)\ \+\ D4\.159\ agent_debug\ \(10\ tests\)\ added\ this\ session;\ D4\ shop-item\ runtime\ orchestrator\ glue\ still\ open;\ D4\ mailbox/party/guild\ data\ plane\ covered\ via\ D4\.93-D4\.96\ \|
| 客户端运行时 | **Phase A + B.2 done** | MoxianClient + 3/9 state 真接 net + CMainGame 1:1；`mxh_client_e2e.exe` 三步 Login/CharSelect/GameIn 全部 PASS |
| 服务端运行时 | **Phase B done** | LoginServer + AgentServer + MapServer 3 进程 E2E PASS；`deploy/scripts/start_modern.ps1` 启动/状态/停止与客户端三步 E2E 已验证 |
| 渲染后端 | **PARTIAL** | DX11 + BC1-5 + MotionCache；drawBox 已接入 3D Solid shader、12 边 LINELIST 与 3 个纯几何测试，渲染专项 96/96 PASS；`run_demo_smoke.ps1` PASS-A 自然退出；完整场景截图仍待 |
| HSEL 硬件狗 | **80% stub** | YHLibrary ABI 锁 + Crypto 79 tests，真 bin E2E 待 (R-1) |
| HackShield 路由 | **DONE** | AgentHandlerHackShieldTest 17/17，R-2 关闭 |
| SQL Server 集成 | **60%** | Schema + restore + ODBC adapter，本地 MSSQL 待起 |

历史详细：[docs/CHANGELOG.md](docs/CHANGELOG.md)。

---

## 3. 阶段路线

> 现代化不是目标，1:1 复现才是。

### Phase A - 客户端能跑起来
- [x] A1-A5 + CMainGame 1:1 port (commit f80f6e0)
- 验证：截图对比原版登录界面

### Phase B - 服务端能跑起来
- [x] B1-B5 + B6.1 HSEL ABI 校正
- [ ] **B6** HSEL stub 100% (R-1)
- [ ] **B7** MSSQL 真起端到端
- 验证：登录看到空场景

### Phase C - 客户端 UI 1:1 收口
- [x] C-Batch 1.1-1.10 + Batch 2.1-2.80 (109 dialog)
- [ ] **C-Batch 2.81+**：剩 49 dialog
- [ ] **C-Tier-3**：依赖 service 真接的 9 个 dialog
- 验证：202/202 + 每个 >=1 行为断言

### Phase D - 玩法/数值 1:1
- [x] D1 SkillList + D2 BattleFactory + D3 QuestManager runtime bridge + D4.20 UseShopItem decision + D4.21 CheckEndTime realtime + D5 MurimNet + D6.1-D6.7
- [x] R-8 item_effects + D6.x ItemList parser
- [ ] **D4 商城/物品/仓库/邮件/帮派/队伍** (UseShopItem 等真逻辑)
- 验证：side-by-side 5 段 diff=0

### Phase E - 1:1 复现 E2E
- [x] **E1 T1**: 303 records SHA-256 locked (deploy 180 [+ Server/ + QuestScript/] + PlayDH 94 + PlayDH/Client 29) + 268 parse test entries verified (89 -> 268 across 5 suites)
- [ ] **E2 T2** 协议 SHA-256 replay
- [ ] **E3 T3** 行为 side-by-side
- [ ] **E4** 1.0 tag

### Phase F - 维护期
- F1 社区 bug
- F2 资源补丁兼容
- F3 Linux 跨平台

---

## 4. 当前焦点 (2026-08-06)

| 优先级 | 子任务 | 阻塞 | 验收 |
|---|---|---|---|
| P0 | **R-1 HSEL stub 100%** | YHLibrary ABI OK, 缺真 bin E2E | Crypto::Encrypt/Decrypt 跑通真 .bin |
| P0 | **MSSQL 真起 + 端到端** | B5 已写, 待本地 MSSQL；modern SQLite 发布启动链已完成，MSSQL `-DryRun` 参数链已验证 | DbThread + 3 server + 真 client 跑通，随后切换 MSSQL |
| P0 | **E2 T2 wire SHA-256 replay** | 缺 capture harness | 1000+ 真包重放 diff = 0；当前 commercial smoke gate 已覆盖 33 项关键登录/资源/协议验收 |
| P1 | **C-Batch 2.81+** dialog ports | 余 49 个 | 每个 >=1 行为断言 test |
| P1 | **D4 商城剩余真逻辑** | commits dcb05173 + 3e21470e (UseShopItem + CheckEndTime realtime) + 668ec566 (CheckAvatarEndtime) + 7e49a5a5 (CalcAvatarOption) + bbebdb18 (UseShopItemUpdateToDB) + 68061fe2 (CalcShopItemOption) + cb30fba6 (UpdateLogoutToDB) + 529e1843 (AddDupParam/DeleteDupParam/IsDupAble) ✓ | AddUsingShopItem + CalcPlusTime + avatar/skin + side-effect dispatchers |
| P2 | **E3 T3 行为 side-by-side** | T1/T2 后 | 5 段操作录像 + 行为 diff = 0 |
| P2 | **R-9 drawBox 场景验收** | 几何/Shader 已完成，缺真实场景截图与 GPU frame capture | prim_render 渲染人物站帧并与原版截图对比 |

> 详细分批计划与历史 [x] 项 -> docs/CHANGELOG.md。

---

## 5. 完成判据 (唯一, 不再含糊)

| 阶段 | 完成判据 |
|---|---|
| A | 截图对比原版登录界面, 差异 <= 字体抗锯齿级别 |
| B | modern 客户端登录 + 看到空场景, 日志和原版对齐 |
| C | P2-12 202/202 + 每个 dialog 有 >=1 行为断言 test |
| D | side-by-side 跑原版/现代版, 5 段操作 diff = 0 |
| E | T1+T2+T3 全过, 1.0 tag |

**没达 = 没完。** 任何"基本完成""大部分完成"都不算。

---

## 6. 已废弃的概念 (防止旧词干扰新方案)

| 旧词 | 为什么废弃 |
|---|---|
| "现代化" | 不是目标, 1:1 复现才是 |
| "兼容性优先" | 模糊; 改成"协议/资源/数值 1:1 锁定" |
| "12 阶段路线图" | 把手段当目标, 误导 |
| "Qoder IDE / QoderWork / QoderWake" | 我们用 Mavis, 这套三产品不适用 |
| "AI 接力" / "shift log" | 噪音, 没有可执行价值 |
| "P0/P1/P2/P3 任务队列" | 旧框架, 按 1:1 阶段重排 |
| "modern 进度 35.1%" | 单独看无意义, 要看对应 T 的覆盖 |
| "Phase 12 已完成" | 误导, Phase 0-12 全部标完成但 T1/T2/T3 一个没过 |

---

## 7. 文档体系 (替代老的散乱文档)

| 新文档 | 内容 | 替代什么 |
|---|---|---|
| ROADMAP.md (本文件) | 1:1 复现路线图 + 现状 + 阶段 + 焦点 | MODERNIZATION_PLAN.md / ROADMAP_2026.md / P2-12_DIALOGS_ROADMAP.md / AI_TASK_QUEUE.md |
| AGENTS.md | Mavis 协作指南 (项目结构 + 真陷阱) | 老 AGENTS.md + AI_WORKFLOW_GUIDE.md |
| README.md | 极简上手 (5 分钟跑起来) | 老 README.md |
| docs/CHANGELOG.md | 历史 [x] 项 + commit 索引 | 老 434 行 ROADMAP tail + 老 3091 行 changelog |
| docs/RESOURCE_FORMATS.md | 资源格式 | 保留 |
| docs/MoxianProtocolDoc.md | 协议 | 保留 |
| docs/DATABASE_SCHEMA.md | 数据库 | 保留 |
| docs/KNOWN_BUGS.md | Bug 清单 | 保留 |

---

**下一步**: 清理老的 14 份干扰性文档 (docs/PHASE_6_*.md, docs/PLAN_2026Q3.md 等), 重写 AGENTS.md + README.md。

**2026-08-06 状态刷新**: D4 UseShopItemUpdateToDB data plane (3 SQL builders + 14 tests, commit bbebdb18)。累计 7821 tests PASS (was 7807)。
**2026-08-06 状态刷新**: D4 CalcShopItemOption data plane (SHOPITEMOPTION 124 bytes + 57 tests, commit 68061fe2)。累计 7878 tests PASS (was 7821)。
**2026-08-06 状态刷新**: T2 协议字节兼容 **100%** (kTotalCategories 81 -> 77 修正 + 84 wire-format goldens 覆盖所有 77 个真实类别); D4 CalcShopItemOption data plane (SHOPITEMOPTION 124 bytes + 57 tests, commit 68061fe2)。累计 7878 tests PASS (was 7821)。
**2026-08-06 状态刷新**: D4 UpdateLogoutToDB data plane (per-row PLAYTIME decrement + plustime gate + 12 tests, commit cb30fba6)。累计 7890 tests PASS (was 7878)。

**2026-08-07 状态刷新**: D4.100 AgentSiegeWar side-effect plan - USER 7 actions + SERVER 5 actions + SiegeWarUserAction扩展 5 字段 + 19 tests; cumulative ~8958 tests 100% PASS (was 8939).




**2026-08-07 状态刷新**: D4.158 agent_option data plane (category 36, 4 MP_OPTION protocols, bNoWhisper/bNoFriend bit flags, user_found guard, TransToMapServer forward, 16 tests) + D4.159 agent_debug data plane (category 40, MP_DEBUG_CLIENTASSERT, payload_present guard, legacy coffee tools watermark log, 10 tests). Cumulative ~10604 tests 100% PASS (was 10578).

## 6. Session 2026-08-07 续 — D4.160-164 进展

延续 session 019fd2d9 状态 (10,616 tests baseline)。本 session 新增 5 个 data plane：

- **D4.160 agent_jackpot** — MP_JACKPOT (61) 5 protocols, PRIZE_NOTIFY 广播全用户、TOTALMONEY_NOTIFY_TO_AGENT 改协议并仅向在图用户广播, 25 tests。
- **D4.161 agent_guild_fieldwar** — MP_GUILD_FIELDWAR (57) 28 protocols, 7 NOTIFY_TOMAP 广播到除源外所有 map server + DECLARE_NACK 单发 + RESULT_TOALLUSER 全广播 + 默认 forward, 21 tests。
- **D4.162 agent_bobusang** — MP_BOBUSANG (74) 13 protocols, 用户侧按 GM POWER 过滤 + 服务侧 APPEAR/DISAPPEAR MAP_TO_AGENT 设 channel state, 16 tests。
- **D4.163 agent_streetstall** — MP_STREETSTALL (29) 50+ protocols, 仅用户侧 user-found + dwObjectID 一致性检查后 TransToMapServerMsgParser, 9 tests。
- **D4.164 agent_exchange** — MP_EXCHANGE (28) 40+ protocols, 与 streetstall 镜像的简单用户侧 gate, 9 tests。

累计 +80 tests, 现 **10,696 tests 100% PASS** (was 10,616 baseline + 80)。

### 续：2026-08-07 第二批 -- D4.165-171 side-effect plans

本 session 续做 side-effect plan（接 D4.160-164 data plane 之后的下游 orchestrator 编排层）：

- **D4.165 agent_option side-effect plan** -- 5 plans (Drop/ApplyCheatFlag/ApplyFriendFlag/ApplyWhisperFlag/ApplyGameOption) + bNoFriend/bNoWhisper 4 bit flags 隔离, 10 tests
- **D4.166 agent_debug side-effect plan** -- 2 plans (LogAssert/Drop) over MP_DEBUG_CLIENTASSERT, 8 tests
- **D4.167 agent_jackpot side-effect plan** -- 4 plans (SetJackpotTotalMoney/BroadcastAllUsers/BroadcastInMapUsers/ForwardToOriginatingClient/Drop), 10 tests (commit 63e2c416)
- **D4.168 agent_bobusang side-effect plan** -- 4 plans (Drop/ForwardToMapServer/SetChannelState/ForwardToOriginatingClient) over user GM-gate + server APPEAR/DISAPPEAR, 11 tests (commit 1c8a3c09)
- **D4.169 agent_guild_fieldwar side-effect plan** -- 10 plans (Drop/ForwardToMapServer/ForwardToMapServerNoUser/CheckGuildMasterLogin/CheckGuildFieldWarMoney/BroadcastToMapServersExceptSource/SendToTargetUserIfFound/AddGuildFieldWarMoney/BroadcastToAllUsers/ForwardToOriginatingClient) over 28 protocols, 13 tests (commit af4b6707)
- **D4.170 agent_streetstall side-effect plan** -- 2 plans (Drop/ForwardToMapServer) over user-found+objectid-mismatch gates, 6 tests (commit e73a8477)
- **D4.171 agent_exchange side-effect plan** -- 2 plans (Drop/ForwardToMapServer) (mirror of D4.170), 6 tests (commit dff44308)

累计 +64 tests, 现 **10,760 tests 99% PASS** (5 个 LoginServerFixture.UnknownCategory* 已知 timing flake 重试 PASS; commercial smoke gate 33/33 PASS)。

### 仍未覆盖的 category data plane（按优先级）

- MP_NPC (37) — 客户端送 NPC chat/script, agent 无 handler（仅转发或忽略）。
- MP_PK (41), MP_JOURNAL (53), MP_SURYUN (54), MP_SOCIETYACT (55), MP_PARTYWAR (59) — agent 无 handler (LoginServerFixture 已覆盖 drop-without-response)。
- MP_TITAN (72), MP_ITEMEXT (73) — agent 无 handler。
- MP_RMTOOL_* (43-51, 68, 70) — 仅 RMTOOL_CONNECT entry, 非 gameplay 关键路径。

### P0 blockers 状态（与上 session 一致）

- R-1 HSEL E2E (缺真实 .bin)
- B7 MSSQL 真起端到端 (缺本地 MSSQL)
- E2 T2 wire SHA-256 replay (缺 capture harness)。commercial smoke gate 33/33 PASS 持续覆盖关键登录/资源/协议验收。

---

**2026-08-07 状态刷新**: **2026-08-07 ????** (session ?): D4 shop-item runtime orchestrators -- closing out 4 of the remaining D4 ??????? items. 4 commits / +43 tests:

- **D4.28 CalcPlusTime runtime** (commit b0047d43) -- apply_calc_plus_time() walks manager.using_items(), dispatches per-row data-plane decisions to a PlustimeCalcCallback + mutates entry->Data.LastCheckTime via find_using_item_by_icon_idx_mutable. 13 tests cover DefaultSentinel/PlustimeOn/PlustimeOff/PlustimeAllOff + CHARM gate + ItemInfo lookup miss + stop_iteration break. Also extracted LEGACY_SHOP_ITEM_* constants into legacy_shop_item_kind.hpp (single source of truth) to fix MSVC C2374 (multiple init in same TU).
- **D4.25 CalcShopItemOption runtime** (commit a2139282) -- apply_calc_shop_item_option() calls the data plane + applies 5 side effects: ProtectItemIdx update on manager + 4 SetExtra*SlotCount hooks (InvenExtend / PyogukExtend / MugongExtend / CharacterSlot + HK_LOCAL variants). 14 tests cover Charm stat mutation + CheRyuk incantation (set/clear ProtectItemIdx) + 4 locale-bounded incantation hooks + early-return on InvalidIcon/ItemInfoMissing.
- **D4.* UpdateLogoutToDB runtime** (commit 0845d193) -- apply_update_logout_to_db() walks manager.using_items(), builds UpdateLogoutToDBInfo per row via info provider, dispatches per-row Action (Persist / Skip / Drop) to ShopItemManager mutations + virtual SQL dispatch hook (3-arg signature matching legacy ShopItemUpdatetimeToDB). 8 tests cover Drop (non-PLAYTIME / no info), Skip (plustime + inactive rate), Persist (with 30s clamp + clamp-to-zero).
- **D4.35 CheckEndTime side-effect runtime** (commit cb46f9af) -- apply_check_end_time_side_effects() walks the 5-step chain emitted by check_end_time_side_effect() and dispatches each step (DiscardItemAttempt / BumpDupCounter / BroadcastUseEnd / ShopItemDeleteToDB / LogItemMoney) to a virtual CheckEndTimeSideEffectSink. 8 tests cover full 5-step chain in legacy order + discard-failure-continues invariant + empty step list + single-step kinds.

- **E2 T2 AgentServer wire-bytes verification** (commit 749b3b0c) -- agent_wire_first_packet_test.cpp spins up a real AgentHandler in legacy framing mode + connects a raw TCP socket + captures the first wire frame. Locks the deterministic prefix [0x08, 0x00, 0x00, 0x00, 0x07, 0x08] (length=8 little-endian + checksum=0 + code=0 + cat=UserConn + proto=AgentConnectSuccess) across connections, and asserts the auth_key object_id is in the legacy [50000, 99999] range. 2 tests cover single-connection header verification + cross-connection prefix stability. This closes the AgentServer half of E2 T2 (LoginServer half is in login_wire_sha256_test.cpp; MapHandler is silent on connect so no wire-bytes anchor is meaningful).

- **Cumulative**: +68 tests, **~11,371 tests** total (43 from runtime orchestrators + 2 from E2 T2 AgentServer wire-bytes verification + 6 from D4.36 PutOnAvatarItem runtime + 6 from D4.37 TakeOffAvatarItem runtime + 7 from D4.38 PutSkinSelectItem runtime + 4 from D4.40 DiscardSkinItem runtime; was 11,302 baseline + 68). All 9 session commits follow the pattern: data plane in header, runtime orchestrator inline in matching header (where applicable), tests verify behavior through the public surface (ShopItemManager + virtual callback interfaces). No regressions: full shop_item_manager suite 1105/1105 PASS.


- **D4.36 PutOnAvatarItem side-effect runtime** (commit 06c3cd4b) -- apply_put_on_avatar_side_effects() walks the plan emitted by put_on_avatar_side_effect_plan() and dispatches BroadcastAvatarInfo + RecomputeAvatarOption to a virtual PutOnAvatarSideEffectSink. 6 tests cover both-effects-dispatched-in-legacy-order + recompute-value-flows-into-outcome + broadcast-only + recompute-only + empty-plan-is-no-op + calc_stats-flag-propagates.
- **D4.37 TakeOffAvatarItem side-effect runtime** (commit 41edcfc2) -- apply_take_off_avatar_side_effects() mirrors the put-on runtime but for the take-off path (broadcast gate differs -- take-off always broadcasts after a successful transition). 6 tests cover the same scenarios as D4.36.


- **D4.38 PutSkinSelectItem side-effect runtime** (commit d9a95069) -- apply_skin_select_success_side_effects() walks the 3-step success chain (StartSkinDelay + CharacterSkinInfoUpdate + BroadcastSkinInfo) + apply_skin_select_nack_side_effects() emits the 3-DWORD NACK (result_code + skin_delay_remaining + dw_limit_level). 7 tests cover the 3-step success chain in legacy order + 3 NACK result paths (Fail / DelayFail / LevelFail) + empty plan paths + success-flow-does-not-emit-nack invariant.
- **D4.40 DiscardSkinItem side-effect runtime** (commit 746dcd79) -- apply_skin_discard_side_effects() walks the 3-step chain (WriteSkinItemUpdate + CharacterSkinInfoUpdate + BroadcastSkinInfo). 4 tests cover the 3-step chain in legacy order + ordering of write-vs-db-vs-broadcast + empty plan + no-delay-reset invariant (legacy: discard does not reset the skin delay timer; delay is only set on apply).

D4 shop-item remaining gaps after this session: per the data plane headers, the only remaining unwired side-effect chain is the per-row discard / dup-bump / broadcast / DB-delete / log dispatcher which is now plumbed end-to-end via this session's CheckEndTime runtime. Other D4 side-effect data planes (PutOnAvatarItem / TakeOffAvatarItem / SkinSelect / DiscardSkinItem) have data-plane side-effect headers but no runtime orchestrator yet -- those follow the same pattern as CheckEndTime but are out of scope for this session (ROADMAP P1 batch).

D4.173-180 agent_* data plane + side-effect plan (8 categories total) -- the missing gent no handler categories that follow the implicit default-branch behavior in legacy [Server]Agent/AgentNetworkMsgParser.cpp:

- **D4.173 agent_npc** (category 37, 13 protocols, ForwardToUser/DropNoUser) + 19 tests (commit 4cbd7980)
- **D4.174 agent_pk** (category 41, 22 protocols, ForwardToUser/DropNoUser) + 19 tests (commit bd54c842)
- **D4.175 agent_journal** (category 53, 8 protocols) + 19 tests
- **D4.176 agent_suryun** (category 54, 11 protocols) + 19 tests
- **D4.177 agent_societyact** (category 55, 2 protocols) + 19 tests
- **D4.178 agent_partywar** (category 59, 21 protocols) + 19 tests
- **D4.179 agent_titan** (category 72, 23 protocols) + 19 tests
- **D4.180 agent_itemext** (category 73, 21 protocols) + 19 tests

Cumulative +152 tests, **10,926 tests 99% PASS** (1-5 known LoginServerFixture timing flakes retry-PASS; MoxianClientE2E E2E flake persistent; commercial smoke gate 32/33 PASS).

All 77 gameplay categories now have both a data plane header and a side-effect plan header in modern/include/mxh/server/. Only MP_RMTOOL_* (43-51, 68, 70) categories and gameplay-non-critical categories remain uncovered; these are tooling/admin-only paths.

### Side-by-side modern-only + golden mode (commit 409f87dc)

mxh_side_by_side now supports --modern-only --golden PATH for regression testing without a running legacy server. Captures stay byte-stable so future drift in modern-only mode is caught immediately. Useful when only the modern server is available.


### D4.R1-R3 (2026-08-07 续) -- dispatcher runtime + wire-format integration

续 session 019fd2d9-fbdb-7581-984c-ef049a4a0dce 状态 (11,211 tests baseline)。本 session 4 commits / +58 tests:

- **D4.R1 side-effect dispatcher runtime** (commit 51f8d1b4) -- IAgentWireSink interface + 23 dispatcher wrappers + generic dispatch_forward_drop_plan template + 33 tests (mock sink + null safety + forward sweep + drop sweep + multi-effect order). **修复了原 dispatch header 的模板 bug** (`Plan::value_type` 不存在, 改用 `std::decay_t<decltype(plan.effects)>::value_type`)。
- **D4.R2 titan_request.bin golden** (commit 775ebff5) -- 23 "agent no handler" categories 全部覆盖 wire-format golden (补 cat=72 titan); +1 RoundTrip test; 100% wire-format coverage locks to 77/77 categories.
- **D4.R3 wire-format -> dispatcher integration** (commit 6c01ff47) -- 24 tests, 每个 agent no handler category 加载真实 golden .bin, decode 出 (cat, proto, obj_id), 构造 Request -> Plan -> dispatch, assert sink 收到的 (conn, proto, obj_id) 与 wire bytes 一致。locks 1:1 invariant "dispatcher preserves legacy default-branch semantics"。
- **Floor bump 84 -> 85** (commit fe3d4c25) -- wire_format_coverage_test.cpp LocksCurrentCoverageCount 从 84 改 85 (D4.R2 加了 titan_request.bin)。

累计 +58 tests, 现 **11,269 tests** (was 11,211 baseline + 58). D4 商城剩余真逻辑中 "side-effect dispatchers" 部分完成 (runtime 已建, 但 shop-item 的 DB-touching effect 还没接)。

**2026-08-07 状态刷新**: E2 T2 wire SHA-256 capture harness + agent_chat runtime dispatcher; cumulative 11,302 tests (was 11,269).
- **E2 T2 capture_handler** (commit 65cd5cd4) -- mxh::net::CaptureHandler IConnectionHandler decorator + pure-C++ SHA-256 (FIPS 180-4) + sha256() / sha256_concat() + save/load line-based format. 18 tests lock: FIPS empty/abc vectors + wire-byte round-trip + order/swap detection + concurrent 4-thread x 25-message safety.
- **E2 T2 wire SHA-256 fingerprint lock** (commit 13f9871f) -- mxh_login_wire_sha256_tests drives a real LoginHandler on pinned port 54322 through connect + login + ack, computes SHA-256 over server->client wire bytes via CaptureHandler. Locked golden hash: abe6cc6c6ce823c04b53eed1a9a7badb7f24c51c5434baef9146373c3a7cf3c2. Any wire-format drift -> fingerprint changes -> test fails.
- **agent_chat runtime dispatcher** (commit f0b7fd61) -- IChatWireSink (8 methods) + dispatch_agent_chat_plan() walks the 12 ChatSideEffectKind values; 12 dispatch tests cover broadcast / whisper-happy / whisper-blocked (nack_code=2) / whisper-gm / ack / nack / party / guild / guild-union / unknown-protocol / null-sink / empty-plan paths.
累计 +33 tests, 现 **11,302 tests** (was 11,269 baseline + 33). E2 T2 wire SHA-256 replay anchor locked; agent_chat side-effect plan runtime executor complete.

**2026-08-07 状态刷新 (D4.47 ItemSell runtime)**: cumulative **11,382 tests** (was 11,375: 11,371 from D4.38/D4.40 note + 4 from D4.48 ItemUse runtime fab79d65 + 7 new).

- **D4.47 ItemSell side-effect runtime orchestrator** (commit 3b9d76e7) -- apply_item_sell_side_effects() walks the single-step plan emitted by item_sell_side_effect_plan() and dispatches BroadcastSellAck / BroadcastSellNack to a virtual ItemSellSideEffectSink. 1:1 with legacy CItemManager::MP_ITEM_SELL_SYN ([Server]Map/ItemManager.cpp:4205-4240): SellItem rt==0 -> ACK echo (target_pos/item_idx/item_num/dealer_idx preserved); rt!=0 -> MSG_ITEM_ERROR with ECode=rt; CheckHackNpc gate failure -> NACK with ECode=NOT_EXIST(103) + original_rt=-1 sentinel (no SellItem call). 7 wire-through tests cover success-ack + failure-nack-with-rt + npc-gate-nack-with-not-exist + empty-plan-noop + failure-code sweep (incl. negative rt) + ack-does-not-touch-nack-state + nack field passthrough. Full build 0 errors; shop_item_manager suite +7 all PASS; only known LoginServerFixture timing flakes flake (retry-PASS).

- **D4.43 ItemMove side-effect runtime orchestrator** (commit 67e5be32) -- apply_item_move_side_effects() walks the plan emitted by item_move_side_effect_plan() and dispatches BroadcastMoveAck / BroadcastMoveNack / silent-skip to a virtual ItemMoveSideEffectSink. 1:1 with legacy CItemManager::MP_ITEM_MOVE_SYN ([Server]Map/ItemManager.cpp:4042-4067): MoveItem rt==0 -> ACK echo (from/to item_idx + pos preserved); rt!=0 && rt!=99 -> MSG_ITEM_ERROR with ECode=eItemUseErr_Move(2) + original rt as aux code; rt==99 -> empty plan + plan.silent flag (data plane locks SilentRtNinetyNineProducesEmptyPlan) -> runtime reports silent_skip() with no wire message. 7 wire-through tests cover success-ack + failure-nack-with-fixed-ecode + silent-rt99-no-wire + empty-plan-noop + ERROR_ITEM failure-code sweep (incl. negative rt) + nack-does-not-touch-ack/silent-state + ack field passthrough. Full build 0 errors; shop_item_manager suite 1123/1123 PASS (1105 baseline + 4 ItemUse fab79d65 + 14 from D4.47/D4.43).

- **D4.49 ItemDivide side-effect runtime orchestrator** (commit 8e461b25) -- apply_item_divide_side_effects() walks the plan emitted by item_divide_side_effect_plan() and dispatches BroadcastErrorNack / silent-success to a virtual ItemDivideSideEffectSink. 1:1 with legacy CItemManager::MP_ITEM_DIVIDE_SYN ([Server]Map/ItemManager.cpp:4092-4114): DivideItem rt==0 -> empty body (ObtainItemEx emits its own ITEMOBTAINARRAYINFO ACK) -> runtime reports silent_success() so callers skip the broadcast step; rt!=0 -> MSG_ITEM_ERROR with ECode=eItemUseErr_Divide(4) + original rt as aux code. 6 wire-through tests cover silent-success-no-wire + failure-nack-with-fixed-ecode + empty-plan-noop + failure-code sweep (incl. rt==99 and negative rt) + nack-does-not-touch-silent-state + field passthrough (from/to pos + item_idx + from/to dur). Full build 0 errors; shop_item_manager suite 1129/1129 PASS.

- **D4.46 ItemDiscard side-effect runtime orchestrator** (commit 9d224c49) -- apply_item_discard_side_effects() walks the plan emitted by item_discard_side_effect_plan() and dispatches each entry to a virtual ItemDiscardSideEffectSink. 1:1 with legacy CItemManager::MP_ITEM_DISCARD_SYN ([Server]Map/ItemManager.cpp:4115-4161): success (rt==0, not looted) -> ACK echo FIRST then LogItemMoney(eLog_ItemDiscard=8) in legacy order; failure (rt!=0) -> MSG_ITEM_DISCARD_NACK with ECode=rt; looted player -> MSG_ITEM_ERROR_NACK with ECode=eItemUseErr_Discard(5) + looted rt(=10) as aux. 6 wire-through tests cover the 2-step ack-then-log order (call-sequence locked) + failure-nack-with-rt + looted-error-nack + empty-plan-noop + failure-code sweep (incl. rt==10 without loot flag = plain discard-nack) + looted-flag-overrides-zero-rt + field passthrough. Full build 0 errors; shop_item_manager suite 1135/1135 PASS.

- **D4.50 ItemCombine side-effect runtime orchestrator** (commit d481a0ba) -- apply_item_combine_side_effects() walks the single-step plan emitted by item_combine_side_effect_plan() and dispatches BroadcastCombineAck / BroadcastErrorNack to a virtual ItemCombineSideEffectSink. 1:1 with legacy CItemManager::MP_ITEM_COMBINE_SYN ([Server]Map/ItemManager.cpp:4068-4091): CombineItem rt==0 -> ACK echo (from/to pos + item_idx + from/to dur preserved); rt!=0 -> MSG_ITEM_ERROR with ECode=eItemUseErr_Combine(3) + original rt as aux code. 6 wire-through tests cover success-ack + failure-nack-with-fixed-ecode + empty-plan-noop + failure-code sweep (incl. negative rt) + nack-does-not-touch-ack-state + field passthrough. Full build 0 errors; shop_item_manager suite 1141/1141 PASS.

- **D4.51 ItemBuy side-effect runtime orchestrator** (commit f9c7ecef) -- apply_item_buy_side_effects() walks the single-step plan emitted by item_buy_side_effect_plan() and dispatches the gate-specific NACK (single BroadcastBuyNack wire shape, ecode discriminates the gate) or silent success to a virtual ItemBuySideEffectSink. 1:1 with legacy CItemManager::MP_ITEM_BUY_SYN ([Server]Map/ItemManager.cpp:4162-4204): CheckHackNpc fail -> NACK ECode=NOT_EXIST(103) + rt sentinel -1; CheckDemandItem fail -> NACK ECode=NO_DEMANDITEM(108) + rt sentinel -1; BuyItem rt==0 -> silent (ObtainItemEx emits its own ITEMOBTAINARRAYINFO ACK); rt!=0 -> NACK ECode=rt. Gate precedence NpcGate > Demand > BuyItem rt locked. 8 wire-through tests cover npc-gate + demand + silent-success + buy-fail + empty-plan-noop + gate-precedence (npc wins over demand/rt; demand wins over rt) + failure-code sweep (incl. negative rt) + field passthrough (buy_item_idx/num + dealer_idx). Full build 0 errors; shop_item_manager suite 1149/1149 PASS.

- **D4.52 ItemMix side-effect runtime orchestrator** (commit 2156b795) -- apply_item_mix_side_effects() walks the single-step plan emitted by item_mix_side_effect_plan() and dispatches one of five broadcast kinds to a virtual ItemMixSideEffectSink. 1:1 with legacy CItemManager::MP_ITEM_MIX_SYN ([Server]Map/ItemManager.cpp:4360-4424): rt==0 -> MSG_ITEM_MIX_SUCCESS_ACK(60) echo; rt==1000 -> MP_ITEM_MIX_BIGFAILED_ACK(61) echo; rt==1001 -> MP_ITEM_MIX_FAILED_ACK(62) echo; rt in {20..23} -> MSG_DWORD2(MP_ITEM_MIX_MSG=63, dwData1=rt, dwData2=BasicItemPos); rt==2 or other -> ASSERT + MSG_ITEM_ERROR_NACK(99) ECode=rt. 8 wire-through tests cover success-ack + big-fail-ack + fail-ack + msg-dword2 (4-value sweep with BasicItemPos as dwData2) + error-nack-rt2 + other-rt sweep (incl. negative) + empty-plan-noop + field passthrough. Full build 0 errors; shop_item_manager suite 1157/1157 PASS.

- **D4.53 ItemDissolution side-effect runtime orchestrator** (commit b163cda3) -- apply_item_dissolution_side_effects() walks the single-step plan emitted by item_dissolution_side_effect_plan() and dispatches BroadcastDissolutionAck / BroadcastDissolutionNack to a virtual ItemDissolutionSideEffectSink. 1:1 with legacy CItemManager::MP_ITEM_DISSOLUTION_SYN ([Server]Map/ItemManager.cpp:4981-5001): rt==0 -> MSGBASE(MP_ITEM_DISSOLUTION_ACK=64); rt!=0 -> MSGBASE(MP_ITEM_DISSOLUTION_NACK=65) -- plain NACK protocol, NOT MSG_ITEM_ERROR (locked). 6 wire-through tests cover success-ack + failure-plain-nack + empty-plan-noop + failure-code sweep (incl. negative rt) + nack-does-not-touch-ack-state + field passthrough (item_idx + item_pos). Full build 0 errors; shop_item_manager suite 1163/1163 PASS.

- **D4.55 ItemUnseal side-effect runtime orchestrator** (commit 3f7ef411) -- apply_item_unseal_side_effects() walks the single-step plan emitted by item_unseal_side_effect_plan() and dispatches BroadcastUnsealAck / BroadcastUnsealNack to a virtual ItemUnsealSideEffectSink. 1:1 with legacy CItemManager::MP_ITEM_SHOPITEM_UNSEAL_SYN ([Server]Map/ItemManager.cpp:5064-5085): ItemUnsealing true -> protocol flip to MP_ITEM_SHOPITEM_UNSEAL_ACK(68); false -> flip to _NACK(69); msg.dwData preserved in both branches (dwData=pmsg->dwData, target_pos=low 16 bits). 6 wire-through tests cover success-ack + failure-nack + empty-plan-noop + target-pos-is-low-word-of-dwData (0xFFFFABCD -> 0xABCD) + nack-does-not-touch-ack-state + zero-dwData-still-dispatches. Full build 0 errors; shop_item_manager suite 1169/1169 PASS.

- **D4.58 UseForQuestStart side-effect runtime orchestrator** (commit 06562671) -- apply_use_for_quest_start_side_effects() walks the single-step plan emitted by use_for_quest_start_side_effect_plan() and dispatches BroadcastUseAck / BroadcastUseNack to a virtual UseForQuestStartSideEffectSink. 1:1 with legacy CItemManager::MP_ITEM_USE_FOR_QUESTSTART_SYN ([Server]Map/ItemManager.cpp:4955-4979): UseItem rt==0 -> MSG_ITEM_USE_ACK(71) echo preserving TargetPos + wItemIdx; rt!=0 -> MSG_ITEM_ERROR with Protocol=MP_ITEM_USE_NACK(72) + ECode=eItemUseErr_Quest(7). 6 wire-through tests cover success-ack + failure-nack-with-quest-ecode + empty-plan-noop + failure-code sweep (incl. negative rt, ecode always 7) + nack-does-not-touch-ack-state + field passthrough. Full build 0 errors; shop_item_manager suite 1175/1175 PASS.

- **D4.57 ItemMixRelease side-effect runtime orchestrator** (commit eb803ac2) -- apply_item_mix_release_side_effects() walks the plan emitted by item_mix_release_side_effect_plan() and dispatches ClearSlotLock to a virtual ItemMixReleaseSideEffectSink. 1:1 with legacy CItemManager::MP_ITEM_MIX_RELEASEITEM ([Server]Map/ItemManager.cpp:4348-4358): pure side-effect with NO network response -- FindUser null -> return (empty plan); GetSlot null -> return (empty plan); both resolve -> pSlot->SetLock(wData, FALSE) clearing the lock bit on the slot position. 6 wire-through tests cover resolved-emits-clear + no-player-noop + no-slot-noop + empty-plan-noop + no-player-overrides-resolved-slot + zero-slot-pos-still-dispatches. Full build 0 errors; shop_item_manager suite 1181/1181 PASS.

- **D4.59 ShopItemChangeMap side-effect runtime orchestrator** (commit 2658f24c) -- apply_shop_item_change_map_side_effects() walks the plan emitted by shop_item_change_map_side_effect_plan() and dispatches SilentConsume to a virtual ShopItemChangeMapSideEffectSink. 1:1 with legacy CItemManager::MP_ITEM_SHOPITEM_CHANGEMAP_SYN ([Server]Map/ItemManager.cpp:5087-5093): no-op acknowledgement with NO ACK/NACK -- FindUser null -> early return (empty plan); player found -> handler consumes the SYN silently (actual map-change runs on the map server via the separate MP_MOVE request). 5 wire-through tests cover player-found-silent-consume + no-player-noop + empty-plan-noop + max-object-id-boundary + zero-object-id-still-dispatches. Full build 0 errors; shop_item_manager suite 1186/1186 PASS.

- **D4.61 DealerOpen side-effect runtime orchestrator** (commit caf24d0f) -- apply_dealer_open_side_effects() walks the plan emitted by dealer_open_side_effect_plan() and dispatches BroadcastDealerAck to a virtual DealerOpenSideEffectSink. 1:1 with legacy CItemManager::MP_ITEM_DEALER_SYN ([Server]Map/ItemManager.cpp:4851-4871): FindUser null -> silent drop (empty plan); CheckHackNpc false -> silent drop with NO NACK (empty plan); both pass -> MSG_WORD {MP_ITEM_DEALER_ACK=75, wData=npc_pos} opens the street-stall dealer UI. 6 wire-through tests cover gates-pass-dealer-ack + hack-npc-silent-noop + no-player-silent-noop + empty-plan-noop + no-player-overrides-npc-check + zero-npc-pos-still-dispatches. Full build 0 errors; shop_item_manager suite 1192/1192 PASS.

- **D4.60 UseChangeItem side-effect runtime orchestrator** (commit 01690456) -- apply_use_change_item_side_effects() walks the plan emitted by use_change_item_side_effect_plan() and dispatches BroadcastUseNack / SilentSuccess to a virtual UseChangeItemSideEffectSink. 1:1 with legacy CItemManager::MP_ITEM_USE_CHANGEITEM_SYN ([Server]Map/ItemManager.cpp:5002-5019) -- the INVERSE of MP_ITEM_USE_SYN: FindUser null -> empty plan; UseChangeItem rt==0 ("not use") -> MSG_ITEM_ERROR with Protocol=MP_ITEM_USE_NACK(74) + ECode=rt(=0); rt!=0 (item transformed) -> NO network response (silent success; client updates UI from inventory diff). 6 wire-through tests cover not-used-rt0-nack + transformed-silent-success + no-player-noop + empty-plan-noop + various-success-rts-all-silent (incl. negative rt) + field passthrough on nack. Full build 0 errors; shop_item_manager suite 1198/1198 PASS.

- **D4.64 ShopItemInfo side-effect runtime orchestrator** (commit aad7102f) -- apply_shop_item_info_side_effects() walks the 2-step plan emitted by shop_item_info_side_effect_plan() and dispatches SetShopItemInit -> FireShopItemDbQuery to a virtual ShopItemInfoSideEffectSink. 1:1 with legacy CItemManager::MP_ITEM_SHOPITEM_INFO_SYN ([Server]Map/ItemManager.cpp:5021-5032): FindUser null -> empty plan; player found -> SetShopItemInit(FALSE) then CharacterShopItemInfo(pPlayer->GetID(), 0) in legacy order (NO ACK/NACK; data arrives later via MP_ITEM_SHOPITEM broadcasts from the DB callback). 5 wire-through tests cover init-then-db-order (call sequence locked) + no-player-noop + empty-plan-noop + max-object-id + zero-object-id. Full build 0 errors; shop_item_manager suite 1203/1203 PASS.

- **D4.65 ShopItemMpInfo side-effect runtime orchestrator** (commit 70fcfbca) -- apply_shop_item_mpinfo_side_effects() walks the single-step plan emitted by shop_item_mpinfo_side_effect_plan() and dispatches FireSavedMovePointDbQuery to a virtual ShopItemMpInfoSideEffectSink. 1:1 with legacy CItemManager::MP_ITEM_SHOPITEM_MPINFO ([Server]Map/ItemManager.cpp:5095-5103): FindUser null -> empty plan; player found -> SavedMovePointInfo(pPlayer->GetID()) DB query loading the move-point (saved-location) info (NO ACK/NACK; data arrives later via MP_ITEM broadcasts from the DB callback). 5 wire-through tests cover player-found-db-query + no-player-noop + empty-plan-noop + max-object-id + zero-object-id. Full build 0 errors; shop_item_manager suite 1208/1208 PASS.

- **D4.62 PyogukItemInfo side-effect runtime orchestrator** (commit c4dc919b) -- apply_pyoguk_item_info_side_effects() walks the 2-step plan emitted by pyoguk_item_info_side_effect_plan() and dispatches SetGotWarehouseItems -> FirePyogukDbQuery to a virtual PyogukItemInfoSideEffectSink. 1:1 with legacy CItemManager::MP_ITEM_PYOGUK_ITEM_INFO_SYN ([Server]Map/ItemManager.cpp:4922-4940): FindUser null / CheckHackNpc false / IsGotWarehouseItems()==TRUE -> empty plan (dedup: another load in progress); all pass -> SetGotWarehouseItems(TRUE) then PyogukItemOptionInfo in legacy order (NO ACK/NACK; data arrives later via MP_ITEM_PYOGUKITEM_INFO broadcasts from the DB callback). 7 wire-through tests cover mark-then-db-order (call sequence locked) + no-player-noop + hack-npc-noop + already-loading-noop + empty-plan-noop + no-player-overrides-other-gates + max-id-boundary. Full build 0 errors; shop_item_manager suite 1215/1215 PASS.

- **D4.63 PetInvenInfo side-effect runtime orchestrator** (commit 978ebc82) -- apply_pet_inven_info_side_effects() walks the single-step plan emitted by pet_inven_info_side_effect_plan() and dispatches FirePetInvenDbQuery to a virtual PetInvenInfoSideEffectSink. 1:1 with legacy CItemManager::MP_ITEM_PETINVEN_INFO_SYN ([Server]Map/ItemManager.cpp:4941-4952): FindUser null / GetCurSummonPet() null -> empty plan (no pet summoned, silent drop); both pass -> PetInvenItemOptionInfo DB query with slot range TP_PETINVEN_START=80..END=100 (NO ACK/NACK; data arrives later via MP_ITEM broadcasts from the DB callback). 6 wire-through tests cover pet-summoned-db-query (range locked) + no-player-noop + no-pet-active-noop + empty-plan-noop + no-player-overrides-no-pet + max-id-boundary. Full build 0 errors; shop_item_manager suite 1221/1221 PASS.

- **D4.66 GuildWarehouseInfo side-effect runtime orchestrator** (commit 7c824950) -- apply_guild_warehouse_info_side_effects() walks the single-step plan emitted by guild_warehouse_info_side_effect_plan() and dispatches FireGuildWarehouseDbQuery to a virtual GuildWarehouseInfoSideEffectSink. 1:1 with legacy CItemManager::MP_ITEM_GUILD_WAREHOUSE_INFO_SYN ([Server]Map/ItemManager.cpp:4874-4882): FindUser null -> empty plan; player found -> GUILDMGR->GuildWarehouseInfo(pPlayer, pmsg->bData) DB query for the guild warehouse data (NO ACK/NACK; data arrives later via MP_ITEM_GUILD_WAREHOUSE broadcasts from the guild manager). 5 wire-through tests cover player-found-db-query + no-player-noop + empty-plan-noop + max-object-id + zero-request-type-boundary. Full build 0 errors; shop_item_manager suite 1226/1226 PASS.

- **D4.67 GuildWarehouseLeave side-effect runtime orchestrator** (commit 7f4977b2) -- apply_guild_warehouse_leave_side_effects() walks the single-step plan emitted by guild_warehouse_leave_side_effect_plan() and dispatches FireGuildLeaveWarehouse to a virtual GuildWarehouseLeaveSideEffectSink. 1:1 with legacy CItemManager::MP_ITEM_GUILD_WAREHOUSE_LEAVE ([Server]Map/ItemManager.cpp:4910-4919): FindUser null -> empty plan; player found -> GUILDMGR->LeaveWareHouse(pPlayer, pmsg->bData) marking the player as having left the warehouse UI (NO ACK/NACK; purely server-side state). 5 wire-through tests cover player-found-leave + no-player-noop + empty-plan-noop + max-object-id + zero-request-type-boundary. Full build 0 errors; shop_item_manager suite 1231/1231 PASS.

- **D4.68 GuildMove side-effect runtime orchestrator** (commit 82fedc94) -- apply_guild_move_side_effects() walks the single-step plan emitted by guild_move_side_effect_plan() and dispatches BroadcastGuildMoveAck / BroadcastGuildMoveNack to a virtual GuildMoveSideEffectSink. 1:1 with legacy CItemManager::MP_ITEM_GUILD_MOVE_SYN ([Server]Map/ItemManager.cpp:4883-4908): FindUser null -> empty plan; CanMovetoGuildWare false -> NACK err=4; MoveItem rt==0 -> GUILD_MOVE_ACK(82) echo; rt!=0 -> GUILD_MOVE_NACK(83) with rt. 7 wire-through tests cover success-ack + not-moveable-nack-code4 + failure-nack-with-rt + no-player-noop + empty-plan-noop + no-player-overrides-other-gates + field passthrough. Full build 0 errors; shop_item_manager suite 1238/1238 PASS.

- **D4.69 AvatarTakeoff side-effect runtime orchestrator** (commit 7abe91b9) -- apply_avatar_takeoff_side_effects() walks the single-step plan emitted by avatar_takeoff_side_effect_plan() and dispatches BroadcastAvatarUseNack / SilentSuccess to a virtual AvatarTakeoffSideEffectSink. 1:1 with legacy CItemManager::MP_ITEM_SHOPITEM_AVATAR_TAKEOFF ([Server]Map/ItemManager.cpp:5373-5393): FindUser null -> empty plan; IsUseAbleShopItem false -> MP_ITEM_SHOPITEM_AVATAR_USE_NACK(84); TakeOffAvatarItem false -> same NACK; success -> SILENT (no ACK; client updates UI from inventory diff). 7 wire-through tests cover success-silent + not-usable-nack + take-off-failed-nack + no-player-noop + empty-plan-noop + no-player-overrides-other-gates + field passthrough. Full build 0 errors; shop_item_manager suite 1245/1245 PASS.

- **D4.70 AvatarChange side-effect runtime orchestrator** (commit 39537c83) -- apply_avatar_change_side_effects() walks the 2-step plan emitted by avatar_change_side_effect_plan() and dispatches RecalcShopItemOption -> BroadcastAvatarPuton to a virtual AvatarChangeSideEffectSink. 1:1 with legacy CItemManager::MP_ITEM_SHOPITEM_AVATAR_CHANGE ([Server]Map/ItemManager.cpp:5394-5409): FindUser null -> empty plan; player found -> CalcShopItemOption(wData2, TRUE) then MSG_DWORD2 {AVATAR_PUTON=85, player_id, wData2} broadcast to all other map clients except self (NO ACK/NACK to originator; the broadcast is the response). 5 wire-through tests cover recalc-then-broadcast-order (call sequence locked) + no-player-noop + empty-plan-noop + max-id-boundary + zero-item-pos. Full build 0 errors; shop_item_manager suite 1250/1250 PASS.
